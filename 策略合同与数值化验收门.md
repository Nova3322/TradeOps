# 策略合同与数值化验收门

> 文档性质：全市场趋势突破主策略的研究合同、生产准入框架与后续验收基线
> 当前版本：完整目标合同基线
> 当前状态：仅供方案讨论与参数预登记，不构成实盘授权
> 编制日期：2026-07-18
> 适用市场：Binance 与 Hyperliquid 的 U 本位永续合约；覆盖加密货币、美股及股指、贵金属、商品及商品期货参考资产
> 目标交易所：Binance USDⓈ-M Futures、Hyperliquid
> 执行链路：Trading VenueAdapter → 各场所独立 Freqtrade worker，由 Trading 风控中心统一约束
> 上位文档：[交易系统总体方案.md](</Users/vireo/Documents/trading/交易系统总体方案.md>)

---

## 一、文档要解决的问题

[交易系统总体方案.md](</Users/vireo/Documents/trading/交易系统总体方案.md>) 已经说明整个系统是什么、三个项目如何分工，以及人工审核、自动加仓与风险状态的总体边界。

本文件继续回答三个更具体的问题：

1. 一份趋势突破策略必须冻结哪些内容，才有资格被研究、审核和执行。
2. 什么证据能够把一个版本从研究推进到影子盘、小额实盘和逐级扩容。
3. 出现什么情况时，系统必须关闭加仓、降低规模、暂停交易或退回研究阶段。

这份文件不是在宣称当前策略已经盈利，也不是把旧文档中的 20 / 55 突破、固定美元止盈止损或评分权重直接升级为生产参数。它建立的是一套先冻结、再验证、后授权的判定方法。

核心原则是：

> 任何没有被数值化、版本化，并在对应交易所、方向、风险档位和加仓层级上独立验证的能力，都默认关闭。

正期望只能被证据支持，不能被设计文档保证。即使历史证据通过，未来收益也仍然存在不确定性。

---

## 二、适用范围与文档效力

### 2.1 本合同覆盖什么

本合同覆盖目标主策略：

- 从 Binance 与 Hyperliquid 的 U 本位永续合约支持宇宙发现趋势突破候选，并按资产板块形成风险预算。
- 接收用户在 Web / PWA 创建的人工交易假设；人工提案与系统提案共享风险、加减仓、退出和执行合同，但选择收益证据分开归因。
- 研究趋势质量、成交量、持仓量、流动性、波动适配、资金成本和相关性等候选排序维度；正式维度与权重另行冻结。
- 人工审核开仓。
- 开仓时可选择是否签发有限自动加仓授权。
- 只在盈利且保护已经生效后沿原方向加仓。
- 使用硬止损、趋势退出、目标杠杆带动态去杠杆和系统风险状态管理仓位与退出。
- 由 Trading 下属的 `BinanceVenueAdapter` / `HyperliquidVenueAdapter` 把已批准订单意图交给各自独立的 Freqtrade worker 执行。
- 通过自有 Web / PWA 主控制台和 Trading Telegram Bot 管理审核与异常。
- 在 Binance 与 Hyperliquid 上分别完成执行认证。

### 2.2 本合同不覆盖什么

以下内容必须保持独立，不得借用本策略的收益证据：

- 低波动突破策略。它继续保持不加仓，是独立策略。
- DynamicPositionSizing 旧脚本的账户读取、最近订单锚点、95% 可用保证金放大、直接 CCXT 下单和轮询执行实现；其中目标杠杆数学与布林偏差过滤思想只能在重新定义、验证和纳入 Trading 风控后有限迁移。
- Excel 中的几何复利和回撤情景。
- Vault、FundPool 或 RiskControl 的资金治理收益；合格 Vault 资金可以选择性参与风险分母，但不能借用其收益证明交易策略有效。
- 现货、期权、交割合约和链上长尾资产。
- 做市、套利、跨所对冲和自动反手。

### 2.3 文档优先级

出现冲突时，按以下顺序解释：

1. [交易系统总体方案.md](</Users/vireo/Documents/trading/交易系统总体方案.md>) 中不可被绕过的系统原则。
2. 已人工批准并生效、且不违反上位原则的风险政策。
3. 本策略合同及其已认证附件。
4. 旧 Notion 文档、研究笔记和示例。

人工批准不能覆盖 Risk Engine 的硬阻断，也不能把系统级不可变原则改成例外。

旧材料中的 Telegram 逐次确认加仓、保证金乘杠杆决定仓位、所有参数即时热更新等做法，不再代表目标生产语义。非加密底层资产不再被硬排除，但必须经过板块分类、数据、预言机、交易时段和独立执行认证。

### 2.4 系统假设与可证伪边界

本系统建立在以下六条业务假设上，原意保持不变：

1. 市场存在趋势惯性。
2. 强势突破后，部分标的会进入持续趋势。
3. 趋势行情具有右偏收益特征：多数小亏，少数大赚。
4. 成交量和持仓量可以辅助判断趋势的参与度和有效性。
5. 单笔交易结果不可控，但组合风险敞口可以被系统化控制。
6. 浮盈加仓不是为了赌更大，而是在已有利润保护下释放更多风险预算。

这些内容是待验证、可证伪的策略假设，不是系统事实或收益承诺。每条假设都必须映射到版本化指标、时间外样本、失败条件和停用动作；若趋势延续、右偏收益、成交量 / 持仓量增量信息或浮盈加仓的成本后增量价值未通过验收，对应策略或加仓能力保持关闭。第五条属于风险工程目标，但任何系统都无法消除跳空、流动性枯竭、交易所故障和模型错误造成的超预期损失。

---

## 三、先冻结“认证单位”，再谈策略是否通过

不能笼统地说“趋势策略通过了”。

基础认证对象由以下维度共同确定：

**策略主版本 × 参数版本 × 提案来源 × 资产板块 × 标准化底层资产能力 × 方向 × 交易所 / 执行域 × VenueAdapter × Freqtrade worker / config × 受管账户 × margin_mode × collateral_scope × collateral_pool_id × 风险档位 × 加仓层级**

基础对象只有同时持有四类仍然有效的证书，才构成完整生产认证单位：

**策略证据证书 ∩ 执行证书 ∩ 风险覆盖证书 ∩ 审核证书**

- **策略证据证书**：绑定提案来源、数据合同、系统信号或人工择时、止损、加仓、退出、方向、板块和时间外证据，回答该对象是否存在可重复的成本后优势。
- **执行证书**：绑定交易所、Core 或 HIP-3 DEX、VenueAdapter、Freqtrade worker / config、CCXT、受管账户、`margin_mode`、`collateral_scope`、`collateral_pool_id`、订单与保护语义、预言机、容量和真实成本，回答该对象在该执行环境中是否能够安全实现。
- **风险覆盖证书**：绑定风险政策、风险档位、Authorized Capacity、Heat、板块、相关风险簇、清算缓冲、组合限额、Vault 风险计入模式、资金划转模式和降级恢复规则，回答账户与组合是否允许承担该对象的风险。
- **审核证书**：绑定人工审核主体、身份与权限、审批政策、输入输出合同和操作入口，回答谁可以在什么范围内签发初始授权。

四类证书分别编号、分别保存证据、分别失效；不能用策略收益证明执行安全，也不能用低成本成交证明审核质量。某类版本变化时，先使该类证书及其明确依赖失效，不能无理由把其他证据全部作废，也不能把其他证书的通过状态自动继承给它。账户抽象、`margin_mode`、`collateral_scope` 或 `collateral_pool_id` 变化会改变可用抵押和清算传播边界，相关执行证书与风险覆盖证书立即失效。

例如，以下是不同认证对象：

- Binance、低风险、多头、不加仓。
- Binance、低风险、多头、一次加仓。
- Hyperliquid、低风险、多头、一次加仓。
- Binance、中风险、空头、两次加仓。
- Binance、高风险、多头、两次加仓。
- Hyperliquid `xyz`、美股及股指板块、人工提案、高风险、多头、三次加仓。

### 3.1 允许继承的证据

- 相同信号逻辑可以共享“是否存在方向优势”的研究资料。
- 相同数据合同可以共享数据质量验证方法。
- 相同风险语言可以共享定义。
- 同一策略主版本可以共享固定不变量。

### 3.2 不允许自动继承的证据

- Binance 的成交、费用、资金费、保护订单和容量证据不能证明 Hyperliquid 已通过。
- 多头结果不能自动证明空头结果。
- 低风险通过不能证明中风险或高风险通过。
- 不加仓通过不能证明一次加仓通过。
- 一次加仓通过不能证明第二次或第三次加仓通过；第二次通过也不能证明第三次通过。
- 系统信号提案不能借用人工看图择时的收益，人工提案也不能借用系统排名贡献。
- 加密货币板块通过不能证明美股及股指、贵金属或商品板块通过；Hyperliquid Core 通过不能证明某个 HIP-3 DEX 通过。
- 同一波市场行情中高度相关的多个标的，不能被当成完全独立的多次成功。

### 3.3 合同状态

每个认证单位只能处于以下一种状态：

| 状态 | 含义 | 是否允许真实增险 |
| --- | --- | --- |
| 草案 | 规则或门槛仍有空项 | 否 |
| 研究冻结 | 合同和验收门已预登记 | 否 |
| 时间外通过 | 历史时间外证据通过 | 否 |
| 重放通过 | 异常与订单生命周期重放通过 | 否 |
| 影子通过 | 实时只观察验证通过 | 否 |
| 模拟通过 | 模拟 OMS 和对账通过 | 否 |
| 小额实盘 | 仅在批准容量内运行 | 是 |
| 已认证 | 在批准范围内逐级运行 | 是 |
| 降级 | 缩小范围、档位、加仓层级或容量 | 仅限降级后范围 |
| 暂停 | 只保护、减仓、退出和对账 | 否 |
| 退役 | 不再生成新提案 | 否 |

只要一个关键参数仍为空，相关认证单位最多只能处于“草案”。

---

## 四、已经固定的策略不变量

以下内容不再作为普通研究参数随意调整：

1. 支持范围为 Binance 与 Hyperliquid 可发现的 U 本位永续合约，覆盖加密货币、美股及股指、贵金属、商品及商品期货参考资产；真实交易逐标的和逐执行域认证。
2. 首批交易所为 Binance 与 Hyperliquid。
3. 主策略为全市场趋势突破风险预算策略。
4. 信号只生成候选，不直接生成订单。
5. 先决定最大允许损失，再根据止损距离、成本和容量决定数量。
6. 3x、5x、10x 是低、中、高风险档位的最大有效杠杆，不是必须使用的杠杆。
7. 系统提案与人工提案的初仓都必须人工批准，并由 Risk Engine 自动审核；超额风险直接拒绝。
8. 人工审核时必须明确自动加仓开关。
9. 自动加仓不逐次人工审核，但只能消费开仓时签发的有限授权。
10. 自动加仓默认关闭；启用后系统最多三级，低、中、高默认次数上限为 1 / 2 / 3，单笔可以选择更少。
11. 加仓只沿原方向，只做盈利金字塔，不允许亏损摊平。
12. 止损、目标杠杆带动态去杠杆、风险减仓和紧急退出不等待人工审核。
13. Risk Engine 对人、策略、OMS 和任何辅助建议都拥有最终否决权。
14. 系统只能自动收紧风险，不能自动扩大授权。
15. 人工批准后交易所被锁定，不能自动换所、拆单到另一所或跨所补救。
16. 同一经济仓位只能有一个策略所有者。
17. Freqtrade 是受 Trading 控制的底层执行引擎，不拥有开仓价值判断或风险放宽权。
18. Trading 分别通过 `BinanceVenueAdapter` 与 `HyperliquidVenueAdapter` 管理两所；每个 VenueAdapter 的订单后端是独立 Freqtrade worker。每个 bot / config 只使用一种 `margin_mode` 并独占受管账户；Binance cross、Binance isolated 与 Hyperliquid isolated 使用独立账户或子账户、worker、配置和证书。Hyperliquid Core 与每个 HIP-3 DEX 仍是独立执行认证域。
19. Web / PWA 是主控制台，Telegram 是受限辅助入口；二者都只能向 Trading 提交操作意图。
20. RiskControl / NoTilt Vault 可以按风险政策选择性计入 Current Portfolio MTM Equity 和扩展风险权益，但风险计入开关与资金划转开关相互独立；自动运营补充默认关闭，且即使未来启用也只服务空仓后的下一交易周期，绝不救援活动仓位。
21. 用户决定入场，Trading / Freqtrade 链路是受管交易账户的唯一日常订单发送者。
22. 30% / 50% / 100% 只解锁对应 Add 的候选资格；实际加仓还必须同时通过当前盈利、`L_effective < L_min`、布林偏差回调过滤、保护、风险、成本、流动性和执行门。
23. `L_effective > L_max` 时生成不消费 Add 单元的 `reduce-only` 动态去杠杆目标；“回调”不是基于历史峰值的回撤规则，也不默认撤销 Add 或按加仓层减仓。
24. Add 不配置“本级新增数量 / 风险比例”；每次默认以当前 `E_campaign` 和冻结 `L_target` 计算到目标杠杆的净数量差，系统只授权和消费加仓次数。风险、保证金、流动性或执行门可以把该数量安全缩小，但不能放大。
25. `1R` 在待审核交易提案版本冻结时按 `Total Capital Snapshot_0 × 0.5%` 冻结；低、中、高风险单笔最大允许亏损分别为 1R / 2R / 3R，即初始总资本的 0.5% / 1.0% / 1.5%。
26. 每笔交易的资金占用由独立 `FundingEnvelope_0 = Total Capital Snapshot_0 × trade_funding_pct` 限制；`FundingEnvelope_0` 的展示名称为 Trade Funding Envelope。`trade_funding_pct = N%` 可配置，1.5% 仅为示例，正式默认 `N` 待资金政策冻结。资金占用不是风险，不能与 1R、最大亏损或 Heat 混用。
27. 每次 Add 前必须按最新 `Current Portfolio MTM Equity` 重算组合、板块和场所动态预算；该权益包含未实现盈亏，Vault 只按冻结资本政策纳入，并排除在途、重复和未知资金。冻结授权与当前动态预算取更严格者，盈利不能自动扩大原授权。
28. 用户使用可叠加标签授权；观察、提案、审核、运营、风险、资金和系统管理权限相互分离，任何入口都不能因一个用户拥有多个标签而绕过职责分离、显式拒绝、复核或审计。

改变以上任一不变量，需要产生新的策略主版本，并重新建立证据链。

---

## 五、哪些内容仍然只是研究假设

旧资料已经提出了一些合理方向，但它们尚未被证明是生产最优值。

| 项目 | 当前候选方向 | 当前效力 |
| --- | --- | --- |
| 信号周期 | 4 小时、1 日 | 研究候选 |
| 突破窗口 | 快速约 20 根、慢速约 55 根 | 研究候选 |
| 信号确认 | 已完成 K 线收盘确认 | 倾向性基线，仍需冻结适用范围 |
| 排名维度 | 趋势、成交量、OI、流动性、波动适配、成本、相关性 | 研究候选 |
| 评分权重 | 旧文档中的 0.35 / 0.20 / 0.15 等 | 仅作为起始假设 |
| 默认加仓候选解锁 | 冻结收益率达到 30% / 50% / 100% | 当前业务默认；只解锁 Add 候选，不构成执行触发，仍需通过全部共同硬门 |
| Add 目标杠杆 | 每次按当前 `E_campaign` 计算到冻结 `L_target` 的数量差 | 固定数量语义；不配置每级数量 / 风险比例，风险与执行约束只可安全缩量 |
| 止损模型 | 结构、波动或二者组合 | 待研究 |
| 趋势退出 | 反向突破、结构失效或排名持续恶化 | 待冻结 |
| 目标杠杆带 | `L_target / L_min / L_max`，低于下限才允许继续评估加仓，高于上限动态去杠杆 | 数学思想固定，权益归属和各档数值待研究与逐执行域认证 |
| 回调过滤 | 当前价格相对布林参考线的偏差区间 | 语义固定为加仓过滤器；周期、窗口、标准差倍数、参考线与区间待冻结 |
| 初仓有效期 | 时间与价格双边界 | 待研究 |
| 价格偏离 | 用波动、点差与可执行价格共同定义 | 待研究 |
| 单笔最大允许亏损 | 低 / 中 / 高为 `Total Capital Snapshot_0` 的 0.5% / 1.0% / 1.5% | 固定风险定义，对应 1R / 2R / 3R；1R 固定为 0.5% |
| 单笔资金占用比例 | `trade_funding_pct = N%`；1.5% 仅为示例 | 待按资金政策冻结；它生成 `FundingEnvelope_0`，不是风险比例 |
| 加仓次数 | 低 / 中 / 高默认最多 1 / 2 / 3 次，单笔可以更少 | 默认档位上限；系统硬上限为 3 级 |
| 组合 Heat | 2.5%、1.25%、1.5% 等 | 研究默认值 |
| 风险状态回撤门 | 约 1.0%、1.5%、2.5%、4.0% | 研究默认值 |

只有表中标注为“研究候选”“研究默认值”或“待研究”的项目属于本节候选值；3x / 5x / 10x 杠杆上限、1R = 初始总资本 0.5%、低 / 中 / 高最大亏损 = 1R / 2R / 3R、自动加仓默认关闭、低 / 中 / 高最多 1 / 2 / 3 次、Add 默认加到冻结 `L_target` 以及 30% / 50% / 100% 候选解锁阶梯是已确认的业务默认。该阶梯绝不是单条件自动加仓指令；业务默认仍需能力认证后才能进入实盘，也不得被实现团队自行替换。`trade_funding_pct` 的具体值仍待资金政策冻结，示例 1.5% 不能被实现为隐式默认值。

---

## 六、参数分级与变更后果

为防止研究参数被无意当成运行配置，所有项目分为五类：

| 类别 | 内容 | 变化后果 |
| --- | --- | --- |
| C0 策略不变量 | 市场、信号语义、风险定仓、加仓哲学、退出类型 | 新主版本，完整重验 |
| C1 研究参数 | 周期、突破窗口、权重、过滤门、止损、加仓和退出阈值 | 新参数版本，重新时间外验证 |
| C2 风险政策 | 单仓、标准化底层资产、风险簇、资产板块、交易所 / DEX 执行域、`collateral_scope`、`collateral_pool_id`、Vault 风险计入模式、资金划转模式、组合风险与回撤门 | 收紧可立即生效；放宽需批准和复验 |
| C3 外部运行事实 | 盘口、费率、资金费、交易规则、杠杆档位、账户状态 | 每次实时读取；陈旧或未知则阻断 |
| C4 非交易配置 | 界面、通知、展示和报告样式 | 可日常调整，不得改变交易结论 |

每个研究参数都需要记录：

- 名称与唯一身份。
- 清晰定义与单位。
- 候选范围和最终冻结值。
- 适用方向、周期、交易所和风险档位。
- 依据的数据与证据状态。
- 变化后需要重验的范围。
- 负责人、批准时间和失效动作。

生产运行不能使用未登记默认值，也不能在回撤期间临时调参来解释或修复表现。

存量仓位默认绑定开仓时的策略、参数、风险和执行快照：

- 新策略版本默认只影响新提案和新仓。
- 风险政策收紧可以立即覆盖存量仓位。
- 风险放宽不能把旧仓静默迁移到新版本。
- 新版本改变止损、加仓或退出时，旧仓继续按原合同管理；若发现安全缺陷，只能冻结、减仓或退出。
- 任何跨版本迁移都必须成为显式、可审计的新决策，不能在后台自动替换。

---

## 七、市场、数据与信号合同

### 7.1 可交易标的池

支持宇宙是 Binance、Hyperliquid Core 与所有可发现 HIP-3 DEX 暴露的全部 U 本位永续合约，不再硬排除 `xyz` 等市场中的股票、股指、贵金属和商品类底层资产。Catalog 必须先发现和分类，再由 DEX / 标的的 `approved` 与 `certified` 状态决定 `eligible_to_trade`；生产候选还必须同时通过目标账户与司法辖区可用性、交易状态、报价 / 抵押资产、数据、流动性、成本、预言机、板块风险和执行认证。

“U 本位”在本合同中只指使用风险政策批准的美元稳定资产作为报价或抵押的永续合约，例如账户实际支持的 USDT / USDC；币本位和未认证稳定币不在当前范围，稳定币也必须按可验证市值与脱锚压力计价。

每个标的必须绑定版本化身份：`venue + dex + instrument_id + normalized_underlying + sector_id + quote/collateral + contract_spec_version`。`sector_id` 只能是加密货币、美股及股指、贵金属、商品及商品期货参考资产或未分类；未分类的风险上限为零。

历史研究中的标的池必须能够还原到当时：

- 当时已经上线且真实可交易。
- 当时没有进入退市、只减仓或规则异常状态。
- 当时满足策略预登记的最低流动性、容量和数据质量。
- 下架、改名、流动性衰退和失败资产不能从历史中被删除。
- Binance、Hyperliquid Core 与 HIP-3 DEX 的同名底层资产需要映射到统一标准资产，但仍保留各自合约、DEX 和预言机事实。

非加密底层资产还必须能够历史还原交易时段、休市与节假日、预言机更新时间、公司行动、指数方法、商品基准、传统期货换月和异常结算。不能把休市、预言机停更、拆并股或换月跳变当成普通低波动或突破信号。

使用今天的标的清单回测过去，属于不可接受的幸存者偏差。

### 7.2 价格口径必须分工

合同不能只写一个“价格”：

- 信号价格用于判断突破是否形成。
- 可成交价格用于估计真实入场和退出。
- Mark Price 用于保证金、清算和权益监控。
- 保护触发价格由对应交易所执行附件冻结。
- 资金费和结算价格用于完整成本。

不同价格口径不能在结果不利时临时互换。

### 7.3 K 线与事件时间

- 默认只允许已完成 K 线参与收盘确认型信号。
- 未完成 K 线只能形成观察提示，不能伪装成已确认信号。
- 市场发生时间、系统接收时间、候选生成时间和审核时间需要分别保留。
- 迟到、乱序、重复和修复数据必须可追踪。
- 相同数据版本、策略版本和参数版本必须得到相同候选结果。

### 7.4 信号只产生候选

一个突破信号至少要经过：

**候选资格 → 绝对流动性与成本门 → 排名 → 组合冲突检查 → 风险分配 → 冻结提案**

排名只能决定优先级，不能让高分标的绕过：

- 流动性下限。
- 最大点差和滑点边界。
- 资金成本边界。
- 交易所健康状态。
- 单一底层资产、风险簇和板块上限。
- 系统风险状态。

### 7.5 Perptape 与 Trading 的边界

- Perptape 提供市场发现、突破告警与市场数据上下文。
- Trading 必须使用目标交易所的最新公开与私有事实重新核验。
- Perptape 不决定风险、不签发授权、不下单，也不作为真实仓位事实源。
- Trading 通过目标场所的 VenueAdapter 重新核验并标准化订单语义；对应 Freqtrade worker 不重新判断信号是否值得交易，只执行 VenueAdapter 转交的、已经批准并实时复核的 Trading 意图。
- Perptape 断开不能影响已有仓位的硬止损和紧急退出。

---

## 八、提案、审核与授权合同

### 8.1 三个对象不能混为一谈

1. **冻结交易提案**：说明系统信号或人工交易假设在风险定仓后准备做什么。
2. **有限授权**：说明人批准的最大边界是什么。
3. **一次性执行意图**：说明 OMS 此刻准备执行哪一个具体动作。

人工审核的是提案，不直接编辑交易所订单。任何影响风险的修改都必须形成新提案和新审核。

提案有两种来源：

- **SYSTEM**：系统候选、排名和策略资格产生草案。
- **MANUAL**：用户在 Web / PWA 输入图表交易假设。人工草稿必须先经过风险定仓，再由作用域匹配的 `REVIEWER` 明确批准冻结版本；提案人与审核人只有命中版本化 `self_review_policy` 时才可为同一身份。填写或保存草稿本身没有下单效力。

两种来源从 Risk Engine 开始使用同一授权、订单和退出链，但择时与收益证据分开归因。人工请求数量超过风险核准数量时必须拒绝，并展示可接受上限；不能静默缩量后直接执行。

MANUAL 提案必须选择一个已经冻结的仓位管理模板。若该假设不满足全市场趋势突破的信号定义，就归属于独立 `MANUAL_HYPOTHESIS` 策略所有者；它可以复用风险、订单和退出基础设施，但不能借用 SYSTEM 趋势策略的正期望证书。

### 8.2 一份提案必须冻结的内容

- 提案来源、提案人、交易所、Core / HIP-3 DEX、账户或子账户、标的、标准化底层资产、板块和方向。
- 策略、参数、风险、数据与成本版本。
- 系统提案的信号形成时点、排名与候选失效条件，或人工提案的交易假设、决策周期和失效依据。
- 触发价、订单类型、委托价、允许价格区间、最大滑点、`reduce_only`、`valid_from`、`valid_until / time-in-force`。
- 初始止损、趋势失效、目标杠杆带动态去杠杆与布林偏差回调过滤语义。
- 用户请求数量、风险核准数量、预计有效杠杆和档位上限。
- `Total Capital Snapshot_0` 及其资本范围与时间戳；冻结交易 1R 的货币金额、档位倍数和最大允许亏损。系统按最坏允许成交、费用、滑点和止损穿透复算。
- `trade_funding_pct`、`FundingEnvelope_0` / Trade Funding Envelope 金额、资金来源和 CTO 结算状态；资金占用与风险预算分别展示、分别校验。
- 开仓后的单标的、标准化底层资产、相关风险簇、板块、交易所 / DEX 和组合风险。
- 自动加仓是否开启。
- 加仓风险档位、最多次数、30% / 50% / 100% 候选解锁版本、`L_target / L_min / L_max`、回调过滤版本和授权最长生命周期；不得出现每级新增数量或风险百分比字段。
- 当前交易所规则、VenueAdapter、Freqtrade worker / config、账户、`margin_mode`、保证金能力、`collateral_scope`、`collateral_pool_id`、余额和风险快照。
- 风险资本模式、Exchange Risk Equity、Eligible Vault Equity、Extended Risk Equity、Vault 贡献版本，以及与之独立的资金划转模式和 Capital Transfer Orchestrator 政策版本。

初仓 `reduce_only` 必须为 `false`；自动减仓、止损和退出必须为 `true`。方向、触发价、委托价、数量、有效期或最大风险无法映射到目标交易所时，提案直接失败。

### 8.3 初仓授权与加仓授权是两个生命周期

**初仓授权**只在较短的执行窗口和价格边界内有效。

**加仓授权包**绑定同一交易所、账户、标的、方向、策略版本和仓位生命周期。它只有在以下事实全部成立后才激活：

- 初仓已经真实成交。
- 真实仓位已经完成对账。
- 足额保护订单已经被交易所确认。
- 当前系统状态允许自动加仓。

开仓批准不是无限期自动交易许可。

初仓、Add-1、Add-2 和 Add-3 是四个独立的一次性授权单元。低风险只签发到 Add-1，中风险只签发到 Add-2，高风险才可签发 Add-3；每个单元都具有唯一身份，并只能处于以下一种状态：

- 休眠。
- 可用。
- 消费中。
- 已消费。
- 已撤销。
- 已过期。
- 已失效。

同一个单元同一时刻只能被一个执行意图锁定。30% / 50% / 100% 达标只使对应单元具备生成候选的资格；某次候选因 `L_effective`、回调、风险、成本或其他门不满足而未锁定时，单元保持可用，后续新事实可以重新形成候选，但不得形成隐形排队任务。执行意图可以把单元暂时置为“消费中”以阻止并发重复；只要该意图终态确认实际成交量为零，释放 Reserved Heat 后单元恢复“可用”，不计一次 Add。任何大于零的真实成交都会永久消费该单元，部分成交的继续处理仍属于原执行意图，不得另开一次 Add；发送结果未知时只能查询原意图，不能释放或生成新的授权消费。Add-1 消费不会自动耗尽 Add-2 或 Add-3，也不能让 Add-1 再次使用；之后发生的动态去杠杆不返还、不重新武装已消费 Add。

授权记录还要保留审核主体类型、身份、审核政策和版本。

### 8.4 授权上限不等于风险预留

这是本合同必须固定的新边界：

- **Authorized Capacity** 表示人工最多允许系统做什么。
- 它不会在整个仓位生命周期内长期占用组合 Heat。
- 当某一级加仓真正满足条件时，系统才短期申请 **Reserved Heat**。
- 若当时没有组合容量，则该次加仓直接跳过。
- 预授权不保证未来一定能加仓，也不允许抢占、排队或借用其他仓位额度。

这样既保留了“加仓无需逐次人工审核”，也防止大量尚未触发的授权耗尽组合容量。

### 8.5 授权失效

以下情况使受影响的初仓或剩余加仓授权失效：

- 超过时间、价格或仓位生命周期边界。
- 交易所、账户、标的或方向变化。
- `margin_mode`、账户抽象、`collateral_scope` 或 `collateral_pool_id` 变化。
- 止损被放宽、取消或人工改成未授权状态。
- 策略结构、价格口径或风险放宽版本变化。
- 仓位、挂单、余额、成交或保护无法对账。
- 系统进入 NO_PYRAMID：所有剩余加仓授权失效，已经签发且仍有效的初仓授权仍需重新通过实时风控。
- 系统进入 NO_NEW_POSITION 或更严格状态：尚未消费的初仓和加仓授权全部失效。
- 用户撤销授权。
- 仓位完全退出。

风险状态恢复到 NORMAL 后，已经失效的加仓授权不会自动复活。若仍想加仓，需要重新人工签发新的有限授权。

### 8.6 扩权与收紧

以下属于扩大授权，必须重新人工审核：

- 低风险升中风险或高风险。
- 中风险升高风险。
- 持仓后重新开启自动加仓。
- 增加剩余加仓次数。
- 增加最大损失、最终仓位、价格容忍或授权期限。
- 更换交易所。

以下可以立即收紧：

- 关闭自动加仓。
- 降低风险档位。
- 减少剩余加仓次数。
- 缩小仓位、价格容忍或授权期限。
- 收紧止损和组合风险。

收紧只会立即阻止尚未发送的动作。已经发送的订单需要进入撤销和对账：

- 已确认撤销的未成交部分才能释放 Reserved Heat。
- 撤销或成交结果未知的部分转入 Unknown Heat。
- 真实仓位超过新档位时，立即禁止后续增险，并按冻结政策继续保护或受控减仓。

### 8.7 Web / PWA 与 Telegram 不改变授权语义

自有 Web / PWA 是统一主控制台，供电脑和手机查看信号、审核开仓、设定风险档位、管理仓位和处理异常。

只有 Web / PWA 提供完整人工提案页，并展示用户请求数量、风险核准数量、资金占用金额、冻结 1R、动态组合预算、四板块风险、Vault 风险计入模式、独立的资金划转状态和三档 1 / 2 / 3 级加仓预览。启用或提高 Vault 贡献必须进入风险政策变更流程；资金是否允许划转属于另一项权限，二者都不能作为单笔提案字段随手联动。

Trading Telegram Bot 负责：

- 接收开仓提案、成交、止损、加仓、风险预警和系统异常。
- 批准或拒绝一份仍然有效的开仓提案。
- 关闭自动加仓。
- 暂停新开仓。
- 提交紧急减仓请求。

Telegram 不能任意指定标的、方向、仓位或杠杆开仓，不能创建完整人工提案、提高风险、恢复新开仓或直接调用 Freqtrade。所有操作先形成 Trading 中的一次受限意图，并通过身份、授权、幂等、账户和 Risk Engine 校验；拒绝、暂停和关闭加仓留在 Trading，交易订单才交给 Freqtrade，保证金动作交给 Margin Controller，资金划转交给 Capital Transfer Orchestrator。

授权的语义来自同一冻结提案、同一身份验证和同一审计记录，不取决于动作来自 Web 还是 Telegram。拒绝、关闭加仓和暂停可以立即收紧；恢复或扩权回到 Web / PWA 完成更强审核。

PWA 离线时只允许显示带时间戳的最后快照，不能离线批准或假定操作成功。电脑、手机和 Telegram 的动作都必须收到 Trading 的唯一回执；重复、延迟或已过期操作无效。

旧状态中的“等待 Telegram 确认”应统一解释为“等待开仓审核”；不再存在“等待加仓逐次人工确认”。

### 8.8 多标签用户与权限合同

一个用户可以同时拥有多个标签，但标签只授予明确动作，不代表用户取得某个界面的全部能力：

| 标签 | 默认允许 | 默认禁止 |
| --- | --- | --- |
| `OBSERVER` 观察者 | 查看其作用域内的信号、提案、仓位、风险、资金和审计结果 | 创建、审核、下单、改风险或划转资金 |
| `PROPOSER` 提案者 | 创建、编辑和提交 MANUAL 提案 | 批准提案、绕过 Risk Engine 或直接执行 |
| `REVIEWER` 审核者 | 在授权板块、场所、账户和风险档位内批准或拒绝初仓提案 | 修改提案事实、直接下单、放宽风险政策 |
| `OPERATOR` 运营者 | 关闭自动加仓、暂停新开仓、发起已授权紧急减仓、处理对账与恢复流程 | 创建资金授权、提高风险、绕过唯一订单入口 |
| `RISK_ADMIN` 风险管理员 | 提交或批准其作用域内的风险政策变更、冻结和降级 | 资金划转、直接下单；单人完成同一项放宽的提出与批准 |
| `TREASURY_ADMIN` 资金管理员 | 按资金政策发起或复核 Vault / 场所划转 | 交易提案审核、改变交易风险或借划转救仓 |
| `SYSTEM_ADMIN` 系统管理员 | 管理身份、标签、作用域、MFA 和系统配置 | 默认不继承提案、审核、风险、资金或交易权限 |

权限决策采用“标签允许的动作并集 ∩ 账户 / 场所 / 板块 / 风险档位 / 金额 / 时间作用域”，再应用显式拒绝；**显式拒绝永远优先**。一个用户拥有多个标签不会自动豁免职责分离：默认禁止提案者审核自己的提案，风险放宽、资金划转和高风险初仓按冻结政策要求双人复核；确需允许自审的低风险场景也必须形成单独版本化政策，而不是依赖标签并集。

审核、风险放宽、资金划转、恢复增险、紧急减仓和标签管理必须使用 MFA 或动作级重新认证。每次身份判定都记录用户、全部标签、有效作用域、设备 / 会话、认证强度、请求、规则命中、显式拒绝、最终决定和时间；标签变更立即撤销旧会话中的扩权能力，不能依赖前端隐藏按钮实现权限控制。

---

## 九、风险合同

### 9.1 风险权益、当前组合 MTM 权益、抵押身份与资本范围

**交易所保证金权益（Exchange Margin Equity）**用于监控交易所实际保证金、清算、可发送数量和账户回撤，包含未实现盈亏。

任何保证金权益和可用容量都必须绑定两个不可省略的身份字段：

- `collateral_scope`：本执行域中可以共同承担保证金与清算风险的账户、DEX、抵押资产和 margin mode 边界。
- `collateral_pool_id`：该抵押池的版本化唯一身份；不同池不能因同属一个用户或钱包而合并可用余额。

仓位、订单、保护、Heat、VenueAdapter、Freqtrade worker 和执行证书必须引用同一组抵押身份。账户从 Standard 切换 Unified / Portfolio Margin、cross 与 isolated 切换、子账户迁移、DEX 抵押归属变化或任何会改变共享抵押边界的动作，都必须生成新的 `collateral_scope / collateral_pool_id` 并使原执行与风险覆盖证书失效。

**交易所风险权益（Exchange Risk Equity）**用于交易所和账户级新增风险门，采用更保守口径：

- 已对账抵押品和已实现盈亏按可变现价值计入。
- 未实现亏损全部扣减。
- 未实现浮盈不用于扩大账户级新增风险。
- 预计费用、资金费、未决负债和转出预留扣减。
- 稳定币按可验证市值保守计价。
- 每个交易所和全组合维护经外部入金、出金和跨所转账调整后的权益高点。
- 未到账、无法对账或无法在交易所确认的资金不计入。
- 任一关键账户事实未知时，默认全局新增风险为零；只有完成双所硬隔离认证后，才按独立剩余额度处理例外。

**扩展风险权益（Extended Risk Equity）**是初始授权、账户和跨场所风险的第二道保守上限：

`Extended Risk Equity = Exchange Risk Equity + Eligible Vault Equity`

- `EXCHANGE_ONLY` 是默认风险计入模式，Eligible Vault Equity 为零。
- `EXCHANGE_PLUS_VAULT` 需要独立风险政策批准，Eligible Vault Equity 只按认证 Vault 的确认余额扣除 pending、源端预留转出、在途和费用后，再应用保守价格、资产、流动性、控制权和时效折扣，并受独立贡献上限约束。它是唯一的 Vault 净贡献账本，Current Portfolio MTM Equity 与 Extended Risk Equity 只引用该净值，不各自重复扣减。它只改变风险分母，不自动打开任何资金划转权限。
- 只有所有 active whitelist 均属于批准控制集合，且不存在未批准的待激活 whitelist、权限扩张或名单变化时，Vault 才具备贡献资格。未知 whitelist、待激活权限扩张、名单变化、Panic Lock、Full Exit pending、owner 变更、未知资产、RPC / 价格分歧或状态陈旧时，对应 Vault 贡献归零并要求重新审批。
- 启用或提高 Vault 贡献属于扩权；贡献下降可以自动收紧，恢复不能自动放宽。
- Extended Risk Equity 不替代 Current Portfolio MTM Equity：前者不借未实现浮盈扩大保守容量，后者完整包含最新正负未实现盈亏，用于每次决策的实时组合预算。任一层更严都要收紧或拒绝。

**当前组合 MTM 权益（Current Portfolio MTM Equity）**是每次初仓和 Add 风险复核使用的最新组合事实：

`Current Portfolio MTM Equity(t) = Σ 已确认交易所 Margin Equity(t) + Eligible Vault Equity(t) - 尚未在前两项扣除的其他组合负债(t)`

- 每个交易所的最新 Margin Equity 必须包含正负未实现盈亏，并按同一时间边界、已认证 Mark 和成本口径重建；未实现亏损立即收紧预算，未实现盈利虽然进入最新 MTM 分母，却不能突破已经冻结的单笔授权。
- Vault 是否纳入、采用何种资产折扣、价格新鲜度和贡献上限由资本政策冻结；未纳入的 Vault 余额不能临时加入分母。
- 同一笔资金在源端预留、链上在途、跨链、场所确认和 Vault 确认之间只能计入一次。在途、结果未知或无法对账的金额已在 Eligible Vault Equity 源账本中归零，不得在 Current Portfolio MTM Equity 尾部再次扣减。
- 任一关键交易所、Vault、价格、仓位、订单或转账事实未知时，禁止初仓和 Add；不能沿用旧的较高权益快照。

**Current Total Capital** 不是另一套实时口径，而是待审核交易提案版本冻结时对 `Current Portfolio MTM Equity` 的冻结快照；其规范字段名为 `Total Capital Snapshot_0`。它同时生成该战役的 1R 和 Trade Funding Envelope，包含快照时的未实现盈亏，但之后不因行情、入金、出金或加仓而重写。风险预检重跑若会改变该快照、资金信封或 1R，必须生成新的冻结提案版本并重新等待审核；审核或发送前复核不得静默刷新原版本。

**扩展资本 MTM 权益（Extended Capital MTM Equity）**沿用同一资本范围形成 C-DD 时间序列。Vault 纳入 / 移出、入金、出金和转账按外部流量调整高点，避免把资本范围变化误判为盈亏；事实不可验证时 C-DD 为 `UNKNOWN` 并触发风险冻结。

即使使用 `EXCHANGE_PLUS_VAULT`，最终仓位仍必须同时通过基于目标交易所自身 Exchange Risk Equity 的单所风险上限，以及 Exchange Margin Equity 的可用保证金、清算缓冲与交易所规则。Vault 不是交易所余额，未完成 Capital Transfer Orchestrator 的真实到账不能成为可下单保证金，也不能让 Freqtrade 超过交易所可执行容量。

Vault 到交易所的资金迁移必须在 `VAULT_CONFIRMED → TRANSFER_IN_FLIGHT → EXCHANGE_CONFIRMED` 中互斥记账，任何时刻只能计入一处；在途资金贡献为零。RiskControl / NoTilt Vault 与 Hyperliquid Vault / Subaccount 必须使用不同身份和账本。

### 9.2 场所资金目标与 Capital Transfer Orchestrator

每份待审核交易提案版本先冻结独立资金信封：

`FundingEnvelope_0 (Trade Funding Envelope) = Total Capital Snapshot_0 × trade_funding_pct`

- `trade_funding_pct` 是可配置的资金政策参数，1.5% 只是示例，不是硬编码默认值。
- 资金信封表示本战役最多可占用的已结算抵押 / 运营资金，不表示最大允许亏损；例如 1.5% 的资金占用在 10x 杠杆下也绝不等于 1.5% 风险。
- 场所已有运营金必须在首个初仓订单前完成余额对账和 `FundingEnvelope_0` 原子预留，不虚构 CTO 目的端流程。只有初仓前从 Vault 划入的资金需要经 Capital Transfer Orchestrator 完成 `DESTINATION_CONFIRMED → SETTLED`，并在目标交易所余额中可验证。
- 未结算资金不能支持初仓。仓位生命周期开始后，初仓和 Add 只能使用已经结算且属于该信封 / 场所运营池的资金与同仓权益；不得因加仓、浮亏、保证金不足或临近清算中途从 Vault 补充救仓资金。
- `FundingEnvelope_0` / Trade Funding Envelope、冻结最大亏损、实际保证金占用和名义仓位必须在界面与审计账本中分别显示，任何两项都不能互相替代。

每个交易场所和 `collateral_pool_id` 都要冻结资金政策：目标分配 `venue_operating_target_pct = n%`，由配置周期开始时的 Managed Settled Capital 换算目标金额 `T_v`，再设置补充下沿 `T_low` 与归集上沿 `T_high`，且满足 `T_low < T_v < T_high`。正式比例和金额属于资金政策附件中的未决参数，不能由策略按单仓临时修改。

`venue_allocation_ratio = 场所已确认运营资本 ÷ 全部已批准运营资本`

- 处于 `[T_low, T_high]` 内时不因小幅漂移划转，避免资金来回震荡。
- 场所空仓、无增险挂单、无 Unknown Heat、全部成交与余额已对账后，超过 `T_high` 的已实现盈利按冻结政策自动归集至目标区间；未实现浮盈不触发归集。
- 低于 `T_low` 只形成运营补充候选。自动运营补充默认关闭；即使未来经独立认证启用，也只能在该场所空仓后为下一交易周期补充，绝不向活动仓位追加救援资金。执行前还必须确认 Vault 没有 Panic Lock、Full Exit pending、owner / whitelist 权限漂移，目标地址、资产、网络和额度符合批准策略，并通过等待期、授权有效期和状态新鲜度检查。
- 活动仓位存在时，资金不足或清算缓冲不足只能关闭新增风险、动态去杠杆或退出；不能以“恢复目标 n%”为理由救仓。
- 人工发起 Vault → 交易所、交易所 → Vault 或批准账户之间的双向划转，也必须经过同一个 Capital Transfer Orchestrator，不能使用旁路脚本。来自 Vault 的 `MANUAL_CAPITAL_INJECTION` 同样只允许目标场所空仓且不临近清算；活动仓位、未决增险或清算缓冲告急时一律拒绝，防止把人工划转变成救仓旁路。

Capital Transfer Orchestrator 至少保存：`REQUESTED → RISK_CHECKED → APPROVED → SOURCE_CONFIRMED → SOURCE_RESERVED_OUT → WITHDRAW_SUBMITTED → TRANSFER_IN_FLIGHT → DESTINATION_CONFIRMING → DESTINATION_CONFIRMED → SETTLED`；拒绝、失败、取消和结果未知使用独立终态或待对账态。Vault → 交易所进入 `SOURCE_RESERVED_OUT` 时先从 Vault 源端资金账本和 Managed Settled Capital 扣除；若该笔源资金原本已计入风险资本，则在唯一 Eligible Vault Equity 账本中只扣减一次，Current Portfolio MTM Equity 与 Extended Risk Equity 随后同源重算。在途资金贡献为零，只有来源扣减和目标到账都完成对账后才能结束互斥记账。

目标地址、资产、网络、额度与等待期等 whitelist wallet 限制默认属于 Capital Transfer Orchestrator 的版本化软件政策；除非执行附件能以已验证合约代码和链上状态证明，否则不得宣称它们是 Vault 合约本身的硬限制。软件政策变化同样需要审批、审计和重放，但不能借“合约会阻止”省略 CTO 校验。

必须分开两个开关：

- `risk_inclusion_mode`：决定 Vault 是否按冻结折扣同时进入 Current Portfolio MTM Equity 和 Extended Risk Equity；两者仍分别使用实时 MTM 与保守容量语义。
- `capital_transfer_mode`：决定是否仅允许人工划转，或是否启用已认证的空仓自动归集 / 下一周期运营补充。

打开风险计入不等于允许划转；允许划转也不等于允许计入风险分母。任何自动划转权限都是独立扩权，不能随 `EXCHANGE_PLUS_VAULT` 一并隐式打开。

### 9.3 冻结 1R 与动态组合预算

为防止加仓后重新定义风险，美化绩效，必须区分：

- **1R**：`Total Capital Snapshot_0 × 0.5%`，在待审核交易提案版本冻结时固定为货币金额，贯穿整个仓位生命周期，用于初仓、加仓、退出和最终结果归因。
- **档位最大允许亏损**：低风险为 1R，即初始总资本 0.5%；中风险为 2R，即 1.0%；高风险为 3R，即 1.5%。这是整份经济仓位从初仓到全部 Add 的合计上限，不是每次 Add 各获得一份新 R。
- **动态组合风险用量**：每次初仓和 Add 前，按最新 `Current Portfolio MTM Equity`（包含未实现盈亏）重算组合、板块、风险簇与场所预算；交易所保证金和清算门继续按 Exchange Risk Equity / Exchange Margin Equity 取更保守结果。

对风险档位 `m ∈ {1, 2, 3}`：

`Frozen Trade Loss Cap = Total Capital Snapshot_0 × 0.5% × m`

`Dynamic Mode Cap(t) = Current Portfolio MTM Equity(t) × 0.5% × m`

每次 Add 的批准数量必须同时满足两类独立门：本经济仓位的 Trade Worst-Case Loss 取 `Frozen Trade Loss Cap`、`Dynamic Mode Cap(t)` 和 Authorized Capacity 中的最严格值；单一底层资产、风险簇、板块、场所与组合的 Scope Worst-Case Loss 分别受各自当前剩余预算限制。不得把某一聚合作用域的总损失直接与单笔档位上限比较。1R 不因浮盈、入金或加仓而重新放大；最新权益上升只会让动态预算不再更紧，不能自动扩大原始授权，最新权益下降则立即缩小或取消可加仓数量。

### 9.4 风险账本

| 项目 | 含义 | 状态变化 |
| --- | --- | --- |
| Authorized Capacity | 人工批准的最高边界，不是 Heat | 授权过期、撤销、耗尽或仓位结束 |
| Reserved Heat | 即将执行或在途增险动作占用的风险 | 明确失败、撤销并对账，或转为 Open Heat |
| Open Heat | 真实仓位到有效保护价的成本后计划风险 | 真实减仓或退出完成并对账 |
| Unknown Heat | 结果未知订单按最坏可成交情景计入 | 查询并确定真实结果后 |
| Stress Heat | 计入止损穿透、流动性下降、费用和无保护窗口后的压力风险 | 随最新事实和冻结压力场景重新计算 |
| Protected Profit | 经有效保护与保守折扣后的同仓浮盈 | 随保护价和真实仓位重算，只作同仓分析 |

平仓请求已经发送不等于风险已经释放。只有真实仓位和相关订单完成对账，Open Heat 才能下降。

计划 Heat 必须满足互斥和守恒：

- 每一份可能成交的数量在同一时刻只能属于 Reserved、Open 或 Unknown 中的一类。
- 发送增险意图前，按最坏允许成交占用 Reserved Heat。
- 已知成交部分从 Reserved 原子地转为 Open。
- 已知仍在途的未成交部分继续保留 Reserved。
- 已确认撤销或拒绝的未成交部分才可以释放。
- 结果未知的对应部分从 Reserved 转为 Unknown，不能同时漏记或重复计入。
- 计划风险硬门检查 Open + Reserved + Unknown；只有交易所事实完成对账后才能迁移或释放。

Stress Heat 是使用冻结压力场景重新计算的结果，不是第四个可消费余额，也不与计划 Heat 含混相加。它需要单独的压力上限和场景版本。

Protected Profit 只是分析性备忘指标：

- Open Heat 按有效保护价计算，最低为零，不能成为负数。
- 同一份受保护利润在最坏净损失中只能使用一次。
- 若它已经通过保护价降低 Open Heat，就不能再次作为额外额度独立增加加仓容量。
- 它不进入风险权益，也不转给其他仓位。

每次 Add 前必须基于同一个最新决策快照计算加仓后的分层最坏情况损失：

`Trade Worst-Case Loss_after = 本经济仓位的 Open Heat_after + Reserved Heat_after + Unknown Heat + Protected-Profit Giveback + Fee / Slippage / Funding Stress Add-on`

`Scope Worst-Case Loss_after(scope) = Σ scope 内各经济仓位 / 订单的互斥计划损失 + scope 专属成本与压力附加项`

- `Protected-Profit Giveback` 表示从当前 Mark 回落到已确认保护 / 压力退出价时可能吐回的利润；它不能因已经记录为 Protected Profit 而漏算。
- Open、Reserved、Unknown、回吐和成本附加项必须在风险附件中定义互斥归属，防止同一层同一损失重复计算；Stress Heat 仍按独立压力场景校验，而不是机械地再叠加一次。
- Trade Worst-Case Loss_after 只与本交易的冻结 / 动态档位上限和 Authorized Capacity 比较；底层资产、风险簇、板块、场所和组合的 Scope Worst-Case Loss_after 分别只与各自预算比较。任一事实陈旧、Unknown Heat 无法封顶或任一层最坏损失不可计算时，批准数量为零。

### 9.5 三档风险框架

| 档位 | 固定最大有效杠杆 | 自动加仓默认状态 | 启用后的默认次数上限 | 最大允许亏损 |
| --- | ---: | ---: | ---: | --- |
| 低风险 | 3x | 关闭 | 1 次 | 1R = `Total Capital Snapshot_0` 的 0.5% |
| 中风险 | 5x | 关闭 | 2 次 | 2R = `Total Capital Snapshot_0` 的 1.0% |
| 高风险 | 10x | 关闭 | 3 次 | 3R = `Total Capital Snapshot_0` 的 1.5% |

系统支持 0 到该档默认上限的选择，并以三级为硬上限；持仓中增加次数或超过三级属于扩权且不受支持。系统不配置初仓 / 各级 Add 的固定数量比例或风险份额；初仓按止损和档位风险定仓，每次 Add 按目标杠杆差额计算。本仓 Trade Worst-Case Loss 不得突破档位最大允许亏损、Authorized Capacity 或动态单笔上限；板块、风险簇、场所与组合的聚合 Scope Worst-Case Loss 分别不得突破各自边界。

选择某档不表示必须用满杠杆。实际杠杆取以下约束中的最小值：

- 风险仓位需求。
- 用户批准的档位上限。
- 交易所与标的实际上限。
- 保证金和清算缓冲。
- 盘口容量和压力成本。

### 9.6 标的、板块和经济仓位不能被拆分规避

同一策略、同一标准资产、同一方向构成一份经济仓位。

- 同一底层资产拆到 Binance、Hyperliquid Core 或 HIP-3 DEX，仍合并计算底层资产和相关风险簇。
- 反方向仓位不能默认完全抵消。
- 多个高度相关标的在同一突破波次中，按风险簇和独立趋势战役共同约束。
- 同一账户净仓位模式下，不同策略不能同时争夺同一标的。

每个标的必须先分配到加密货币、美股及股指、贵金属、商品及商品期货参考资产之一，并占用对应板块 Heat；未分类标的不能增险。板块上限之外仍需计算跨板块压力簇，不能用分类拆分规避共同宏观风险。

每个风险政策版本都要为四个板块分别冻结 `Sector Authorized Capacity`、`Sector Heat Cap`、`Sector Stress Heat Cap`、单一底层资产上限和交易所 / DEX 子上限。正式数值未冻结时该板块只能研究或影子展示；提高任一板块上限属于风险扩权。

### 9.7 系统风险状态

| 状态 | 允许行为 |
| --- | --- |
| NORMAL | 可生成提案、人工开仓、已授权自动加仓、减仓和退出 |
| NO_PYRAMID | 禁止加仓，剩余加仓授权失效 |
| NO_NEW_POSITION | 禁止开仓和加仓，只管理现有仓位 |
| REDUCE_ONLY | 只允许取消增险、保护、减仓和退出 |
| KILL_SWITCH | 停止扩大风险，确认事实并受控保护、减仓、退出或人工接管 |

状态可以自动收紧，不能自动放宽。恢复必须完成：

- 真实账户和订单对账。
- 根因说明和修复证据。
- 滞回、冷却和复发观察。
- 人工逐级解锁。

---

## 十、VenueAdapter、Freqtrade 与初仓执行合同

### 10.1 开仓前全部条件

只有以下条件同时成立，Trading OMS 才能创建一次性初仓执行意图，并交给对应 VenueAdapter，再由该适配器转交其独立 Freqtrade worker：

- 提案、人工审核和初仓授权仍有效。
- 当前价格仍在批准边界内。
- SYSTEM 提案的信号仍有效；MANUAL 提案的触发、决策周期和失效条件仍有效；盘口、交易规则和账户事实仍新鲜。
- 交易所、标的、方向、保证金模式和杠杆未变化。
- 单标的、底层资产、相关风险簇、板块、交易所 / DEX 和组合风险仍合格。
- Reserved Heat 已成功占用。
- 没有未知订单、残余仓位或对账分歧。
- 部分成交、最低有效仓位、机会失效和保护规则已经冻结。

条件不成立时，提案应过期或失败，不能自动追价、换所或放宽风险。

Freqtrade 在此处是受控执行 worker：

- 不持有人工审批权。
- 不解释或放宽风险政策。
- 不接受 Web、Telegram、FreqUI 或外部服务直接增险。
- 不因自身策略信号、自动仓位调整或内置控制命令绕过 Trading 建仓或加仓。
- 不用自身 ROI、跟踪止损或仓位调整逻辑与 Trading 的退出仲裁和加仓授权竞争。
- 不在结果未知时自行重放旧意图。
- 执行状态回传 Trading，最终仓位和成交仍以交易所私有事实为准。

### 10.2 三条生命周期并列存在

不能用一条“开仓中”状态同时表达所有事实：

| 生命周期 | 关注问题 |
| --- | --- |
| 提案与审核 | 提案是否待审、批准、拒绝、放弃或过期 |
| 授权 | 初仓和加仓权限是否休眠、有效、消费、撤销或失效 |
| 仓位与订单 | Trading 与 Freqtrade 的风险预留、订单在途、部分成交、未知、保护或退出事实 |

三条生命周期由唯一交易身份关联，但各自独立判断。

### 10.3 部分成交与保护

- 任何真实成交都立即成为真实仓位。
- 保护必须覆盖交易所实际仓位，而不是原计划数量。
- 未成交部分不能被假定存在，也不能转成 Open Heat。
- 结果未知时进入 Unknown Heat 并查询，不盲目重试。
- 若无法在合同窗口内形成足额保护，停止新增风险并受控减仓或退出。

### 10.4 交易所选择

系统可以在提案生成前根据点差、深度、资金费、容量和健康状态推荐交易所。

人工批准后交易所即锁定。任何换所、拆单到另一所或跨所补救都产生新提案。

### 10.5 VenueAdapter、Freqtrade worker 与账户隔离

- `BinanceVenueAdapter` 与 `HyperliquidVenueAdapter` 都属于 Trading；它们负责交易所能力映射、规则核验、订单标准化、结果归一化和对账边界，不拥有信号判断、人工审批或风险放宽权。
- 每个 VenueAdapter 的订单后端是该场所独立的 Freqtrade worker。Binance 与 Hyperliquid 不共享 worker、配置、数据库、凭据、订单身份或故障域；Hyperliquid Core 与每个 HIP-3 DEX 继续分别认证。
- Freqtrade 每个 bot / config 只配置一种 `margin_mode`，不得在同一 bot 内混用 cross 与 isolated。cross 和 isolated 分别使用专用账户或子账户、独立 worker / config、独立凭据、数据库、日志、`collateral_pool_id` 和执行证书。
- 杠杆账户必须由单一 Freqtrade bot 独占；不得让第二个 bot、外部脚本或日常人工交易与其共享账户事实。每个受管账户同一时刻只允许一个活动订单发送者，并需要防双主隔离。
- 每个实例绑定策略所有者、批准容量、`collateral_scope` 和 `collateral_pool_id`；账户抽象或抵押池变化后，worker 只能进入冻结对账，不能沿用原证书恢复增险。
- 研究、模拟和生产账户完全分离。
- Binance 凭据只授予交易所需权限并禁止提款；Hyperliquid 使用专用 API Wallet，不使用主钱包私钥。
- 人工在交易所紧急减仓后，Trading 冻结该账户并重新对账。
- VenueAdapter、Freqtrade、CCXT、bot config、账户抽象和交易所执行附件作为一个版本组合认证；任一关键变化不自动继承旧认证。
- FreqUI 仅可作为受限诊断工具，不能成为生产主控制台或旁路操作入口。
- NORMAL、NO_PYRAMID、NO_NEW_POSITION、REDUCE_ONLY 和 KILL_SWITCH 持久保存在 Trading；Freqtrade 重启后默认只对账，在 Trading 重新下发当前状态前不得恢复增险。
- Trading 核心只保留 Venue Port 与能力合同作为未来场所的扩展边界；未出现明确业务需求时，不预建 CFD 或其他场所的适配器、分支、配置或数据实体。未来若独立立项，接入方至少冻结合约乘数、tick / lot、最小数量 / 名义价值、价格源、交易时段、到期 / 换月 / 现金结算、`collateral_scope / collateral_pool_id`、订单保护和对账语义，且不得继承 Binance / Hyperliquid 的证书；缺一项只能只读。
- 入金、提款、Vault 释放、Bridge 和场所间资金划转不进入可写 VenueAdapter，也不使用 Freqtrade 凭证；独立 `CapitalTransferAdapter` 只接受 Capital Transfer Orchestrator 的资金授权。

---

## 十一、自动加仓合同

### 11.1 用户在开仓前选择的内容

每份开仓提案必须明确：

- 自动加仓：开启或关闭。
- 风险档位：低、中、高。
- 最大加仓次数。
- 30% / 50% / 100% 默认候选解锁阶梯或其参数版本。
- 冻结 1R、档位最大允许亏损、Authorized Capacity 与动态组合预算政策版本。
- 加仓授权最长生命周期。
- `L_target / L_min / L_max`、布林偏差回调、保护、成本、流动性和失效参数版本。
- 逐仓是否启用 `POST_REDUCTION_MARGIN_NORMALIZATION`、目标保证金缓冲及最大可回加额；它随仓位管理模板预授权，不在减仓发生后临时扩大。

用户不设置 Add-1 / Add-2 / Add-3 的固定新增数量、初始仓百分比或风险份额。用户关闭自动加仓时，系统只管理基础仓、止损和退出。

### 11.2 自动加仓的共同硬门

每一级 Add 先是可重复评估的候选资格，只有以下条件在同一决策快照中全部满足，Trading 才能把对应一次性 Add 单元从“可用”原子地转成“消费中”：

1. 对应加仓授权有效且尚未消费。
2. 对应 30% / 50% / 100% 候选门已经解锁。
3. 系统状态为 NORMAL。
4. 当前仓位冻结收益率仍不低于该级 30% / 50% / 100% 门槛，且扣除预计退出成本、资金费和滑点后的当前整仓净浮盈仍大于零；曾经达到里程碑但回调后已低于该级门槛不合格。
5. 使用已认证抵押口径计算的 `L_effective < L_min`。
6. 当前价格通过已冻结的布林偏差回调过滤。
7. 原仓保护已经上移并得到交易所确认，且保护数量足额覆盖真实仓位。
8. 趋势结构仍有效，并出现该级允许的延续事实。
9. 上一次订单、成交、费用和保护已经完成对账。
10. 最新 `Current Portfolio MTM Equity` 已按同一时间边界完成对账并包含未实现盈亏；Vault 纳入符合冻结资本政策，且没有在途、重复或未知资金。
11. 当前保证金、清算缓冲和压力风险合格。
12. SYSTEM 提案的排名仍合格，或 MANUAL 提案的交易假设和有效期仍成立；两者的流动性、点差、滑点和资金成本都合格。
13. 没有未知订单、人工未授权修改或数据异常。
14. Reserved Heat 已成功占用。
15. 按新增订单最坏允许成交计算后，本经济仓位的 `Trade Worst-Case Loss_after` 不超过冻结档位最大允许亏损、基于最新 MTM 权益的动态档位上限和 Authorized Capacity 中的最严格边界。
16. 单标的、底层资产、风险簇、板块、场所和组合的 `Scope Worst-Case Loss_after(scope)` 分别不超过各自当前预算；不得把任一聚合 scope 的总损失与单笔档位上限直接比较。
17. 加仓后的 Stress Heat 单独通过冻结的压力门。
18. 加仓不会放宽已有保护价。
19. 每一份部分成交都能在交易所附件规定的窗口内获得足额保护。
20. 当前 `margin_mode`、保证金能力、`collateral_scope` 和 `collateral_pool_id` 与证书一致。
21. VenueAdapter 只把 Trading 签发的 Add-1、Add-2 或 Add-3 一次性意图交给对应 Freqtrade worker；worker 不得自主调整仓位。

任一条件失败，默认终止当前候选并记录原因：

- 尚未发送且事实明确时，释放对应 Reserved Heat。
- 已发送或撤销结果未知时，转为 Unknown Heat 并只查询原意图。
- 当前候选是终态，不保留隐形排队任务；只要 Add 单元尚未进入“消费中”、未过期且未撤销，后续新行情快照可以重新生成一个新候选。
- 执行意图已终态且真实成交量为零时，释放锁与 Reserved Heat，Add 单元恢复可用；后续只能由新决策快照重新候选，不能盲目重放旧订单。
- 任意大于零的真实成交都会消费一次 Add；加仓完成后必须确认最终真实仓位、保护覆盖和 `target_reached`。结果未知时停止后续加仓并降级。

### 11.3 30% / 50% / 100% 盈利候选解锁阶梯

业务默认在 Add-1 达到 30%、Add-2 达到 50%、Add-3 达到 100% 时解锁相应候选资格。达到阶梯不发送订单，也不占用或消费 Add；为消除杠杆与交易所 UI 的歧义，统一口径为：

> `仓位冻结收益率 = 扣除预计平仓成本后的整仓未实现净收益 ÷ 初仓成交后冻结的初始保证金参考额`

- 分母在整个仓位生命周期不因加仓、入金、转账或交易所 UI 变化。
- 价格使用执行附件冻结的 Mark / 可成交价口径；费用、滑点和资金费使用同一版本成本模型。
- 低风险只使用 30%，中风险使用 30% / 50%，高风险才使用全部 30% / 50% / 100%。
- 阶梯不携带新增数量或风险比例；候选通过后统一按第 11.4 节计算到当前冻结 `L_target` 的数量差。
- 调整盈利解锁值、价格口径或分母会产生新的参数版本；自动加仓正式启用前仍需逐级证明增量价值。

每一级还要冻结解锁确认必须持续多久、在哪个 K 线时点确认、保护至少上移到哪里、候选资格多久失效，以及扣除跳空和流动性折扣后实际 Protected Profit 是否足够。阶梯曾经解锁不覆盖第 11.2 节的当前门槛；回调后必须仍不低于对应 30% / 50% / 100% 门才可加仓。

### 11.4 有效杠杆与布林偏差回调过滤

目标杠杆数学只迁移为 Trading 中经过验证的纯计算思想：

`L_effective = abs(已对账净仓位名义价值) ÷ E_campaign`

- `E_campaign` 是该经济仓位按真实初仓、全部成交、费用、资金费、已实现和未实现盈亏重建的当前仓位权益，必须来自交易所事实和已认证的 `collateral_scope / collateral_pool_id`。isolated 使用该仓实际可归属保证金；cross 使用执行附件冻结的共享抵押归属与压力口径，不能用最近订单或随意分摊虚构权益。
- 禁止使用最近一笔订单价格模拟整仓成本，禁止把当前全仓假定为最近一单建立，也禁止使用旧脚本的 `可用保证金 × 95% × 杠杆` 代替风险定仓。
- `Q_target = E_campaign × L_target ÷ P_executable` 生成本次目标净数量，`ΔQ_to_target = max(0, Q_target - abs(Q_current))` 生成沿原方向的默认 Add 数量。每一级都使用当前事实重新计算到同一个冻结 `L_target`，不乘“初仓的 N%”、不乘“冻结收益的 N%”，也不读取每级数量 / 风险比例。
- `ΔQ_approved` 只能取 `ΔQ_to_target` 与冻结最大亏损、最新动态组合预算、Authorized Capacity、`FundingEnvelope_0` 的已结算可用部分、Reserved / Open / Unknown Heat、保证金、清算缓冲、流动性、最小 / 最大订单量和场所规则所允许数量中的最小安全值。任何约束只可缩量或归零，不能把订单放大到目标杠杆之上。
- `ΔQ_approved` 小于最小可执行数量时不发送订单，也不消费 Add。只要实际成交量大于零，本次 Add 即被消费，即使因风险缩量、订单部分成交或价格变化未达到 `L_target`，也不能使用同一 Add 补足差额。
- `L_target / L_min / L_max` 按方向、风险档位、VenueAdapter、margin mode 和抵押池分别冻结；`L_effective` 或分母未知时禁止加仓。
- `L_effective < L_min` 只表示杠杆门允许继续评估 Add，不会单独产生加仓；`L_min <= L_effective <= L_max` 不因杠杆带产生仓位动作。

每个候选、订单回执和最终成交记录都必须展示：`L_effective_before`、`L_target`、`Q_target`、`ΔQ_to_target`、`ΔQ_approved`、缩量原因、预计 / 实际成交后杠杆与 `target_reached`。`target_reached = true` 只表示最终对账杠杆进入执行附件冻结的目标容差，不代表风险门永久通过；`false` 必须同时给出零成交、风险缩量、场所限制、部分成交或价格漂移等明确原因。

“回调”固定解释为 DynamicPositionSizing 所启发的布林偏差过滤，而不是基于历史峰值的回撤规则：

`bollinger_deviation_pct = (current_price - bollinger_reference_price) ÷ bollinger_reference_price × 100%`

只有偏差位于冻结的 `[callback_lower_pct, callback_upper_pct]` 内，回调过滤才通过。参数版本必须包含价格源、K 线周期、是否仅使用已完成 K 线、窗口、标准差倍数、`upper / middle / lower` 参考线、偏差上下界、陈旧门和异常数据处理。系统不要求价格先创造高点再回落，也不会因离开该区间自动撤 Add 或减仓；未通过时只是当前候选失败，后续新快照仍可重新候选。

### 11.5 第一次、第二次和第三次加仓是三个独立事件

- 每个 Add 单元最多被一次大于零的真实成交消费；同一候选事件不能重复发送。
- 第一次加仓完成并对账前，不能生成第二次加仓。
- 第二次完成并对账前不能生成第三次；第二和第三次分别重新通过当前盈利、`L_effective < L_min`、回调、风险和全部共同硬门。
- 同一级 Add 尚未成交时，终态零成交会释放锁，未来新快照允许重新形成候选；一旦任意数量真实成交，该单元永久消费，不得因未达到 `L_target`、后续减仓或再次满足门槛而重复消费。
- 已经失败或过期的旧机会不能无限排队等待更好价格。

### 11.6 加仓是账户增险，但不等于授权扩权

加仓会增加真实名义仓位和账户执行风险，因此每次都必须使用包含未实现盈亏的最新 `Current Portfolio MTM Equity` 重算预算并申请 Reserved Heat。它不需要逐次人工审核，只因为风险上限已经在原始授权包中批准，而不是因为加仓“不算风险”。若最新动态预算比冻结授权更小，只能缩量或跳过；若更大，也不能自动扩大冻结授权。

Protected Profit 只有在有效保护订单已经确认，并扣除压力成本后，才可以解释原仓为什么仍有合同内风险空间。

它只能：

- 用于同一仓位、同一交易所和同一原授权包。
- 通过有效保护价体现在 Open Heat 的降低中。
- 作为同一仓位风险归因与加仓资格的辅助指标。

它不能：

- 在已经降低 Open Heat 后再次独立增加加仓额度。
- 进入风险权益，扩大其他仓位的新增风险。
- 支持跨所补单。
- 支持临时提高档位或次数。
- 被当成“不会亏”的本金。

### 11.7 各档位的认证顺序

- 先验证不加仓基线。
- 再验证低风险一次加仓。
- 再验证中风险第一次和第二次加仓。
- 再独立验证高风险第三次加仓；高风险 10x 的每一级自动加仓必须拥有独立样本、压力与实盘认证。
- 10x 开仓保留人工审核。

### 11.8 `L_effective > L_max` 的动态去杠杆

当已认证口径计算出 `L_effective > L_max` 时，Trading 独立生成 `reduce-only` 动态去杠杆目标，把仓位降向 `L_target` 或 Risk Engine 给出的更小安全目标：

- 该动作不要求仓位盈利，不要求通过 30% / 50% / 100% 阶梯或布林回调。
- 它不消费、返还或重新武装 Add-1 / Add-2 / Add-3，也不因自身发生而自动撤销尚未消费 Add；剩余 Add 是否继续有效仍由授权期限、风险状态和实时硬门决定。
- 目标是净仓位安全收缩，不按任何加仓层先后顺序解释，也不允许减到反向开仓。
- 数量使用当前真实净仓位、已认证 `E_campaign`、目标杠杆、最坏可成交价格、最小订单量、保护和清算缓冲共同计算；结果未知时只查询原意图。
- 动态去杠杆与硬止损、趋势退出或系统减仓同时发生时，交给唯一目标仓位仲裁器，选择更小仓位和更高安全紧迫度。

### 11.9 对 DynamicPositionSizing 的迁移边界

只迁移并重新实现两类思想：目标杠杆区间的数学关系，以及布林参考线偏差区间作为继续加仓过滤器。不得直接复制旧 Binance / Hyperliquid 脚本、配置加载、最近成交订单锚点、合成权益、95% 保证金放大、无状态轮询、直接 CCXT 下单、拆单和异常吞掉逻辑。所有计算进入 Trading 的版本化决策层，所有订单必须经过 Risk Engine、OMS、VenueAdapter 和对应 Freqtrade worker。

---

## 十二、退出合同

人工决定是否建立风险，但退出权限从第一笔真实成交持续到完全清仓，不依赖人工、Telegram、Web、Perptape 或辅助服务。

### 12.1 四类退出

1. **硬保护止损**：以限制单笔计划损失为目标；跳空、止损限价未成交或交易所故障仍可能穿透。
2. **趋势退出**：趋势结构失效或出现预登记的反向条件。
3. **动态去杠杆与安全减仓**：`L_effective > L_max` 或清算 / 压力门要求收缩仓位时，以 `reduce-only` 降向唯一安全目标；布林偏差“回调”不是退出条件。
4. **系统风险退出**：组合状态要求减仓或受控退出。

`low_vol_breakout_bn` 只作为退出行为参考：它当前的确定优先级是固定美元止损、可选固定美元止盈、已完成 K 线趋势通道退出，平仓使用 `reduce-only`。其中趋势通道语义是多头实时价跌破最近 N 根已完成 K 线最低价、空头实时价突破最近 N 根最高价。

新系统继承“已完成 K 线、确定性退出仲裁、已有仓位持续管理、reduce-only”四项理念，但不直接继承其固定美元定仓和轮询式裸保护。当前参考实现没有本文定义的候选解锁、目标杠杆带、布林回调过滤、交易所原生保护、幂等订单和完整部分成交状态机；这些都必须在 Trading 中重新实现并独立验收。

生产初仓优先按初始失效价和冻结 1R 建立交易所原生保护。固定美元止盈可以作为可选策略参数，趋势通道退出可以作为已完成 K 线的退出参数，但二者都进入 Trading 的唯一退出仲裁器；Freqtrade 自身 ROI、跟踪止损或策略回调不得成为并行的第二套退出规则。

### 12.2 多个退出同时发生

所有退出条件必须由唯一退出仲裁逻辑合并为一个目标仓位：

- 分别仲裁“目标剩余仓位”和“执行紧迫度”；更小的目标仓位与更高的安全紧迫度优先。
- 所有退出订单只允许减仓，不能意外反向开仓。
- 同一目标不能产生重复退出意图。
- 新退出目标替换旧保护或退出订单时，不能产生无保护窗口或超量减仓。
- 未完成订单的撤销、替换和剩余保护数量必须使用交易所事实确认。
- 已有退出订单未完成时，不能提前释放风险。
- 人工直接在交易所修改仓位后，系统先冻结新增风险并对账，再继续保护。
- 动态去杠杆触发时，按当前净仓位与认证杠杆公式计算唯一目标，不按加仓层顺序处理，也不自动撤销剩余 Add；它与硬止损、固定止盈或趋势退出同时发生时，仍按更小目标仓位与更高紧迫度合并。

### 12.3 只减仓故障路径

- 已经放在交易所的原生保护在 Web、Telegram、审批或 Trading 故障时继续有效。
- 审批或 Risk Engine 不可用时，预认证的确定性故障路径只允许撤销增险、维护保护、reduce-only 减仓和清仓，绝不允许开仓或加仓。
- 主 Freqtrade worker 不可用时，只能由经过 fencing 的备用 worker 接管，并继续保证单一活动发送者。
- 备用 worker 也不可用时，进入预设的人工交易所接管手册；人工只能保护、减仓或退出。
- Telegram 紧急减仓正常情况下仍只走 Trading；Trading 整体不可用时不能改成直连 Freqtrade，而应明确提示人工交易所接管。
- 故障恢复后先冻结并对账，旧加仓和退出意图都不能盲目重放。

### 12.4 完全退出的定义

退出分成两个终态：

1. **风险暴露已关闭**：真实仓位为零，所有增险、保护和退出订单已经处理，可以按真实事实释放计划 Heat。
2. **财务归因已结算**：费用、资金费、滑点和实际 R 已完成归因。

只有以下事实全部成立，完整仓位生命周期才结束：

- 交易所真实仓位为零。
- 相关增险、保护和退出订单已处理。
- 残余仓位与重复订单为零。
- 手续费、滑点、资金费和实际 R 已结算。
- 初仓与加仓授权全部关闭。
- Open、Reserved 和 Unknown Heat 已按真实事实释放。

“平仓请求已发送”不等于已经退出。

---

## 十三、Binance 与 Hyperliquid 的独立执行合同

### 13.1 可以统一的内容

- 策略逻辑和候选排名。
- 交易 R、风险权益和 Heat 语言。
- 人工提案和有限授权语义。
- 单标的、标准化底层资产、风险簇、板块和组合暴露。
- 收益、成本和审核归因标准。

### 13.2 必须独立的内容

| 项目 | Binance 附件 | Hyperliquid Core / HIP-3 附件 |
| --- | --- | --- |
| VenueAdapter 与 worker | `BinanceVenueAdapter` + 独立 Freqtrade worker / config | `HyperliquidVenueAdapter` + 独立 Freqtrade worker / config |
| 账户与权限 | 独立认证 | 独立认证 |
| 标的与合约规格 | 独立读取与冻结 | 独立读取与冻结 |
| 保证金模式与能力 | cross / isolated 分账户与 worker，独立验证 | removable isolated / strict isolated，逐域独立验证 |
| 抵押身份 | 独立 `collateral_scope / collateral_pool_id` | 按账户抽象、DEX 与抵押资产独立 `collateral_scope / collateral_pool_id` |
| 杠杆与名义档位 | 独立验证 | 独立验证 |
| 保护订单和触发价 | 独立验证 | 独立验证 |
| 部分成交与撤单 | 独立重放 | 独立重放 |
| 费用、资金费和容量 | 独立建模 | 独立建模 |
| 限速、结果未知和恢复 | 独立演练 | 独立演练 |
| 实盘扩容 | 从本所小额开始 | 从本所小额开始 |

两所虽然都以 Freqtrade worker 为订单后端，但一个 VenueAdapter、worker、账户或 margin mode 通过不证明另一个执行单元通过。

Hyperliquid Core 与每个启用的 HIP-3 DEX 也分别认证。每个域独立保存 DEX 身份、抵押资产、预言机、交易时段、市场运营者、费用、流动性、持仓上限、保证金、限速和故障结论；共享钱包或抵押品不能让这些证据互相继承。

### 13.3 VenueAdapter 与 Freqtrade 的共同执行边界

- Trading 保留策略、风险、授权、OMS、组合账本和恢复主权。
- 所有开仓、加仓或其他增险意图都必须经过正常 Risk Engine；保护、撤销增险、reduce-only 减仓和清仓在正常状态下也经过同一路径。
- Risk Engine 不可用时，只有第 12.3 节预认证的确定性只减仓故障门，能够签发唯一、限时、限账户、限仓位且不可扩大风险的 Trading 退出意图。
- VenueAdapter 只接收上述两类由 Trading 签发的意图：正常 Risk Engine 已批准意图，或只减仓故障门意图；它完成场所规则核验和语义映射后，才交给绑定的 Freqtrade worker。任何界面、用户、策略回调或第三方都不能成为第三种来源。
- Freqtrade REST、WebSocket、FreqUI 和内置 Telegram 不直接对用户开放交易权限。
- 用户面对的是自有 Web / PWA 与 Trading Telegram Bot。
- Freqtrade 的本地交易记录用于协调，不覆盖交易所私有事实。
- Freqtrade 自带的强制入场、启动、解锁、市场方向或配置变更能力不是本系统的授权通道。
- 自动仓位调整、ROI、跟踪止损和策略回调不能形成第二套加仓或退出规则。
- 暂停、紧急减仓等动作也先经过 Trading，防止重复退出、反向开仓和跨策略冲突。
- 每个执行单元的 VenueAdapter、Freqtrade 版本、CCXT 版本、bot config、受管账户、`margin_mode`、账户抽象、`collateral_scope / collateral_pool_id` 和执行附件都需要冻结与审计。
- Hyperliquid worker 只有显式配置并认证对应 `hip3_dexes` 后才能识别该 DEX 标的；新增 DEX 会增加数据调用与限速压力，不能因技术上可枚举就自动上线全部市场。
- worker 可以因更严格的本地限制拒绝意图；若实际数量、价格或保护超出提案容差，必须失败并生成新提案，不能静默改写。

### 13.4 保证金能力分类与安全动作顺序

完整目标包含 Binance cross、Binance isolated 与 Hyperliquid isolated。三种运行拓扑分别使用独立账户或子账户、Freqtrade worker / config、凭据、数据库、抵押池身份与执行证书；Binance cross 对应 `CROSS_SHARED`，Binance isolated 与 Hyperliquid isolated 按真实能力分别认证为 `ISOLATED_REMOVABLE` 或 `STRICT_ISOLATED`。工程交付同时覆盖三种目标拓扑，但每种拓扑只有在独立研究、重放、影子、模拟和小额实盘证据通过后才能激活。

执行附件必须把每个标的、账户和 `collateral_pool_id` 归入且只归入一种已认证能力：

| 能力 | 含义 | 允许的保证金动作 |
| --- | --- | --- |
| `CROSS_SHARED` | 多个仓位共享同一抵押池和清算风险 | 不做仓位级保证金移出；新增风险只使用共享池真实可用余额与压力缓冲 |
| `ISOLATED_REMOVABLE` | 仓位抵押隔离，且交易所明确允许在安全约束内增加或移出 isolated margin | 成交对账后可以执行独立认证的 excess-margin normalization；减仓后可按预授权目标回加本次释放保证金 |
| `STRICT_ISOLATED` | 仓位隔离且保证金不能主动移出，通常随减仓按交易所规则同比释放 | 禁止主动移出；真实可用保证金或清算缓冲不足时不加仓；减仓后仅在已认证 top-up 能力下回加本次释放保证金 |

净增仓或净减仓先按 Risk Engine 计算的安全目标以单腿订单执行。默认不要求先提取浮盈，也不通过“先减仓、再重新开仓”改变保证金或成本口径：

- 增仓先完成 Add 意图、成交和保护对账；减仓先完成 `reduce-only` 目标、成交和剩余保护对账。
- 只有 `ISOLATED_REMOVABLE` 在净增仓完全对账后，才可以评估经独立认证的可选 excess-margin normalization；适配器必须依据原生事实计算、试算或请求后确认 `max_removable_margin`，不能假定两所直接返回统一字段。该动作不改变净仓位，也不得使清算缓冲低于冻结门。
- `STRICT_ISOLATED` 不存在主动移出保证金的 normalization。释放多少抵押完全以交易所减仓后的确认事实为准；若该标的的原生 top-up 能力尚未认证，则不执行回加并保持更小仓位或继续退出。
- `CROSS_SHARED` 的盈利、亏损和可用抵押属于共享池事实，不能虚构某个仓位的可提保证金；场所级资金迁移只走第 9.2 节 Capital Transfer Orchestrator。
- 用户要求的浮亏减仓路径登记为 `POST_REDUCTION_MARGIN_NORMALIZATION`：启用逐仓 DynamicPositionSizing 模板时，先 `reduce-only` 到目标数量并完成成交 / 保护对账，再把“本次减仓实际释放额”中合同冻结的部分回加到剩余仓位。它只能使用同一场所、同一账户已预留的本次释放额，不得调用 Vault、增加净仓位、放宽止损或恢复已损失的 1R。
- 同一账户的订单、释放额预留和 Margin Controller 动作必须用 fencing 串行化；保证金腿发送前重新核验费用、其他仓位、挂单和可用抵押。回加失败时保持更小仓位或继续减仓 / 退出，不能反向开仓追赶目标。
- 任一保证金动作失败、结果未知或使 `collateral_scope / collateral_pool_id` 无法确认时，立即禁止新增风险并先对账。

保证金能力是执行证书，不是根据接口名称推测的功能。交易所技术上存在某个接口，也不能在未完成权限、账户模式、最小值、费用、并发、结果未知和恢复重放前开放生产调用。

### 13.5 官方约束对合同的影响

截至 2026-07-14 的官方资料显示：

- Binance 的初始杠杆按具体标的设置，并与可用最大名义价值档位关联；保证金模式也是标的级运行事实。
- Hyperliquid 的最大杠杆取决于资产及名义档位，同时支持 cross 与 isolated；部分资产为 strict isolated，保证金不能主动移出，只会随减仓按交易所规则释放。
- Hyperliquid 普通 isolated 提供增加和移出保证金动作，但仍受初始保证金、总名义价值和转出后最低保证金等约束；接口存在不等于 normalization 已认证。
- Hyperliquid 的 Standard、Unified 与 Portfolio Margin 等账户抽象会改变 spot、perp、DEX 和抵押资产之间的共享边界；切换后必须生成新抵押身份并使原证书失效。
- 当前 Freqtrade 官方资料将 Binance 与 Hyperliquid 永续列为支持范围，但交易所能力并不相同。
- Freqtrade 当前一个 bot / config 只支持一种 margin mode；杠杆交易假设 bot 独占账户，不能让两个 bot 或日常人工交易共享同一杠杆账户。
- 当前 Freqtrade 文档支持配置 Hyperliquid HIP-3 DEX，包括 `xyz` 等域；每个额外 DEX 会增加 API 调用和限速压力，而且不同 DEX 的报价 / 抵押资产可能不同。
- 因此 cross 与 isolated 必须使用独立 bot / config 和独占账户分别认证，不能靠运行时字段在同一 worker 中切换。
- Hyperliquid 的“市价”在该执行栈中可能由带较宽最大滑点的限价方式模拟；其默认边界不能自动成为本策略允许滑点。
- Binance Futures 可使用不同类型的交易所止损，而 Freqtrade 对 Hyperliquid 的交易所止损是限价语义，需要单独验证未成交风险。
- HIP-3 市场由各 deployer 负责市场定义、预言机和运行，并具有各自持仓上限和保证金事实；传统资产参考永续还需验证休市、跳空、公司行动、指数 / 商品基准和异常结算。

因此，系统内部的 3x / 5x / 10x 永远只是更严格的用户政策上限。每次执行还必须取交易所当时实际允许值、名义档位和风险容量中的更小结果，并且结果仍须处于冻结提案允许的数量和价格容差内。

如果 VenueAdapter、Freqtrade 或 CCXT 的默认订单行为超过冻结提案的价格、滑点、时效或保护边界，Trading 必须阻断该动作。Hyperliquid Core 和每个 HIP-3 DEX 只有在实际执行语义、预言机、交易时段、限价未成交、止损限价穿透、保证金能力和恢复路径通过独立重放后，才能分别进入小额实盘。

### 13.6 单所或单实例故障

- 某所公开行情未知：冻结该所新候选和已有仓位自动加仓。
- 某个 VenueAdapter 或 Freqtrade worker 不可用：冻结该执行单元的新仓与加仓，确认交易所原生保护并对账。
- VenueAdapter、Freqtrade 与交易所状态不一致：交易所事实优先，执行单元进入冻结或只减仓。
- 某所私有仓位、挂单、成交或余额未知：默认关闭全局新增风险。
- 两所私有事实同时未知：全局禁止增险并进入最高等级的对账与保护状态。
- 只有两所使用专用账户、不可借用额度、独立 worker，并能计算最大未知暴露和组合上界时，才可认证“健康交易所继续”的例外。
- Unknown Heat 只根据真实仓位、已发送和可能成交订单的最坏边界计算，不把尚未触发的 Authorized Capacity 当成未知仓位。
- 在上述隔离认证完成前，某所私有事实未知的唯一默认行为是全局关闭新增风险。
- 健康交易所不能接管、补做或对冲故障交易所的未知订单。
- 一所恢复不会自动恢复旧提案和旧加仓授权。

---

## 十四、正期望研究设计

### 14.1 证据链

**冻结合同 → 数据验收 → 含完整成本的时间外验证 → 历史事件重放 → 实时影子盘 → 模拟 OMS → 小额实盘 → 分级扩容 → 持续监测**

任何证据门失败都保持或撤回对应能力认证。不能在看到最终结果后临时修改门槛。

### 14.2 独立样本的正确口径

统计样本不能简单按订单数量计算。

同方向、同相关风险簇、同一突破或宏观波次产生的多个仓位，应合并为一个 **独立趋势战役**。趋势战役是处理相关性的统计分组，不是新的风险分母。

研究前必须冻结战役的：

- 标准化底层资产、板块与风险簇版本。
- 提案来源；SYSTEM 与 MANUAL 的择时样本不能混成同一效果样本。
- 方向。
- 开始与结束规则。
- 两笔仓位可归入同一战役的最大时间间隔。
- Binance、Hyperliquid Core 与 HIP-3 DEX 同一经济机会的跨域合并规则。

主要期望指标使用 **净风险单位期望**：全部成本后净 PnL 相对于实际消耗的冻结交易风险总额。置信区间仍以趋势战役为区块评估，避免同一宏观波次中几十个相关标的被误算成几十次独立成功。

Profit Factor 按完整成本后的战役净 PnL 计算；订单级和单仓级结果只作诊断。没有亏损战役时不能把 Profit Factor 视为无穷大并直接通过，必须继续积累样本。

同时保留：

- 完整仓位数量。
- 独立趋势战役数量。
- 多头与空头数量。
- SYSTEM / MANUAL、标准化底层资产、板块、风险簇和市场状态分布。
- 初仓、第一次、第二次和第三次加仓贡献。

策略统计证据必须说明适用于哪个完整认证单位。SYSTEM 的信号方向证据可以在满足同一数据与合约映射时有限共享，但 MANUAL 的择时证据必须单独保留；交易所 / DEX 成本、板块、风险档位和加仓层级仍需把自身证据叠加后才能形成完整认证。

### 14.3 时间顺序与冻结测试集

建议预登记模板：

- 最早约 60% 数据用于研究与参数选择。
- 中间约 20% 用于不少于 5 个 walk-forward 时间外窗口。
- 最新约 20% 作为最终冻结测试集，且至少覆盖 12 个月。
- 单个 walk-forward 可从 18–24 个月训练与 3–6 个月验证起步。
- 每个 walk-forward 窗口至少包含 20 个独立战役，5 个窗口合计至少 100 个。
- 至少 5 个窗口中 4 个净风险单位期望为正。
- 主要推断门使用全部 walk-forward 时间外数据的战役级置信下界大于 0。
- 最终冻结测试集用于确认方向为正，并落在预登记的预测区间内，不能只凭一个点估计宣称独立统计通过。
- 最终冻结测试集只能查看一次。
- 一旦根据最终测试集调参，它立即转为研究集，必须等待新的未来数据形成新测试集。

这些比例是研究起点。若因标的历史较短需要调整，必须在查看结果前记录理由和替代门槛。

### 14.4 最低样本建议

| 范围 | 建议门 |
| --- | ---: |
| 全部时间外证据 | 至少 200 个完整仓位、150 个独立战役 |
| 5 个 walk-forward 窗口 | 合计至少 100 个独立战役，且每窗至少 20 个 |
| 最终冻结测试集 | 至少 50 个独立战役 |
| 同时宣称支持多空 | 多头、空头各至少 30 个独立战役 |
| 常见市场状态 | 每类至少 20 个独立战役，否则该状态未认证 |

固定样本只是最低门。还需要依据战役波动估计达到单侧 95% 置信要求所需的样本量，取两者中更严格的值。样本不足可以继续影子观察或限制性试点，但不能解释为正式通过或进入最高规模。

---

## 十五、数值化验收门

本章的数字是建议的预登记默认值，不是已经证明有效的生产参数。正式研究开始前，需要由策略负责人和风险负责人共同冻结。

### 15.1 零容忍硬门

以下项目的通过门永远为零：

- 未来数据泄漏。
- 使用当前标的清单替代历史时点标的池。
- 未授权增险。
- Risk Engine 被人工或 OMS 绕过。
- 同一触发产生重复增险意图。
- 过期、撤销或已消费授权再次执行。
- 仅因 30% / 50% / 100% 曾经达标，但执行时当前收益已低于对应门，或未通过 `L_effective < L_min`、布林回调和实时风险门便加仓。
- 使用每级固定数量、初仓百分比、冻结收益百分比或风险份额替代到冻结 `L_target` 的差额计算，或把 Add 放大到目标杠杆以上。
- 任意实际成交后未消费对应 Add，或终态确认零成交却错误消耗 Add。
- Add 前未使用包含未实现盈亏的最新 `Current Portfolio MTM Equity`，本仓 Trade Worst-Case Loss 未取冻结 / 动态单笔上限中的更严格者，或任一 Scope Worst-Case Loss 突破对应底层资产、板块、风险簇、场所或组合预算。
- 状态未知时扩大风险。
- 结果未知时盲目重试。
- 自动提高风险档位。
- 自动换所、跨所补单或接管未知仓位。
- 真实仓位超过交易所附件规定的保护窗口仍没有足额保护。
- Web、PWA、Telegram、FreqUI、Freqtrade API 或内置策略绕过 Trading 直接增险。
- 两个 Freqtrade bot 共用杠杆账户、同一 bot 混用 margin mode，或账户 / `collateral_scope / collateral_pool_id` 与证书不一致仍增险。
- 真实订单无法关联完整审计链。
- 未完成对账便恢复新增风险。
- 平仓未确认便释放风险。
- 未分类标的、未认证 HIP-3 DEX 或失效预言机仍然增险。
- 人工请求数量超过风险核准数量却被静默缩量后直接执行。
- 把 `FundingEnvelope_0` / Trade Funding Envelope、保证金占用或名义仓位当成 1R / 最大允许亏损，或初仓所需 Vault 资金尚未结算便发送订单。
- Vault 资金在链上、转账在途或交易所之间重复计入，或被当作交易所可用保证金。
- 活动仓位接受 Vault 运营补充或 `MANUAL_CAPITAL_INJECTION`，或任何双向划转绕过 Capital Transfer Orchestrator。
- `STRICT_ISOLATED` 主动移出保证金，或净仓位成交尚未完成对账便执行 excess-margin normalization。
- 无对应标签 / 作用域仍执行动作、显式拒绝被标签并集覆盖、违规自审、缺少规定复核 / MFA，或身份与授权决定没有完整审计记录。

任一发生，不能用总体收益良好来抵消。

### 15.2 数据质量门

| 项目 | 建议门 |
| --- | ---: |
| 全部预定决策时点与历史标的池的数据完整率 | 不低于 99.95% |
| 每月和每个常见市场状态的完整率 | 不低于 99.5% |
| 实际交易、保护与退出依赖窗口 | 100% 完整 |
| 交易窗口未解决的重复、乱序和缺口 | 0 |
| 持仓期资金费与合约规则覆盖 | 100% |
| 历史时点标的池可重建率 | 100%；否则预先标记系统不可交易 |
| `venue / dex / instrument / underlying / sector / collateral` 历史映射 | 100%；未分类时风险为零 |
| 非加密标的交易日历、预言机、公司行动、指数 / 商品基准和换月事件 | 所有候选、持仓与退出窗口 100% 覆盖 |
| HIP-3 DEX 身份、抵押品、持仓上限和市场状态 | 所有决策时点 100% 可重建 |
| 同一冻结输入重复运行 | 确定性结果完全一致 |

完整率不能只按“已经生成的交易”计算，因为数据缺失可能让本应发生的亏损信号消失。所有排除期间必须在查看 PnL 前冻结，并作为系统不可交易时间保留在收益时间轴中，不能事后删除。报告还必须列出最长连续缺口，以及它是否覆盖候选、保护或退出窗口。

如果成本或成交模拟包含随机性，冻结随机种子时逐笔结果必须一致，并另外验证多随机种子的结果分布稳定。

缺少历史盘口时不能伪造精确成交结果。可以使用保守成本包做方向研究，但实际容量必须等待实时盘口影子数据验证。

### 15.3 防过拟合门

建议要求：

- 全部研究试验都登记，包括失败试验。
- 每类关键参数在研究前登记合法邻域：连续参数可从上下约 20% 起步，离散窗口、布尔开关和接近零的阈值使用各自的相邻合法值。
- 相邻组合至少 70% 保持净正期望，且邻域结果的下四分位仍为正。
- 选定参数不应形成孤立尖峰；邻域中位数接近零时，不使用百分比差异掩盖脆弱性。
- 新增过滤器、评分项或复杂度，至少在 4 / 5 个时间外窗口改善。
- 新复杂度应在研究前选择一个主目标：平均净期望增加至少 0.03R，或风险调整收益改善至少 10%。
- 新复杂度不能让最大回撤和尾部损失恶化超过 10%。
- 多重试验校正后，对正优势的单侧置信度建议不低于 95%。

第一次查看时间外结果前，必须冻结多重试验方法。默认使用以趋势战役为区块的“最大统计量”重采样，将本轮全部信号、参数、过滤器、方向和加仓变体计入同一试验族。试验总数、区块规则、单侧置信方向和失败后重新研究规则都要登记；若改用 Deflated Sharpe 或其他等价方法，也必须在看结果前批准。方法或试验族未冻结时，本项不能通过。

市场状态先用于归因，不能在看完亏损后随意增加一个过滤器。过滤器只有在独立时间窗口重复有效后，才可进入生产合同。

### 15.4 主策略正期望门

| 环境 | 净风险单位期望 | 战役级 Profit Factor | 其他要求 |
| --- | ---: | ---: | --- |
| 基准完整成本 | 每消耗 1 个冻结风险单位，净收益不低于 0.10R | 不低于 1.20 | 战役级评估中，正期望置信度建议不低于 95% |
| 1.5 倍不利成本 | 大于 0 | 不低于 1.10 | 至少 4 / 5 个时间外窗口为正 |
| 2 倍不利成本 | 不低于 0 | 不低于 1.00 | 回撤不得突破冻结 S-DD 上限 |
| 最终冻结测试集 | 大于 0 | 不低于 1.15 | 不得重新选参数后才通过 |

SYSTEM 与 MANUAL 分别证明择时结果；四个资产板块分别报告净期望、回撤、尾部和成本。样本不足的来源或板块只能保持影子、人工受限试点或关闭，不能借用其他来源 / 板块的汇总结果通过。

完整成本至少包含：

- Maker / Taker 手续费。
- 点差和订单规模相关滑点。
- 部分成交、撤单与重报成本。
- 持仓期间全部资金费。
- 极端行情的止损穿透和流动性下降。

成本压力只扩大不利部分：

- 有利 Maker 返佣和有利资金费先归零，不能通过乘数让压力场景变得更好。
- 不利手续费、资金费、点差和撤单成本按预登记情景放大。
- 盘口冲击按目标订单规模重新估算，不能用简单线性倍数替代非线性容量。
- 每次执行都保存当时预测成本和实际成本，后续使用配对误差校准。

胜率只做描述，不作为趋势策略的主要认证门。重点观察净 R、Profit Factor、最大回撤、恢复时间和右尾收益。

第 2.4 节六条假设必须分别留下可证伪证据：趋势惯性与突破后延续使用时间外条件分布检验；右偏收益报告收益偏度、亏损频率、右尾贡献与去除最大赢家后的结果；成交量 / OI 使用消融实验验证增量信息；组合风险控制以第 15.1 节零容忍和压力重放验证；浮盈加仓以第 15.8 节配对增量门验证。任一假设失败，只关闭依赖它的策略组件，不能事后改写假设解释亏损。

### 15.5 回撤与尾部门

必须分开四种回撤，不能用同一个 KILL_SWITCH 数字同时证明策略、交易账户和扩展资本安全：

| 回撤 | 证明什么 | 判定方式 |
| --- | --- | --- |
| 策略证据回撤 S-DD | 该认证单位本身的收益与尾部特征 | 按冻结交易 R 计算，不使用全局 KILL_SWITCH 截断美化结果 |
| 组合政策回放 P-DD | Heat 和风险状态是否按规则收紧 | 模拟 NO_PYRAMID、NO_NEW_POSITION、REDUCE_ONLY、KILL_SWITCH；只证明风控动作正确 |
| 实盘账户回撤 A-DD | 全账户当前真实损失与生存风险 | 按外部资金流调整后的 Mark-to-Market 保证金权益高点计算 |
| 扩展资本回撤 C-DD | 启用 Vault 后交易所与纳入范围链上资金的总资本 MTM 回撤 | 按 Vault 纳入 / 移出、转账和外部资金流调整后的 Extended Capital MTM Equity 高点计算；不用 Extended Risk Equity 作为分母 |

S-DD 使用完整时间序列的 Mark-to-Market 策略影子净值，包含并发仓位、未实现盈亏、费用和资金费，并以研究前冻结的参考资本或参考风险单位归一化。它不能只把已经结束的交易顺序相加。

策略证据回撤的正式上限需要在研究前根据策略波动、风险档位和可接受恢复时间冻结。尾部建议仍包括：

- 每个经济仓位使用“完整成本后净 PnL ÷ 该仓冻结交易 R”计算标准化结果；最差 5% 经济仓位的平均损失原则上不高于 1.5R。
- 单个经济仓位损失超过该交易档位的 `Frozen Trade Loss Cap`（1R / 2R / 3R）时，必须逐笔解释为预登记的跳空、流动性、止损穿透或交易所 / 执行故障情景。
- 趋势战役只作为相关性区块；若另行报告战役尾部，必须预先冻结战役自己的归一化分母。
- 基准、1.5 倍和 2 倍不利成本分别报告最大 S-DD、恢复时间和最差战役。

组合政策回放需要报告风险状态是否按时触发，以及触发后的执行穿透或 overshoot；穿透不能被当作策略回测合格额度。

A-DD 与启用后的 C-DD 都能要求全局系统状态，系统采用两者中更严格的动作；C-DD 永远不能抵消或放宽 A-DD。总体方案中约 1.0% / 1.5% / 2.5% / 4.0% 仍只是研究起点，A-DD 与 C-DD 的正式值需分别在风险政策中冻结。

单个策略表现恶化时，优先暂停或降低该认证单位，不能仅凭策略回测回撤自动把无关策略和全部账户送入全局 KILL_SWITCH。

### 15.6 市场状态门

市场状态使用预登记的机械分类：

- 主状态轴一：上升、下降、震荡，三者互斥。
- 主状态轴二：低波动、正常波动、高波动，三者互斥。
- 正交事件标签：清算或闪崩、流动性骤降、极端资金费、交易所异常；这些可以与任一主状态同时出现。

分类阈值和版本必须在查看对应结果前冻结，不能事后给亏损阶段换标签。

策略不要求在所有状态都盈利，但必须在所有状态守住风险边界。

每个宣称适用的常见主状态至少需要 20 个独立战役；不足时标记为未认证，并限制该状态或最高扩容级。稀有灾难事件通过预登记历史重放，不强求 20 个真实事件。

若某个常见状态样本不少于 20 个独立战役，且净风险单位期望低于 -0.20R，应采取以下之一：

- 降低该状态下的风险。
- 预登记并独立验证过滤器。
- 把该状态明确列为不适用范围。

### 15.7 收益集中度门

建议报告：

- 前 5 个战役占正毛利润的比例。
- 单一底层资产和单一板块占正毛利润的比例。
- 单一预定义主状态或自然季度占正毛利润的比例。

预登记警戒线可设为：

- 前 5 个战役超过正毛利润 50%。
- 单一底层资产超过正毛利润 30%，或单一板块超过预登记集中度门。
- 单一预定义主状态或自然季度超过正毛利润 60%。

趋势策略天然可能由少数大行情贡献利润，超过警戒线不直接判死刑，但不得进入 100% 规模。需要延长样本、限制集中暴露，并验证缺失主要趋势时的回撤仍可接受。

### 15.8 自动加仓的配对增量门

自动加仓不能只看“加仓版本赚钱了”，必须对完全相同的基础开仓做配对比较：

- 不加仓。
- 一次低风险加仓。
- 两次加仓。
- 三次加仓。
- 各风险档位。

两条路径必须使用相同候选、相同组合 Heat 上限、相同批准容量和相同最大事前风险。主比较观察完整组合路径的净风险单位期望与回撤，另行报告每单位新增风险带来的边际净收益，以及加仓占用 Heat 后挤掉的其他机会。

增量差异按同一战役配对评估，不能把加仓与不加仓当成两组互不相关样本。

建议加仓覆盖层至少满足：

- 100% Add 决策可从冻结 `L_target`、当时 `E_campaign` 和可执行价格重算 `Q_target / ΔQ_to_target`；不得出现每级固定比例旁路。
- 100% Add 在发送前保存包含未实现盈亏的 Current Portfolio MTM Equity、冻结 / 动态更严单笔上限、Trade Worst-Case Loss 和各层 Scope Worst-Case Loss；预算突破事件为 0。
- `target_reached` 与最终交易所对账结果一致率 100%，风险缩量、部分成交和价格漂移原因完整率 100%。
- 大于零成交未消费 Add、终态零成交错误消费 Add、同一 Add 重复消费均为 0。
- 至少 4 / 5 个时间外窗口的增量净 R 为正。
- 研究前二选一：每消耗 1 个冻结风险单位的增量至少 +0.03R，或风险调整收益至少改善 10%。
- 1.5 倍成本下，加仓增量仍为正。
- 2 倍成本下，完整策略至少不亏。
- 最大回撤、最差 5% 尾部损失和恢复时间恶化不超过 10%。
- 一次加仓通过后才研究第二次，第二次通过后才研究第三次。
- 低风险通过后，中风险和高风险仍需独立认证。

若加仓只增加总 PnL，却让回撤、尾部、恢复时间或执行脆弱性显著恶化，则不能取得能力认证。

### 15.9 人工与权限审核价值门

必须永久保留：

- 被批准的合格提案。
- 被拒绝、放弃或过期的合格提案。
- 因风险容量不足而跳过的提案。
- 所有未执行的加仓候选。

这些提案使用相同冻结规则计算影子结果，才能回答：

- 人工审核是否真正改善了结果。
- 加仓被跳过是减少了损失，还是错失了有效趋势。

只观察已批准交易会产生选择偏差，不能证明审核有价值。

身份与权限策略还必须通过角色矩阵重放：全部允许动作和拒绝动作的判定可重现率 100%；无标签或越作用域成功、显式拒绝被覆盖、禁止场景自审、缺少强制双人复核 / MFA、标签撤销后旧会话继续扩权以及缺失审计字段的事件均为 0。一个用户的多标签组合必须逐组合测试，不能只分别测试单个标签。

---

## 十六、能力证据与容量激活

完整产品目标一次定义并一次工程交付；本章只决定某个已实现能力何时可以使用真实资金，不是功能分期。工程完成与能力激活分开判定。所有证据门都采用“最低时间和最低样本同时满足”，不能二选一。

### 16.1 基础策略与执行

| 证据门 | 建议最低观察 | 通过条件 |
| --- | --- | --- |
| 研究冻结 | 合同完整 | 所有关键 TBD 有明确关闭动作 |
| 数据验收 | 覆盖全部研究期 | 数据硬门全部通过 |
| 时间外验证 | 至少 5 个窗口和冻结测试集 | 正期望、成本、回撤、稳定性通过 |
| 历史重放 | 预登记极端与故障案例全部覆盖 | 状态、授权、风险账本和退出不冲突 |
| 实时影子 | 至少 30 日、100 份提案、50 个战役 | 信号一致率不低于 99.5%，零重复和过期误接收 |
| 模拟 Freqtrade / OMS | 至少 30 日、50 个完整生命周期、200 次订单状态迁移 | 重启、断线、部分成交、未知订单和止损拒绝演练通过 |
| 10% 小额实盘 | 至少 60 日、30 个独立战役 | 先验证人工初仓、保护、退出和真实成本；自动加仓关闭 |

### 16.2 自动加仓

自动加仓必须作为独立能力认证：

1. 影子计算加仓但不发送订单：至少 30 日且 50 个合格加仓事件。
2. 模拟一次低风险加仓：至少 30 个完整加仓生命周期，并覆盖部分成交、保护替换和结果未知。
3. 低风险、一次加仓、10% 批准容量小额实盘：至少 60 日且 30 个真实加仓事件。
4. 低风险一次加仓通过后，才允许研究中风险或第二次加仓。
5. 中风险第二次加仓通过后，才允许研究高风险第三次加仓。
6. 第二次、第三次、中风险和高风险分别从影子与模拟重新计时、计样本。
7. 高风险 10x 的 Add-1 / Add-2 / Add-3 从独立证据和更严格压力开始，不能只认证最终汇总结果。

历史时间外样本用于证明增量优势；小额实盘用于证明真实执行、成本和保护能力。不能用少量实盘事件重新宣称长期正期望已经被证明。

### 16.3 每个交易所、执行域与板块独立激活与扩容

| 容量门 | 本级新增最低观察 | 该交易所 / 执行域的通过条件 |
| --- | --- | --- |
| 10% 批准容量 | 至少 60 日且 30 个本级新战役 | 人工初仓、保护、退出、对账和真实成本通过 |
| 25% 批准容量 | 再运行至少 60 日且 30 个本级新战役 | 实盘净 R 为正、真实成本在容差内、无 P0 事件 |
| 50% 批准容量 | 再运行至少 90 日且 50 个本级新战役 | 覆盖至少 3 类主市场状态，回撤和尾部在合同内 |
| 可选 75% 过渡 | 非线性冲击显著时至少 60 日且 30 个本级新战役 | 容量压力和部分成交没有出现结构性恶化 |
| 允许进入 100% | 完成目标订单规模的盘口影子与容量压力验证 | 只获得进入权限，尚不能称为 100% 稳定认证 |
| 100% 稳定认证 | 至少 90 日且 50 个本级新战役，累计运行不少于 180 日 | 正期望证据持续成立，成本、故障与恢复长期稳定 |

每一级样本必须主要发生在该级容量，不能用低级成交成本证明高级容量。

这里的 100% 是“该交易所、该认证单位允许消耗的风险预算与 Heat 容量”，不是杠杆倍数、保证金比例或投入全部个人资产。

Binance、Hyperliquid Core 与每个 HIP-3 DEX 分别从自身 10% 小额实盘起步。一个执行域达到 100% 不能使另一个自动扩容；某个板块的样本也不能证明另一板块已通过。SYSTEM 与 MANUAL 在执行安全上可共享同一域的机械证据，但择时和审核效果分别积累。

每一级容量激活还必须同时满足：

- 本级累计净 R 不低于预登记的同长度时间外预测区间下界。
- 没有触发第十七章对应的 30 / 50 战役统计降级信号。
- 本级净风险单位期望、S-DD 和配对成本误差都处于预登记容差。
- 安全、保护、对账和审计零容忍门全部保持通过。

“实盘净 R 为正”只是必要条件，不是单独充分条件。

### 16.4 真实成本容差

每次执行都保存该单预测成本和实际成本，并按交易所、方向、订单规模和流动性档位比较配对误差。

- 20–49 个独立执行事件：只使用中位数和最差值。
- 50–99 个独立执行事件：最多使用 P90。
- 至少 100 个独立执行事件：才使用 P95。

每个流动性档位在上线前同时冻结相对误差门和绝对 bps 门，避免模型接近零时百分比失真，并统一三层动作：

1. 预警。
2. 禁止自动加仓并降低容量。
3. 暂停该交易所新开仓。

精确 bps 与相对阈值属于 Binance / Hyperliquid 执行附件；未冻结时不能通过本项。任何层级都不允许通过扩大止损风险来补偿高成本。

---

## 十七、自动降级、暂停与结构性失效

### 17.1 立即暂停新增风险

出现以下任一情况，立即停止开仓与加仓：

- 真实仓位超过规定窗口仍未获得足额保护。
- 重复增险订单。
- 订单、仓位、余额或保护无法对账。
- 风险越界。
- 关键数据失真。
- 未授权增险。
- 交易所规则变化导致合同不再成立。
- 标的板块无法确认、HIP-3 DEX / 预言机异常或非加密市场关键日历与公司行动未知。
- `EXCHANGE_PLUS_VAULT` 下 Vault 余额、价格、控制权或 RPC 变成未知；对应贡献先归零并重新计算容量。
- 审计链断裂。
- 任一界面、VenueAdapter、Freqtrade worker 或外部服务绕过 Trading 正常授权链直接增险。

根据严重度进入 NO_NEW_POSITION、REDUCE_ONLY 或 KILL_SWITCH。

Vault 贡献下降本身不触发任何活动仓位资金注入，也不要求无条件市价清仓；系统先按新的 Extended Risk Equity 关闭加仓和开仓，再由 C-DD、A-DD、Heat 与交易所健康度决定是否进入受控 `reduce-only` 减仓。空仓后的下一周期运营补充只能按第 9.2 节的独立开关和 CTO 状态机重新判断。

### 17.2 表现降级建议

| 触发 | 动作 |
| --- | --- |
| 30 战役累计净 R 低于冻结的同长度时间外分布 10% 分位 | 预警；统计确认后关闭该认证单位自动加仓 |
| 50 战役累计净 R 低于冻结的同长度时间外分布 5% 分位 | 该认证单位未来授权容量降一级 |
| 50 战役窗口净期望不高于 0 且战役级 Profit Factor 低于 1 | 该认证单位未来授权容量降一级并复核 |
| S-DD 达到该认证单位局部回撤预算 50% | 关闭该认证单位自动加仓 |
| S-DD 达到该认证单位局部回撤预算 75% | 暂停该认证单位新开仓 |
| S-DD 达到该认证单位局部回撤预算 100% | 暂停并撤销该认证单位的未来增险权限 |
| A-DD 达到账户风险政策阈值 | 按总体方案触发全局 NO_PYRAMID、NO_NEW_POSITION、REDUCE_ONLY 或 KILL_SWITCH |
| C-DD 达到扩展资本风险政策阈值 | 启用 Vault 时按总体方案触发全局状态；不能用于放宽 A-DD |
| 至少 80 个新战役后，净期望的 95% 置信上界仍低于 0 | 判定结构性失效，退回研究 |
| 实际成本越过执行附件的预警 / 禁止加仓 / 暂停门 | 按对应层级限制该交易所 |

统计窗口只能包含同一可比较的策略、提案来源、板块、方向、交易所 / 执行域、风险档位和加仓层级。30 与 50 战役的实时结果必须分别和相同长度的时间外预测分布比较。

统计触发使用预登记的不重叠窗口或顺序检验，不能每完成一笔就无限重复使用同一个显著性门。安全事件仍然立即触发；统计表现降级可以要求两个连续窗口确认，但实际账户回撤硬门不等待确认。

“容量降一级”只影响未来授权与新增风险，不默认强制平掉仍有足额保护的仓位。生产中不能为了避免降级而修改观察窗口。

### 17.3 认证失效范围

- 信号、止损、退出或仓位算法变化：整个策略主版本失效。
- 周期、权重、过滤、目标杠杆计算或 `target_reached` 容差变化：对应参数版本失效。
- 数据源、价格口径或成本模型关键变化：重新时间外验证与重放。
- `BinanceVenueAdapter` 或 Binance 规则变化：Binance 受影响 worker / config 的执行认证失效。
- `HyperliquidVenueAdapter`、Hyperliquid Core 或某个 HIP-3 DEX 的适配、预言机、抵押品或规则变化：只使受影响执行域认证失效，并重新评估共享抵押品风险。
- Freqtrade、CCXT、bot config、`margin_mode`、保证金能力或关键执行配置变化：受影响执行单元认证失效。
- 账户抽象、`collateral_scope` 或 `collateral_pool_id` 变化：相关执行证书和风险覆盖证书失效，必须冻结增险并重新对账、压力测试与认证。
- `risk_inclusion_mode`、`capital_transfer_mode`、目标 `n%`、`T_low / T_high` 或 CTO 权限政策变化：对应风险 / 资金政策证书失效；收紧可立即覆盖，放宽重新批准与重放。
- 风险政策收紧：立即覆盖。
- 风险政策放宽，包括启用 / 提高 Vault 贡献或板块上限：新批准和复验后生效。

### 17.4 恢复条件

恢复分三类：

1. **运营故障**：完成真实账户和风险账本对账、根因说明、修复重放，并至少 7 日且 20 个相关影子事件无复发后，由人工逐级恢复。
2. **成本或容量失效**：退回最近通过的容量，在该级重新积累所需真实执行样本后才能申请恢复。
3. **结构性正期望失效**：关闭旧认证，产生新策略或参数版本，从数据、walk-forward 和冻结测试集重新开始；不能依靠 7 日影子恢复。

恢复系统不等于复活旧提案、旧订单和旧加仓授权。

---

## 十八、辅助建议源边界

任何辅助建议源都不得持有密钥、签名权、下单权、资金权或风控覆盖权；当前产品不预建其身份、审批路线、指标或专用数据实体。

---

## 十九、完整审计与盈利归因

每笔交易必须形成不可断裂的证据链：

**SYSTEM 信号 / 候选或 MANUAL 假设 / 草稿 → 身份、标签与作用域判定 → 标的与板块识别 → 当前总资本、资金信封与风险定仓 → 资金已结算 → 冻结提案 → 人工批准 → 授权 → 动态 MTM 风险复核与风险预留 → Trading 复合意图 → VenueAdapter → Freqtrade worker 交易订单 / Margin Controller 保证金动作 → 交易所事实 → 原生保护 → 自动加减仓 → 退出 → 结算**

### 19.1 每笔交易绑定的版本

- 策略主版本。
- 参数版本。
- 提案来源、提案人和人工假设版本。
- Market Catalog、标准化底层资产、板块和风险簇版本。
- 风险政策版本。
- `Total Capital Snapshot_0`、冻结 1R、档位最大允许亏损、`FundingEnvelope_0` / Trade Funding Envelope、每次 Add 的 Current Portfolio MTM 快照、Trade / 各层 Scope Worst-Case Loss 和 `target_reached` 证据。
- 风险资本模式、Vault 合格资产 / 折扣 / 贡献上限、`risk_inclusion_mode` 和 `capital_transfer_mode` 版本。
- 数据合同版本。
- 成本模型版本。
- Binance、Hyperliquid Core 或 HIP-3 DEX 执行附件版本。
- VenueAdapter、Freqtrade、CCXT、worker / bot config、受管账户、`margin_mode`、保证金能力、`collateral_scope` 和 `collateral_pool_id` 版本。
- 用户身份、全部标签、有效作用域、MFA / 复核事实和审核政策版本。

不同版本的绩效只有在明确可比较时才能汇总。

### 19.2 结果拆分

- 信号选择贡献。
- SYSTEM 信号选择与 MANUAL 人工择时贡献，二者分别统计。
- 排名和组合分配贡献。
- 资产板块和跨板块风险分配贡献。
- 交易所选择贡献。
- 初始仓贡献。
- 第一次加仓贡献。
- 第二次加仓贡献。
- 第三次加仓贡献。
- 硬止损、趋势退出和动态去杠杆贡献；布林回调只归因于加仓过滤，不归入退出贡献。
- 手续费、点差、滑点与资金费。
- 人工审核贡献。
- 异常、恢复和执行偏差损失。

总 PnL 只能说明最终结果，不能说明系统为什么赚钱或亏损。

---

## 二十、当前版本的决策登记

### 20.1 已固定

- Binance 与 Hyperliquid 的 U 本位永续支持宇宙，按加密货币、美股及股指、贵金属、商品及商品期货参考资产分板块风险。
- Binance 与 Hyperliquid。
- Trading 下属 `BinanceVenueAdapter` / `HyperliquidVenueAdapter`；两者只把已批准交易订单交给独立 Freqtrade worker，Trading 保留策略、风险、授权、OMS、控制动作与对账主权；场内保证金动作进入 Margin Controller，资金划转进入 Capital Transfer Orchestrator。
- Web / PWA 是电脑与手机统一主控制台。
- Telegram 只提供通知和白名单受限操作，全部动作先经过 Trading。
- SYSTEM 系统提案和 MANUAL 人工提案双入口；人工提案只在 Web / PWA 创建。
- 用户支持 `OBSERVER / PROPOSER / REVIEWER / OPERATOR / RISK_ADMIN / TREASURY_ADMIN / SYSTEM_ADMIN` 多标签；权限受作用域约束、显式拒绝优先，并执行自审限制、必要复核、MFA 和完整审计。
- 初仓由人工明确批准，Risk Engine 自动审核并拒绝超额风险；程序是受管账户唯一日常订单发送者。
- 开仓时选择是否允许自动加仓。
- 自动加仓不逐次人工审核。
- 低 / 中 / 高对应最大 3x / 5x / 10x。
- 1R 固定为待审核交易提案版本冻结时 `Total Capital Snapshot_0` 的 0.5%；低 / 中 / 高整份经济仓位最大允许亏损分别为 1R / 2R / 3R，即 0.5% / 1.0% / 1.5%。
- `FundingEnvelope_0` 等于 `Total Capital Snapshot_0 × trade_funding_pct`，展示名称为 Trade Funding Envelope；1.5% 仅为示例，正式默认 `N` 待资金政策冻结，资金占用不是风险。来自 Vault 的初仓资金必须在下单前结算，持仓期禁止以任何名义从 Vault 救仓。
- 自动加仓默认关闭；启用后低 / 中 / 高默认最多 1 / 2 / 3 次，加仓系统硬上限为三级。
- 冻结收益率 30% / 50% / 100% 只解锁 Add-1 / Add-2 / Add-3 候选；实际加仓时当前收益还须不低于对应门，并同时满足 `L_effective < L_min`、布林偏差回调过滤、保护、风险、成本、流动性与执行门。
- 每次 Add 默认按当前 `E_campaign` 补到冻结 `L_target`，不配置每级数量、初仓百分比、冻结收益百分比或风险份额；风险与执行约束只可安全缩量，并显示 `target_reached`。
- Add 单元由任意大于零的真实成交消费一次；终态零成交不消费并可在新快照重新候选。动态去杠杆不消费或返还 Add。
- 每次 Add 前使用包含未实现盈亏的最新 Current Portfolio MTM Equity 重算组合预算；本仓 Trade Worst-Case Loss 取冻结 / 动态单笔上限中更严格者，各层 Scope Worst-Case Loss 分别与对应预算比较。各层计算必须包含归属于该层的 Open / Reserved / Unknown Heat、受保护利润回吐和费用 / 滑点 / 资金费压力，盈利不能自动扩权。
- “回调”是当前价格相对布林参考线的偏差区间过滤，不是历史盈利峰值回撤，不自动撤销 Add 或按加仓层减仓。
- `L_effective > L_max` 时以 `reduce-only` 降向 `L_target` 或更小安全目标，按当前净仓位执行，不按加仓层顺序解释。
- 只做盈利金字塔，不亏损加仓。
- 风险只自动收紧。
- 交易所批准后锁定。
- 退出自动执行。
- 两所独立执行认证。
- Hyperliquid Core 与每个 HIP-3 DEX 独立执行认证。
- 每个 Freqtrade bot / config 只使用一种 `margin_mode` 并独占杠杆账户；cross / isolated 使用独立账户、worker、配置和证书。
- Binance cross、Binance isolated 与 Hyperliquid isolated 均为完整目标能力，分别使用隔离运行单元并逐标的认证对应保证金能力。
- 保证金能力明确分为 `CROSS_SHARED / ISOLATED_REMOVABLE / STRICT_ISOLATED`；净仓位动作先单腿执行，只有 removable 在增仓对账后可选择执行已认证的 excess-margin normalization。
- 逐仓 DynamicPositionSizing 模板可预授权 `POST_REDUCTION_MARGIN_NORMALIZATION`：减仓成交后只把本次实际释放且已预留的场内保证金按冻结目标回加到剩余仓位；不得调用 Vault、增加净仓位或放宽止损，未认证 / 失败时保持更小仓位或继续退出。
- Vault 是否计入 Current Portfolio MTM Equity 和 Extended Risk Equity 可选且默认关闭；风险计入开关与资金划转开关独立。空仓盈利自动归集，自动运营补充默认关闭且即使未来启用也只为空仓后的下一周期服务，活动仓位绝不接受 Vault 救援注资。
- 每个场所 / 抵押池使用目标 `n%` 与 `T_low / T_high` 滞回政策；人工双向划转和自动运营划转统一进入 Capital Transfer Orchestrator。
- `collateral_scope / collateral_pool_id` 是证书身份；账户抽象或共享抵押边界变化会使相关证书失效。
- Trading 核心只保留 Venue Port 扩展边界；未立项的 CFD 等场所不预建具体适配器，也不能继承两所证书。
- 本系统以趋势惯性、突破后部分标的持续趋势、右偏收益、成交量 / 持仓量的辅助信息、组合风险可系统控制以及受保护浮盈加仓可释放风险预算六条可证伪假设为研究前提；对应证据不成立时关闭能力。

### 20.2 建议作为研究默认值预登记

- 4 小时与 1 日周期。
- 20 / 55 突破窗口。
- 已完成 K 线确认。
- 组合 Heat 和风险状态沿用总体方案中的研究起点。
- 本文件第十五章第 2 节至第十七章的样本、正期望、成本、回撤和扩容研究门；第 15.1 节零容忍门属于永久固定规则。

### 20.3 仍需研究后冻结

- 多空是否使用相同周期和参数。
- 突破、确认、失效和趋势退出的精确规则。
- 评分维度、权重和绝对入选门。
- 初始止损、趋势退出和动态去杠杆的精确确认与安全目标参数。
- 初仓授权的时间和价格边界。
- 各级保护距离、候选确认周期和机会失效；默认 30% / 50% / 100% 候选解锁口径已固定，每级数量 / 风险比例不属于参数，任何真实成交后 Add 不重新武装。
- 按方向、风险档位、VenueAdapter、margin mode 与抵押池冻结的 `L_target / L_min / L_max` 和 `E_campaign` 精确归属公式。
- `target_reached` 容差、风险 / 执行缩量顺序，以及零成交、部分成交和价格漂移的展示与恢复语义。
- 布林回调的价格源、周期、已完成 K 线要求、窗口、标准差倍数、参考线、偏差区间、陈旧门和异常处理。
- Binance、Hyperliquid Core 与已批准 HIP-3 DEX 的具体标的白名单和优先选择规则。
- 四个板块的正式 Heat / Stress Heat 限额、跨板块风险簇和 Vault 最大贡献比例。
- `trade_funding_pct` 的正式值、Current Total Capital / Current Portfolio MTM Equity 资本范围、Vault 折扣、动态预算和最坏损失各组件的互斥记账规则。
- 各场所 / `collateral_pool_id` 的目标 `n%`、`T_low / T_high`、空仓盈利归集量、最小划转量、等待期、有效期与 CTO 失败恢复门。
- 各标的 / 账户的三类保证金能力映射、`ISOLATED_REMOVABLE` excess-margin normalization，以及 `POST_REDUCTION_MARGIN_NORMALIZATION` 的目标缓冲、最大回加额、fencing、失败降级与认证白名单。
- 各标的与执行域的流动性、容量、点差、滑点、资金费、交易时段和预言机门。
- 清算缓冲和极端止损穿透门。
- 真实成本容差的最终正式值。

在这些项目冻结前：

- 可以做研究与影子展示。
- 不得把主策略称为生产已认证。
- 自动加仓保持关闭。
- 本文件不签发现实交易权限。

---

## 二十一、后续文档拆分建议

本合同冻结后，建议依次形成八份附件：

1. **主策略参数登记表**：冻结周期、突破、排名、止损、30% / 50% / 100% 候选解锁、`L_target / L_min / L_max`、目标差额算法、`target_reached`、布林回调、动态去杠杆和退出。
2. **风险、授权与抵押附件**：冻结 `Total Capital Snapshot_0`、1R、三档最大亏损、Current Portfolio MTM Equity、最坏损失互斥组件、Heat、Add 单元、`collateral_scope / collateral_pool_id`、保证金能力、减仓后回加保证金授权、状态和恢复。
3. **资金分配与 CTO 附件**：冻结 `FundingEnvelope_0` / Trade Funding Envelope、`trade_funding_pct`、场所目标 `n%`、`T_low / T_high`、初仓前结算门、空仓盈利归集、运营补充开关、双向划转状态机、源端预留与 whitelist 软件政策。
4. **Binance VenueAdapter 执行附件**：冻结 Binance worker / config、独占账户、margin mode、保证金能力、保护、部分成交、结果未知和恢复。
5. **Hyperliquid VenueAdapter 执行附件**：按 Core / HIP-3 DEX、账户抽象、抵押池和 worker / config 从头独立冻结同类事实。
6. **Freqtrade 与控制面附件**：冻结每 bot 单一 margin mode、worker、账户隔离、Web / PWA、Telegram 和旁路阻断。
7. **身份、多标签与审核附件**：冻结标签动作矩阵、作用域、显式拒绝、自审政策、双人复核、MFA、会话撤权和审计字段。
8. **故障重放与证据清单**：列出每次能力激活需要的历史、影子、模拟和实盘证据，包括目标未达、零 / 部分成交、动态预算收紧、保证金 normalization、资金在途、权限冲突和账户抽象变化。

这些附件是后续实现的输入，不是本次开始写代码的许可。

---

## 二十二、最终判断

当前最合理的交易模块形态不是一个“突破后立即自动买入”的机器人，而是一套有明确认证单位的交易操作系统：

- Perptape 发现市场机会。
- Trading 把候选变成冻结提案。
- 人工按多标签、作用域、职责分离和复核政策审核开仓与有限自动加仓边界。
- 30% / 50% / 100% 只解锁候选；自动加仓只在 Add 有效、当前盈利、`L_effective < L_min`、布林回调、保护、趋势延续和实时风险全部成立时执行。
- 每次 Add 只计算到冻结 `L_target` 的差额，由最新组合 MTM 风险预算安全缩量；任何实际成交消费一次，零成交不消费，并明确显示是否达到目标。
- Risk Engine 始终可以否决。
- Trading OMS 只生成一次性、可追踪、仍然有效的意图；VenueAdapter 完成场所映射，绑定的独立 Freqtrade worker 只负责执行。
- Binance 与 Hyperliquid 分别证明自己能安全执行。
- 退出不依赖人工或辅助服务。
- 每个已完成工程交付的能力仍须通过影子、模拟与小额实盘证据门后才可扩容。
- 任何未知、漂移或证据失效都会让系统降级，而不是继续赌。

真正的正期望不是某个漂亮回测数字，而是以下四件事同时成立：

1. 冻结规则在时间外仍有净优势。
2. 加仓相对不加仓基线提供可重复的增量价值。
3. 真实成本、执行和故障没有吃掉优势。
4. 风险状态能在策略失效前限制损失，并且不会被人工绕过。

只要其中一项尚未被证明，对应能力就应保持关闭。

---

## 二十三、当前官方规则参考

以下链接核对至 2026-07-14，只用于说明为什么交易所执行、margin mode、账户独占、抵押池和保证金动作必须独立认证；`CROSS_SHARED / ISOLATED_REMOVABLE / STRICT_ISOLATED`、CTO 和风险授权仍是本系统自己的保守能力分类与政策。正式实现和每次上线前仍需重新核对：

- Binance USDⓈ-M Futures：[产品与 API 入口](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/Introduction)
- Binance USDⓈ-M Futures：[Change Initial Leverage](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#change-initial-leverage)
- Binance USDⓈ-M Futures：[Notional and Leverage Brackets](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#notional-and-leverage-brackets)
- Binance USDⓈ-M Futures：[Change Margin Type](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#change-margin-type)
- Binance USDⓈ-M Futures：[Modify Isolated Position Margin](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#modify-isolated-position-margin)
- Hyperliquid：[Margining](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/margining)
- Hyperliquid：[Account Abstraction Modes](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/account-abstraction-modes)
- Hyperliquid：[Margin Tiers](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/margin-tiers)
- Hyperliquid：[Order Types](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/order-types)
- Hyperliquid：[Liquidations](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/liquidations)
- Hyperliquid：[Exchange Endpoint：Leverage、Isolated Margin 与资金划转](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)
- Hyperliquid：[HIP-3 Builder-deployed Perpetuals](https://hyperliquid.gitbook.io/hyperliquid-docs/hyperliquid-improvement-proposals-hips/hip-3-builder-deployed-perpetuals)
- Hyperliquid：[Perpetuals 元数据、strict isolated 标注与 HIP-3 DEX 查询](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals)
- Freqtrade：[杠杆、单一 Margin Mode 与账户独占约束](https://www.freqtrade.io/en/stable/leverage/)
- Freqtrade：[Exchange-specific Notes 与 HIP-3 配置](https://docs.freqtrade.io/en/stable/exchanges/)
- Freqtrade：[REST API](https://www.freqtrade.io/en/stable/rest-api/)
- Freqtrade：[Telegram](https://www.freqtrade.io/en/stable/telegram-usage/)
- Freqtrade：[FreqUI](https://docs.freqtrade.io/en/latest/freq-ui/)
