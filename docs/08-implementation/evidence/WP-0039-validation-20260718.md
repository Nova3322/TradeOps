# WP-0039 验证证据（2026-07-18）

## 1. 验证范围

- `VenuePositionSnapshot.unrealized_pnl` 进入 current protected-position projection；
- `protected-position-risk-v2` 将 UPNL 纳入计算哈希；
- v12 execution contract、v1-v11 和错误 schema 拒绝；
- canonical UPNL 为零时返回 `ADD_CURRENT_UNREALIZED_PNL_NOT_POSITIVE`；
- 拒绝不创建 ADD decision、OrderIntent 或 RiskReservation，不消费 AddUnit；
- 正 UPNL 与 calculation hash 持久化回归；
- 全量测试、覆盖率、schema drift、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/unit/test_projections.py           15 passed
tests/integration/test_projections.py    10 passed
tests/integration/test_execution.py      26 passed
```

专项场景确认：

```text
execution.intent.create.v12 + schema 12 -> accepted contract
execution v1..v11                      -> COMMAND_TYPE_MISMATCH
v12 + non-12 schema                    -> PAYLOAD_SCHEMA_VERSION_MISMATCH
canonical UPNL = 0 / -1                -> ADD_CURRENT_UNREALIZED_PNL_NOT_POSITIVE
nonpositive-UPNL ADD decision/intent/reserve -> 0 / 0 / 0
nonpositive-UPNL AddUnit               -> AVAILABLE
positive UPNL in decision snapshot     -> 9.75
protected-position calculation version -> protected-position-risk-v2
```

projection 单元测试确认仅改变 canonical UPNL 就会改变 calculation hash；unknown projection
不暴露 UPNL。

## 3. Schema 与迁移

本包不新增数据库结构：

```text
alembic current
20260718_0030 (head)

alembic check
No new upgrade operations detected.
```

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
314 passed, 1 warning in 73.62s
Total coverage: 88.06%
Required test coverage of 85% reached
```

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
111 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 41 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0030
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. ADD 不再忽略 canonical venue UPNL，零或负收益无法进入新增风险。
2. UPNL 绑定 exact position snapshot 并进入 v2 calculation hash，可重放。
3. 旧 v11 execution 显式拒绝，没有静默兼容旁路。
4. 本包只证明 gross venue UPNL 为正，没有解决净浮盈、frozen return 或 `E_campaign`。
