# WP-0054 验证证据（2026-07-18）

## 1. 验证范围

- latest actionable target 到 immutable non-dispatchable plan snapshot；
- command receipt、audit、outbox 与 plan snapshot 原子持久化；
- 同幂等键 replay、不同幂等键和并发命令收敛；
- HOLD、version conflict、stale position 失败关闭；
- 数据库 source-binding insert guard、UPDATE/DELETE immutability 与 downgrade guard；
- full regression、branch coverage、静态检查、schema/readiness 与现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/unit/test_campaign_reduction_preparation.py
tests/integration/test_campaign_reduction_preparation.py
7 passed in 4.47s
```

持久化计划边界：

```text
action=EXIT
side=SELL
order_quantity=0.5
reduce_only=true
order_type_status=UNAVAILABLE
venue_execution_terms_status=UNAVAILABLE
environment=SHADOW
dispatch_eligible=false
live_order_eligible=false
OrderIntent delta=0
```

收敛结果：

```text
same idempotency key -> ALREADY_PROCESSED receipt replay
different key, same target -> ALREADY_PREPARED
two concurrent keys -> one PREPARED + one ALREADY_PREPARED
durable snapshot count=1
```

失败场景：

```text
latest target HOLD -> CAMPAIGN_REDUCTION_TARGET_NOT_ACTIONABLE
target version mismatch -> VERSION_CONFLICT
stale current position -> CAMPAIGN_CURRENT_POSITION_UNAVAILABLE
```

## 3. 数据库与迁移

```text
previous schema=20260718_0036
current schema=20260718_0037
new table=campaign_reduction_plan_snapshots
record contract=campaign-reduction-plan-snapshot-v1
plan contract=campaign-reduction-execution-plan-v1
```

验证结果：

```text
direct UPDATE -> rejected: campaign_reduction_plan_snapshots is immutable
forged duplicate INSERT -> rejected
non-empty downgrade -> rejected: cannot downgrade while Campaign reduction plans exist
empty 0037 -> 0036 -> 0037 round trip -> passed
```

## 4. 全量质量门

### 4.1 全量功能回归

```text
380 passed, 1 warning in 89.94s
```

### 4.2 Branch coverage 门

```text
380 passed, 1 warning in 125.68s
Total coverage: 87.85%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint 与类型

```text
ruff format --check src tests migrations
152 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 58 source files

git diff --check
passed
```

### 4.4 Schema、readiness 与现实能力门

```text
schema=20260718_0037
alembic check=No new upgrade operations detected
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. reduction plan 已成为可重放的耐久准备证据，不再依赖一次查询返回值。
2. 单 target 在重放、不同幂等键和并发下只形成一条快照。
3. 快照没有 OrderIntent、sender claim 或场所参数，不能被解释为可执行订单。
4. 现实能力门保持关闭；本包不构成实盘就绪或逐场所认证证据。
