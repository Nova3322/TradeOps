# WP-0046 验证证据（2026-07-18）

## 1. 验证范围

- `VENUE_FUNDING` 作为 reconciliation v2 的第八个必需来源；
- settled funding PAYMENT/RECEIPT 的原生币种与 signed economic cost；
- effect/sign、scope、external ID、input membership 与 immutable 约束；
- v1 历史证据兼容、v2 新 run exact manifest 和防数据丢失 downgrade；
- 全量回归、branch coverage、静态检查、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_reconciliation.py
tests/integration/test_venue_facts.py
37 passed in 7.63s
```

成功场景：

```text
payment: amount=2.5 USDT, effect=PAYMENT
receipt: amount=-1.25 USDT, effect=RECEIPT
source_type=VENUE_FUNDING
fact_type=FUNDING_PAYMENT
schema_version=2
required_source_count=8
```

失败与完整性场景：

```text
PAYMENT amount paired with RECEIPT effect
  -> VENUE_FUNDING_PAYMENT_INVALID

attempted update of canonical funding amount
  -> venue_funding_payments is immutable

downgrade with canonical funding or v2 reconciliation evidence
  -> cannot downgrade while canonical funding or v2 reconciliation evidence exists
```

## 3. 归属与币种边界

```text
funding command=execution.venue-funding-payment.record.v1
schema=20260718_0035
fact authority=VENUE_PRIVATE
environment=SHADOW
live dispatch eligible=false
```

资金费只保留原生 currency 和 signed cost；没有把 USDT 按 1:1 转成 USD，没有写入 Campaign attribution，
也没有用于计算 Frozen Return 或 `E_campaign`。

## 4. 全量质量门

### 4.1 全量功能回归

```text
344 passed, 1 warning in 73.20s
```

### 4.2 Branch coverage 门

```text
344 passed, 1 warning in 111.33s
Total coverage: 88.20%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint、类型与迁移

```text
ruff format --check src tests migrations
131 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 49 source files

alembic check
No new upgrade operations detected.

alembic current
20260718_0035 (head)

git diff --check
passed
```

### 4.4 Readiness 与现实能力门

```text
schema=20260718_0035
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. 新 reconciliation run 不再允许遗漏资金费来源；零条结果也必须提交完整 `VENUE_FUNDING` 水位。
2. 已结算 funding payment/receipt 以不可变私有场所事实保存，符号合同在模型、服务和数据库同时生效。
3. 历史 v1/七来源 run 保持可读，新 v2 证据不会被降级迁移静默删除。
4. 本包不推断 Campaign 归属、不进行 FX，也没有改变任何现实资金能力。
