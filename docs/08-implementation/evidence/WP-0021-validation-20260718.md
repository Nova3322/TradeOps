# WP-0021 验证证据（2026-07-18）

## 1. 验证范围

本次验证覆盖：

- `capital_projection_binding` 必填且无旧协议回退；
- 风险政策 ACCOUNT freshness 驱动事务内组合重算；
- manifest ID/version/hash 与 projection version 强绑定；
- 提案交易账户 exact-scope membership；
- 当前权益、UPNL、可用保证金、Vault 计入和冻结资本的反伪造；
- manifest 缺失/过期、账户事实 stale、USDT 缺少 FX 时在风险数学前失败关闭；
- 风险决策 manifest 外键、hash/version check、不可变字段和 source snapshot 证据链；
- proposal risk、portfolio projection、纯数学与 Shadow OrderIntent 兼容性回归；
- migration upgrade/downgrade、schema drift、格式、lint、strict Mypy 与全量 coverage gate。

按用户特别约束，未调用 Codex Security 或任何 Codex Security 审计 Skill、插件、模块。

## 2. 实际执行结果

### 2.1 专项与兼容性回归

```text
risk math + proposal risk + capital scope + projections
50 passed

execution + trading authorization + capability certificates
35 passed
```

### 2.2 全量测试与覆盖率

```text
223 passed, 1 warning
Required test coverage of 85.0% reached.
Total coverage: 87.16%
```

唯一 warning 来自 FastAPI TestClient 依赖链中的 Starlette/httpx deprecation，不是本工作包失败。

### 2.3 格式、lint 与类型

```text
ruff format --check src tests migrations
86 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 33 source files
```

### 2.4 数据库迁移与漂移

在本地 disposable PostgreSQL `trading_test` 已实际执行：

```text
20260718_0020 -> 20260718_0021
20260718_0021 -> 20260718_0020
20260718_0020 -> 20260718_0021
20260718_0021 (head)
No new upgrade operations detected.
```

数据库约束实际存在：

```text
ck_risk_decisions_capital_binding_integrity
fk_risk_decisions_capital_scope_manifest
readiness=(True, None)
```

### 2.5 Durable readiness 与 live gates

```text
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
schema=20260718_0021
readiness=(True, None)
```

## 3. 结果判定

1. 提案 RiskPrecheck 不再能脱离完整账户 manifest 使用调用方自报的当前资本。
2. 决策证据可追到 manifest 和每个 canonical venue account snapshot，且 hash/version 受 DB 约束。
3. stale、missing、expired、FX unknown、错绑和数值伪造在风险数学前失败，不产生风险决策假事实。
4. 现有纯风险数学与 Shadow 执行事务兼容；最终 ORDER_PRECHECK 的同类绑定仍明确未完成。
