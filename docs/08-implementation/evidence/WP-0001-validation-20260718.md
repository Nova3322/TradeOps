# WP-0001 验证证据：2026-07-18

> 证据状态：PASS
> 验证时间：2026-07-17T18:06:35Z
> 验证基线：父节点 `92a6a3018bdf`，WP-0001 待提交工作树
> 作用域：耐久控制面基础；不签发任何实盘、资金、订单、Telegram 或部署权限

## 固定环境

| 项目 | 验证值 |
| --- | --- |
| Python | 3.12.13 |
| uv | 0.11.17 |
| PostgreSQL | 18.4，临时 `postgres:18-alpine` 容器 |
| Alembic head | `20260718_0001` |

连接串只通过进程环境传入，未写入仓库或本证据。测试数据库是一次性本地容器，不含用户资产或真实账户数据。

## 静态与自动化结果

执行：

```text
ruff format src tests migrations
ruff check src tests migrations
mypy src
TEST_DATABASE_URL=<disposable-postgres-url> pytest --cov --cov-report=term-missing
```

结果：

- Ruff：全部通过，20 个文件格式稳定。
- Mypy strict：12 个源文件无问题。
- Pytest：23 passed。
- 覆盖率：90.78%，门槛 85%。
- 有一个 FastAPI `TestClient` 上游弃用提示，不影响本次通过；依赖升级时必须复核并移除。

已覆盖的关键行为：

1. 三项真实能力迁移后全部为 `DISABLED:v1`，首包没有 enable 操作。
2. 状态、command receipt、audit event 与 outbox 在同一事务提交。
3. 同语义重试返回原结果，不重复状态、审计或 outbox 副作用。
4. 同幂等键不同语义返回 `IDEMPOTENCY_KEY_REUSED`，只追加冲突审计。
5. 两个并发重复命令只有一个完成业务副作用。
6. 处理器异常回滚状态、receipt、审计与 outbox 的全部写入。
7. 数据库触发器拒绝 audit event 的 UPDATE 和 DELETE。
8. inbox 同一消费者与消息只处理一次；相同消息身份不同载荷会失败关闭。
9. readiness 对已迁移数据库与三项门禁返回 ready；API 失败路径返回稳定 503 错误码。

## 迁移往返

执行 `upgrade head -> downgrade base -> upgrade head`，观察结果：

```text
downgrade: command_receipts=ABSENT, deny_immutable_audit_change()=ABSENT
upgrade:   20260718_0001 (head)
AUTO_ADD=DISABLED:v1
CAPITAL_TRANSFER=DISABLED:v1
LIVE_ORDER_SEND=DISABLED:v1
```

降级删除首包表、审计触发器和函数；重新升级恢复全部默认关闭种子。该往返只证明空测试库的结构回滚，不是未来生产数据恢复证明。

## 尚未授权与后续门

- 没有 Proposal / Approval / Risk / OMS / Venue / Freqtrade / Margin / CTO 实现。
- 没有订单发送、交易所私钥、签名、资金划转或真实账户连接路径。
- 没有 Web/PWA 或 Telegram 产品入口。
- Vault 现实事实、司法辖区以及正式 owner/reviewer/backup 仍需外部输入。
- 任何真实能力启用都必须由后续独立工作包、测试与证书完成；本证据不得外推。
