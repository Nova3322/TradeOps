# WP-0016 验证证据（2026-07-18）

## 1. 验证范围

本次验证覆盖 canonical `VenueProtectionSnapshot`：

- `CONFIRMED / DEGRADED / UNKNOWN` 三态的独立语义；
- exact open-position ownership、方向、数量和事件时序；
- 全局去重、跨运行重新链接和冲突拒绝；
- protection input item count 与 immutable link manifest 的精确一致；
- 超量事实、缺失事实、直接数据库绕过和 update/delete 的失败关闭；
- order/fill/position 既有事实与 ExecutionFact v4 回归；
- migration upgrade/downgrade、schema drift、格式、lint、strict Mypy 与全量 coverage gate。

## 2. 实际执行结果

### 2.1 专项与回归

```text
tests/integration/test_venue_facts.py
19 passed

reconciliation + execution + execution_fact_binding + successor_lease_recovery
51 passed
```

### 2.2 全量测试与覆盖率

```text
187 passed, 1 warning
Required test coverage of 85.0% reached.
Total coverage: 86.78%
```

唯一 warning 来自 FastAPI TestClient 依赖链中的 Starlette/httpx deprecation，不是本工作包失败。

### 2.3 格式、lint 与类型

```text
ruff format --check src tests migrations
76 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 30 source files
```

### 2.4 数据库迁移

在本地 disposable PostgreSQL `trading_test` 上实际执行：

```text
20260718_0015 -> 20260718_0016
20260718_0016 -> 20260718_0015
20260718_0015 -> 20260718_0016
20260718_0016 (head)
```

Alembic drift 检查：

```text
No new upgrade operations detected.
```

## 3. 结果判定

1. protection 事实不再是无结构 payload，而是精确绑定 canonical open position 的 immutable set snapshot。
2. `UNKNOWN` 不等于零保护，`DEGRADED` 不等于足额保护，只有满足完整结构合同的事实才为 `CONFIRMED`。
3. 对账输入少一条或多一条保护成员均不能推进 phase。
4. 所有 live capability gates 仍为 `DISABLED`；本工作包不构成实盘保护认证或实盘就绪证明。
