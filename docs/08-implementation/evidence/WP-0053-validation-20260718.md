# WP-0053 验证证据（2026-07-18）

## 1. 验证范围

- durable zero target 到 non-dispatchable reduce-only EXIT plan；
- LONG/SHORT 平仓方向纯规则；
- current-target 差额与 action；
- HOLD、target missing、stale position 失败关闭；
- full regression、branch coverage、静态检查、schema/readiness 与现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/unit/test_campaign_reduction_plan.py
tests/integration/test_campaign_reduction_plan.py
6 passed in 2.77s
```

zero-target 计划：

```text
direction=LONG
side=SELL
current_quantity=0.5
target_quantity=0
order_quantity=0.5
action=EXIT
urgency=IMMEDIATE
reason_codes=[PROTECTION_MISSING]
reduce_only=true
```

非执行边界：

```text
order_type_status=UNAVAILABLE
venue_execution_terms_status=UNAVAILABLE
environment=SHADOW
live_order_eligible=false
```

失败场景：

```text
no durable target -> CAMPAIGN_REDUCTION_TARGET_MISSING
latest target HOLD -> CAMPAIGN_REDUCTION_TARGET_NOT_ACTIONABLE
stale current position -> CAMPAIGN_CURRENT_POSITION_UNAVAILABLE
```

## 3. 数据库与版本

```text
plan_version=campaign-reduction-execution-plan-v1
schema=20260718_0036
alembic revision change=none
```

plan 是 query-time 计算；没有新增记录、OrderIntent、sender claim、worker 请求或场所订单。

## 4. 全量质量门

### 4.1 全量功能回归

```text
373 passed, 1 warning in 85.94s
```

### 4.2 Branch coverage 门

```text
373 passed, 1 warning in 131.70s
Total coverage: 87.93%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint 与类型

```text
ruff format --check src tests migrations
147 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 56 source files

git diff --check
passed
```

### 4.4 Readiness 与现实能力门

```text
schema=20260718_0036
alembic check=No new upgrade operations detected
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. future OMS 不再需要 caller 自报 reduction side、quantity 或 current binding。
2. HOLD 或陈旧/不一致事实不会产生看似可执行的计划。
3. 未认证订单类型和场所参数保持显式 `UNAVAILABLE`。
4. 本包没有 OrderIntent 或发送能力，现实能力保持关闭。
