# WP-0041 验证证据（2026-07-18）

## 1. 验证范围

- INITIAL exact scope canonical current-position 解析；
- fresh confirmed FLAT 与零数量硬门；
- missing、OPEN、UNKNOWN、stale 和 caller 非零数量失败关闭；
- immutable position snapshot ID/hash 的 decision、input snapshot、return 和 outbox 绑定；
- flat fact TTL 收紧最终有效期；
- Execution v14 旧命令/旧 schema 拒绝；
- migration、schema drift、全量测试、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_execution.py    34 passed in 14.06s
```

专项场景确认：

```text
fresh CONFIRMED FLAT + quantity=0     -> ALLOW path binds exact snapshot ID/hash
confirmed OPEN                         -> INITIAL_REQUIRES_CANONICAL_FLAT_POSITION
UNKNOWN source                         -> INITIAL_CURRENT_POSITION_UNAVAILABLE
stale FLAT source                      -> INITIAL_CURRENT_POSITION_UNAVAILABLE
missing source                         -> INITIAL_CURRENT_POSITION_UNAVAILABLE
caller current_position_quantity > 0  -> EXECUTION_INPUT_INVALID
all rejection cases                    -> no decision / intent / reservation
```

成功 INITIAL 还确认 decision/input snapshot/return/outbox 使用同一快照 ID/hash，且 decision
`valid_until` 不晚于 `position facts_as_of + policy POSITIONS max_age_ms`。

## 3. 命令与迁移

```text
risk.precheck.evaluate.v9       payload schema 9
execution.intent.create.v14     payload schema 14
execution v1..v13               COMMAND_TYPE_MISMATCH
v14 + non-14 schema             PAYLOAD_SCHEMA_VERSION_MISMATCH
```

```text
alembic current
20260718_0032 (head)

alembic check
No new upgrade operations detected.

database containing INITIAL flat-position evidence
downgrade 0032 -> 0031 refused:
cannot downgrade initial flat-position binding while evidence remains

empty disposable test database downgrade 0032 -> 0031
passed

upgrade 0031 -> 0032
passed; 20260718_0032 (head)
```

迁移不 seed 仓位事实、不创建决定，也不打开任何能力。旧 v1-v13 决定允许空 binding；v14 新 INITIAL
由服务保证 exact binding。外键阻止被引用的 venue position snapshot 删除，pair/check constraint 阻止
不完整 binding 或 ADD 误写；存在新证据时 downgrade 明确失败。

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
331 passed, 1 warning in 95.54s
Total coverage: 88.24%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
118 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 43 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0032
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. INITIAL 不再只相信 caller 自报零仓位；同范围场所仓位必须由 fresh canonical FLAT 证明。
2. 快照 ID/hash 和完整投影可从最终决定重放，空仓事实 TTL 不能被长授权放宽。
3. 任何缺失、未知、陈旧或非空仓事实均在新增风险图创建前失败关闭。
4. 本包仍是 SHADOW 控制合同，不代表真实 collector、OMS 或场所发送已经完成或认证。
