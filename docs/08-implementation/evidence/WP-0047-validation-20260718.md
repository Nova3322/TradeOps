# WP-0047 验证证据（2026-07-18）

## 1. 验证范围

- Campaign opening/current-position 区间与 exact sender scope 绑定；
- latest successful reconciliation v2 的完整 `VENUE_FUNDING` 水位；
- PAYMENT/RECEIPT 原生 signed candidate totals 与确定性重建；
- 完整零计数、latest FAILED 和不足窗口的失败关闭语义；
- 全量回归、branch coverage、静态检查、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_campaign_funding_coverage.py
3 passed in 3.87s
```

成功候选场景：

```text
scope_interval_coverage_status=EXACT
payment=2.5 USDT
receipt=-1.25 USDT
native_signed_cost_total=1.25 USDT
candidate_count=2
campaign_attribution_status=UNAVAILABLE
economic_equity_status=UNAVAILABLE
```

同一 sources/TTL、相差 1ms 的有效查询得到相同 projection hash；篡改 candidate count 被模型自校验拒绝。

完整零计数场景：

```text
input_item_count=0
candidate_count=0
source_payments=()
native_signed_cost_totals=()
scope_interval_coverage_status=EXACT
```

失败场景：

```text
latest exact-scope reconciliation status=FAILED
  -> CAMPAIGN_FUNDING_COVERAGE_UNAVAILABLE

latest successful funding watermark starts after Campaign interval
  -> CAMPAIGN_FUNDING_COVERAGE_UNAVAILABLE
```

## 3. 非归属与版本边界

```text
projection_version=campaign-funding-coverage-projection-v1
schema=20260718_0035
alembic revision change=none
```

候选 totals 没有写入 Campaign entry，没有把 USDT 按 1:1 转成 USD，也没有用于 Frozen Return 或
`E_campaign`。latest 失败水位不会由较旧成功 run 掩盖。

## 4. 全量质量门

### 4.1 全量功能回归

```text
347 passed, 1 warning in 75.47s
```

### 4.2 Branch coverage 门

```text
347 passed, 1 warning in 114.13s
Total coverage: 88.11%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint 与类型

```text
ruff format --check src tests migrations
133 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 50 source files

git diff --check
passed
```

### 4.4 Readiness 与现实能力门

```text
schema=20260718_0035
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. Campaign 现在能证明最新 funding input 的完整水位覆盖 opening-only 当前持仓区间。
2. PAYMENT/RECEIPT 只作为 exact scope/time candidates 展示；完整零计数与缺失水位不再混淆。
3. latest 失败或不足窗口固定失败关闭，不会复用较旧成功证据。
4. exact 候选不等于经济所有权；本包未创建归属、FX 或现实资金能力。
