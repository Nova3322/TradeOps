# WP-0038 验证证据（2026-07-18）

## 1. 验证范围

- `AddEligibilitySnapshot` 删除 `current_effective_leverage`；
- v11 execution contract、v1-v10 和错误 schema 拒绝；
- 旧 leverage 字段返回 `EXECUTION_INPUT_INVALID`；
- canonical quantity/Mark/multiplier 与 submitted campaign equity 的服务端派生；
- 18 位保守上取整、`L_effective == L_min` 拒绝；
- calculation hash 在 input snapshot、decision、命令结果与 event 中一致；
- ADD canonical protection、零成交释放、正成交消费与风险预留回归；
- 全量测试、覆盖率、schema drift、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_execution.py    24 passed
```

专项场景确认：

```text
execution.intent.create.v11 + schema 11 -> accepted contract
execution v1..v10                      -> COMMAND_TYPE_MISMATCH
v11 + non-11 schema                    -> PAYLOAD_SCHEMA_VERSION_MISMATCH
v11 + current_effective_leverage       -> EXECUTION_INPUT_INVALID
derived L_effective == frozen L_min    -> ADD_LEVERAGE_NOT_BELOW_MINIMUM
canonical protection hash mismatch     -> ADD_ELIGIBILITY_CANONICAL_FACT_MISMATCH
```

成功 ADD 样本使用 canonical current notional `60`、submitted campaign equity `72`，派生保守
`L_effective=0.833333333333333334`。样本明示记录
`campaign_equity_source=CALLER_PENDING_CAMPAIGN_PNL_LEDGER`，没有把 caller equity 声称为耐久真源。

## 3. Schema 与迁移

本包不新增数据库结构：

```text
alembic current
20260718_0030 (head)

alembic check
No new upgrade operations detected.
```

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
311 passed, 1 warning in 70.05s
Total coverage: 88.05%
Required test coverage of 85% reached
```

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
111 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 41 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0030
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. ADD 客户端不再能提交任意 `current_effective_leverage` 绕过 `L_min`。
2. 当前名义价值仅来自 exact canonical position，派生杠杆向上取整并可哈希重放。
3. 旧字段和旧 v10 execution 均显式拒绝，没有静默兼容旁路。
4. 本包没有解决 campaign equity、frozen return 或 trend 真源，也不构成真实 ADD 或实盘能力。
