# WP-0044 验证证据（2026-07-18）

## 1. 验证范围

- 两笔 partial/full canonical opening fills 的确定性 source 排序；
- cumulative quantity/notional、native signed fee totals 与 intent/fill counts；
- KNOWN realized PnL 零值和 UNKNOWN 条数严格分离；
- projection source refs/hash、自校验与无 fill 失败关闭；
- 全量回归、静态检查、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_campaign_opening_projection.py
2 passed in 1.17s
```

专项数据：

```text
fill 1: quantity=0.2, notional=10000, BNB charge=0.002, realized PnL UNKNOWN
fill 2: quantity=0.3, notional=15000, BNB rebate=-0.001, realized PnL KNOWN 0 USDT

projection:
fill_count=2
intent_count=1
cumulative_quantity=0.5
cumulative_notional=25000
native_fee_totals=[BNB: 0.001]
known_realized_pnl_totals=[USDT: 0]
realized_pnl_unknown_count=1
economic_equity_status=UNAVAILABLE
```

篡改 cumulative quantity 被 projection validator 拒绝；无 accepted fill 返回
`CAMPAIGN_OPENING_FILL_PROJECTION_UNAVAILABLE`。

## 3. 数据与版本

```text
projection_version=campaign-opening-fill-projection-v1
schema=20260718_0034
alembic revision change=none
```

投影不写数据库，source of truth 仍是 WP-0043 immutable CampaignFillEconomicEntry。相同 source/version
不包含 wall-clock 字段，必须重建同一 projection hash。

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
340 passed, 1 warning in 98.14s
Total coverage: 88.17%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
128 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 48 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0034
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. opening fill prefix 的数量、名义价值和原生费用现在可从 immutable sources 确定重建。
2. KNOWN zero 与 UNKNOWN realized PnL 保持不同，不做跨币种估值或隐式 1:1。
3. 投影固定声明 Campaign equity 不可用，不能替代 current position、资金费、退出账本或 `E_campaign`。
4. 本包没有数据迁移、外部写入口或现实资金能力变化。
