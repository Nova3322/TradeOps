# WP-0052 验证证据（2026-07-18）

## 1. 验证范围

- latest durable target 重新绑定 fresh current-position hash；
- missing-protection + durable-target 多来源解释；
- stable carry source 不制造无限 target version；
- protection 恢复后 zero target 继续有效；
- full regression、branch coverage、静态检查、schema/readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_campaign_target_facts.py
5 passed in 4.90s
```

重复 missing-protection evaluation：

```text
v1: PROTECTION_MISSING, target=0
v2: PROTECTION_MISSING + DURABLE_TARGET_ACTIVE, target=0
next evaluation: NO_CHANGE, object_version=2, fact_count=2
```

保护恢复：

```text
latest durable target=0
canonical protection=CONFIRMED
carry source=DURABLE_TARGET_ACTIVE
new target=0
action=EXIT
Campaign.status=CLOSING
```

目标没有被恢复为 current quantity，也没有依赖 caller 提供旧 binding。

## 3. 非执行边界

```text
schema=20260718_0036
alembic revision change=none
candidate.policy_version=campaign-target-carry-forward-v1
```

本包仅增加 Campaign target evaluation 的内部来源；没有 OrderIntent、sender claim、场所请求或真实账户连接。

## 4. 全量质量门

### 4.1 全量功能回归

```text
367 passed, 1 warning in 81.50s
```

### 4.2 Branch coverage 门

```text
367 passed, 1 warning in 127.68s
Total coverage: 87.98%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint 与类型

```text
ruff format --check src tests migrations
144 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 55 source files

git diff --check
passed
```

### 4.4 Readiness 与现实能力门

```text
schema=20260718_0036
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. durable target 不再只靠“禁止放宽”被动保留，而能在 fresh current binding 上主动重新参与仲裁。
2. stable source namespace 使 carry 语义在一次归一后收敛为 `NO_CHANGE`。
3. 保护恢复不会取消已经耐久化的 zero target。
4. 本包仍没有退出 OrderIntent 或发送能力，现实能力保持关闭。
