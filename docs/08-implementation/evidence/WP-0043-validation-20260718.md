# WP-0043 验证证据（2026-07-18）

## 1. 验证范围

- accepted canonical VenueFill 到 Campaign/Intent/Fact/AddUnit 的唯一归属；
- 原生成交、手续费/返佣、settlement currency 与可选 venue realized PnL 原样保存；
- UNKNOWN realized PnL 与明确零值分离；
- fill/entry hash、命令返回、outbox event 和数据库 insert guard 绑定；
- update/delete 不可变、migration downgrade guard、schema drift 与全量质量门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_execution.py    37 passed in 16.17s

campaign fill economics focused tests  3 passed in 1.02s
```

专项场景确认：

```text
canonical INITIAL fill + BNB CHARGE + USDT settlement
    -> one immutable Campaign entry with exact native currencies
    -> return/outbox bind the same entry ID/hash

realized_pnl=NULL
    -> realized_pnl_status=UNKNOWN; not coerced to zero

realized_pnl=0
    -> realized_pnl_status=KNOWN; remains distinct from UNKNOWN

negative REBATE
    -> preserved as a negative native fee amount

scope/instrument mismatch
    -> CAMPAIGN_FILL_ECONOMIC_ENTRY_FACT_MISMATCH

database UPDATE or DELETE
    -> campaign_fill_economic_entries is immutable
```

现有执行专项同时覆盖 INITIAL/ADD、部分/完整成交、canonical ownership、风险迁移和后续 position/protection
流程；新增 entry 未改变原有状态机结果。

## 3. 命令、事务与迁移

ExecutionIntent 与执行事实命令版本保持不变；本包没有新增 caller 字段或外部账本写入口。entry 只在
INTERNAL reconciliation 已接受 canonical VENUE_FILL 时派生。

```text
alembic current
20260718_0034 (head)

alembic check
No new upgrade operations detected.

database containing one CampaignFillEconomicEntry
downgrade 0034 -> 0033 refused:
cannot downgrade campaign fill economic entries while records remain

empty disposable test database downgrade 0034 -> 0033
passed

upgrade 0033 -> 0034
passed; 20260718_0034 (head)
```

迁移不 seed VenueFill、ExecutionFact、entry 或能力。insert guard 重验 Intent/Fact/Fill/Reservation 的
所有权、scope、逐笔经济字段、费用、realized PnL 和 source hashes；唯一约束阻止重复归属。

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
338 passed, 1 warning in 99.02s
Total coverage: 88.25%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
126 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 47 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0034
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. 每笔被执行状态机接受的 canonical fill 现在有唯一、不可变、可重放的 Campaign 经济归属。
2. 手续费/返佣保留原生币种和符号，UNKNOWN realized PnL 不会静默变成零。
3. 本包没有计算成本基础、资金费、FX、Frozen Return 或 `E_campaign`，也没有实现减仓/退出账本。
4. 所有 entry 仍为 SHADOW-only，不构成真实 collector、OMS、财务完成或实盘能力认证。
