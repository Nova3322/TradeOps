# WP-0022 验证证据（2026-07-18）

## 1. 验证范围

- 提案 capital binding 必填并冻结到授权快照，无旧协议回退；
- final request 必须沿用同一 manifest ID/version/hash 与 projection version；
- 最终 ORDER_PRECHECK 在事务内重算最新 MTM、UPNL、风险权益与可用保证金；
- `Total Capital Snapshot_0`、1R 与 Funding Envelope 继续使用冻结授权值；
- 旧资本、替换 manifest、账户事实 stale/Unknown/missing/FX unknown 在风险数学前失败关闭；
- ALLOW 与确定性 DENY 均保存 source snapshot 证据链和 projection hash；
- execution decision 的 manifest 外键、hash/version check 与 immutable trigger；
- OrderIntent、风险预留、事实绑定、sender fencing、successor lease 和 reconciliation 回归；
- 0022 upgrade/downgrade、schema drift、readiness、静态检查与全量覆盖率门。

按用户特别约束，未调用 Codex Security 或任何 Codex Security 审计 Skill、插件、模块。

## 2. 实际执行结果

### 2.1 专项与下游回归

```text
authorization + final ORDER_PRECHECK
35 passed

execution fact binding + sender fencing + successor lease
48 passed

proposal review + capability certificate + RiskPrecheck + capital scope + projections
71 passed
```

### 2.2 冻结与动态预算证据

```text
Total Capital Snapshot_0 = 100000
frozen 1R_0             = 500
latest portfolio MTM    = 90000
latest UPNL             = -10000
dynamic LOW cap         = 450
```

同一用例成功创建 SHADOW 原子预留；提交旧 100,000 current MTM 的调用方请求返回
`CAPITAL_INPUT_MISMATCH`，未写执行风险决策、OrderIntent 或预留。

### 2.3 全量测试与覆盖率

```text
228 passed, 1 warning
Required test coverage of 85.0% reached.
Total coverage: 87.16%
```

唯一 warning 来自 FastAPI TestClient 依赖链的 Starlette/httpx deprecation，不是本工作包失败。

### 2.4 格式、lint 与类型

```text
ruff format --check src tests migrations
87 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 33 source files
```

### 2.5 数据库迁移与漂移

```text
20260718_0022 -> 20260718_0021
20260718_0021 -> 20260718_0022
20260718_0022 (head)
No new upgrade operations detected.
```

数据库约束实际存在：

```text
ck_exec_risk_capital_binding_integrity
fk_exec_risk_capital_scope_manifest
```

### 2.6 Durable readiness 与 live gates

```text
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
schema=20260718_0022
readiness=(True, None)
```

## 3. 结果判定

1. 最终 ORDER_PRECHECK 不再使用未验证的调用方当前资本。
2. 当前权益变化只收紧/恢复冻结授权内部容量，不能重写 `Snapshot_0` 或扩大 1R。
3. 最终决策可追到授权冻结 manifest 和本次最新 canonical account source snapshot。
4. 现实下单能力未开启；Ledger 派生 funding/heat/scope exposure 仍是下一项风险闭环工作。
