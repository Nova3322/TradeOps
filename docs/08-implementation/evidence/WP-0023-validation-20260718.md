# WP-0023 验证证据（2026-07-18）

## 1. 验证范围

- 组织级 RiskReservation/RiskExposureState 固定顺序行锁与完整聚合；
- campaign Open/Reserved/Unknown Heat 精确派生；
- global funding used/reserved/unknown 精确派生；
- canonical available margin 扣除内部 Reserved/Unknown margin；
- 七层 scope allocation 按 active Heat 比例收敛；
- 调用方高报、低报均失败关闭；
- Unknown、部分成交、零成交释放、正成交、successor lease 与 reconciliation 回归；
- durable exposure snapshot、source evidence、hash 列和 immutable trigger；
- 0023 upgrade/downgrade、schema drift、格式、lint、strict Mypy 与全量 coverage gate。

按用户特别约束，未调用 Codex Security 或任何 Codex Security 审计 Skill、插件、模块。

## 2. 实际执行结果

### 2.1 专项与下游回归

```text
final ORDER_PRECHECK durable exposure
16 passed

execution fact binding + sender fencing + successor lease + reconciliation
57 passed
```

专项证据包括：

```text
canonical available margin = 10000
internal margin reserved   = 500
risk available margin      = 9500

caller high report -> DURABLE_EXPOSURE_INPUT_MISMATCH
caller low report  -> DURABLE_EXPOSURE_INPUT_MISMATCH
```

### 2.2 全量测试与覆盖率

```text
230 passed, 1 warning
Required test coverage of 85.0% reached.
Total coverage: 87.33%
```

唯一 warning 来自 FastAPI TestClient 依赖链的 Starlette/httpx deprecation，不是本工作包失败。

### 2.3 格式、lint 与类型

```text
ruff format --check src tests migrations
88 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 33 source files
```

### 2.4 数据库迁移与漂移

```text
20260718_0023 -> 20260718_0022
20260718_0022 -> 20260718_0023
20260718_0023 (head)
No new upgrade operations detected.
```

数据库实查：

```text
execution_risk_decisions.durable_exposure_snapshot_hash nullable=NO
ck_exec_risk_capital_binding_integrity present
```

### 2.5 Durable readiness 与 live gates

```text
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
schema=20260718_0023
readiness=(True, None)
```

## 3. 结果判定

1. 最终 ORDER_PRECHECK 不再接受调用方自报的 funding、Heat 或 scope 当前占用。
2. 内部未发送/Unknown margin 与 venue available margin 只扣一次，可复算且失败关闭。
3. 每个最终风险决策可追到完整 RiskExposureState 版本、Ledger sequence 和 evidence hash。
4. 提案阶段同类 Ledger 绑定及动态损失组件仍未完成，现实下单能力继续关闭。
