# WP-0037 验证证据（2026-07-18）

## 1. 验证范围

- `AddEligibilitySnapshot` 删除 `protection_valid` / `authorization_valid`；
- v10 execution contract、v1-v9 和错误 schema 拒绝；
- 旧 ADD 布尔字段返回 `EXECUTION_INPUT_INVALID`；
- 耐久 authorization graph、SystemRiskState、exact capability 和 current protection projection 回归；
- ADD canonical evidence mismatch、零成交释放、正成交消费与风险预留回归；
- 全量测试、覆盖率、schema drift、readiness 和现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

```text
tests/integration/test_execution.py    23 passed
```

专项场景确认：

```text
execution.intent.create.v10 + schema 10 -> accepted contract
execution v1..v9                    -> COMMAND_TYPE_MISMATCH
v10 + non-10 schema                 -> PAYLOAD_SCHEMA_VERSION_MISMATCH
v10 + protection_valid              -> EXECUTION_INPUT_INVALID
v10 + authorization_valid           -> EXECUTION_INPUT_INVALID
canonical protection hash mismatch  -> ADD_ELIGIBILITY_CANONICAL_FACT_MISMATCH
```

成功 ADD 仍必须通过耐久 package/unit/campaign/system 状态、exact capability、current canonical
position/protection、Risk Fact Set、MARKET payload、资本、风险账本和 scope 限额。确认零成交可释放 AddUnit，
任意正成交完整消费；结果 Unknown 继续锁定。

## 3. Schema 与迁移

本包不新增数据库结构：

```text
alembic current
20260718_0030 (head)

alembic check
No new upgrade operations detected.
```

## 4. 全量质量门

### 4.1 全量测试与覆盖率

```text
310 passed, 1 warning in 70.64s
Total coverage: 88.03%
Required test coverage of 85% reached
```

### 4.2 格式、lint 与类型

```text
ruff format --check src tests migrations
111 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 41 source files

git diff --check
passed
```

### 4.3 Readiness 与现实能力门

```text
schema=20260718_0030
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. ADD 客户端不再能提交或依赖 `protection_valid` / `authorization_valid`。
2. 授权与保护继续由同一事务内的耐久状态、能力和 canonical projection 决定。
3. 旧字段和旧 v9 execution 均显式拒绝，没有静默兼容旁路。
4. 本包没有解决 trend/return/equity 真源，也不构成真实 ADD 或实盘能力。
