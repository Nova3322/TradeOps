# WP-0055 验证证据（2026-07-18）

## 1. 验证范围

- Campaign 全部 INITIAL/ADD OrderIntentState 的 stable/blocking 分类；
- POSITION_RECONCILED 与 PROTECTION_CONFIRMED 不阻塞历史稳定初仓；
- active Add 的 ID、状态版本和数量进入 blocker；
- 新 position 失去保护且同时存在 active Add 时，EXIT target 不能形成 reduction plan；
- WP-0053 plan 与 WP-0054 preparation 回归；
- full regression、branch coverage、静态检查、schema/readiness 与现实能力门。

按用户明确约束，未调用、启用或建议 Codex Security，也未使用任何 Codex Security 审计 Skill、插件或模块。

## 2. 专项验证

Occupancy 单元与集成测试：

```text
tests/unit/test_campaign_intent_occupancy.py
tests/integration/test_campaign_intent_occupancy.py
5 passed in 3.53s
```

Reduction 全链回归：

```text
occupancy + plan + preparation unit/integration
18 passed in 9.58s
```

分类结果：

```text
POSITION_RECONCILED initial -> CLEAR
PROTECTION_CONFIRMED initial -> CLEAR
INTENT_CREATED add -> BLOCKED
observed_intent_count=2
stable_intent_count=1
blocking intent quantity=0.1
```

组合故障：

```text
active Add=INTENT_CREATED
new current position protection=PROTECTION_MISSING
durable target=EXIT
Campaign state=CLOSING
plan result=CAMPAIGN_REDUCTION_INTENT_OCCUPIED
```

## 3. 数据库与现实边界

```text
schema=20260718_0037
migration change=none
occupancy persistence=none
OrderIntent creation delta=0
sender claim delta=0
live capability change=none
```

普通 resolver 的 occupancy 是 query-time 证据；WP-0054 preparation 在 Campaign/target 锁之后使用
`lock_intents=true`。真正 reduction OrderIntent 与发送前仍必须再次复核，当前 CLEAR 不可复用为 permit。

## 4. 全量质量门

### 4.1 全量功能回归

```text
385 passed, 1 warning in 91.10s
```

### 4.2 Branch coverage 门

```text
385 passed, 1 warning in 149.55s
Total coverage: 87.87%
Required test coverage of 85% reached
```

唯一 warning 为既有 Starlette/httpx 弃用提示，与本包无关。

### 4.3 格式、lint 与类型

```text
ruff format --check src tests migrations
155 files already formatted

ruff check src tests migrations
All checks passed!

mypy --strict src/trading_control_plane
Success: no issues found in 59 source files

git diff --check
passed
```

### 4.4 Schema、readiness 与现实能力门

```text
schema=20260718_0037
alembic check=No new upgrade operations detected
Database.is_ready()=(True, None)
AUTO_ADD=DISABLED
CAPITAL_TRANSFER=DISABLED
LIVE_ORDER_SEND=DISABLED
```

## 5. 结果判定

1. 已存在且可能改变仓位的意图不再与 reduction plan 并存而被忽略。
2. `RESULT_UNKNOWN`、部分成交和未完成 reconciliation 均失败关闭。
3. 历史稳定初仓不会永久阻塞保护失效退出路径。
4. 本包没有 reduction OrderIntent、claim 或发送能力，不构成实盘就绪证明。
