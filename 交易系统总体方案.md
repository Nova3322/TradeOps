# 交易系统总体方案

> 版本：V1.3 方案基线
> 日期：2026-07-14
> 当前执行范围：Binance USDⓈ-M Futures 与 Hyperliquid 的 U 本位永续合约；按加密货币、美股及股指、贵金属、商品及商品期货参考资产分配风险
> 扩展边界：核心合同保留未来 CFD、股票及其他衍生品平台的执行适配能力，但不承诺接入，也不把未认证平台计入当前范围
> 文档状态：可进入产品拆分、研究验证与开发规划；尚不代表任何现有代码已经满足本方案

---

## 一、文档定位

本文是交易系统未来开发与重构的总纲，解决系统边界、决策权、风险权、交易生命周期、盈利验证、人工审核与 AI 演进问题。

现有材料继续保留，但用途需要重新归类：

| 现有内容 | 未来定位 |
| --- | --- |
| 全市场趋势突破系统文档 | 目标策略与模块协作参考 |
| 低波动突破系统 | 独立策略插件；其持仓巡检、退出优先级、已完成 K 线趋势退出与 `reduce-only` 平仓作为参考，不把它误称为加仓或 OMS 实现 |
| DynamicPositionSizing | 目标杠杆区间再平衡与布林带回调过滤的迁移参考；保留数学思想，废弃轮询脚本、最近订单成本锚点和直接下单实现 |
| 仓位计算 Excel | 情景分析与研究附件，不作为运行时事实源 |
| Perptape / New project 2 | 市场监控、突破发现与公开行情来源 |
| RiskControl / NoTilt Vault | 长期资金储备与资金释放闸门 |
| 本文 | 冲突发生时的总体架构、权限与风险基线 |

本文不定义每个指标的精确公式，也不定义代码、接口和数据库细节。策略参数、交易所接口和技术实现应在后续专项文档中展开。

---

## 二、最终产品定义

系统定义为：

> 一套当前面向 Binance 与 Hyperliquid、覆盖已认证 U 本位永续合约，并为未来可选交易场所保留适配边界的跨资产趋势与人工交易假设风险管理系统。

它既可以通过市场监控发现趋势机会，也允许用户把看图形成的交易假设录入为人工提案；两类提案最终都由同一风控、授权、OMS、Freqtrade 和对账链路处理。系统按资产板块、趋势质量、流动性、持仓量、交易成本和相关性分配有限风险。

本文的“U 本位”指报价或抵押资产是经风险政策批准、与美元锚定的稳定资产，例如目标账户实际支持的 USDT 或 USDC；不包含币本位合约，也不假设所有稳定币永远等值 1 美元。

首期操作原则固定为：

- 系统提案和人工提案的初仓都必须由人工明确批准，并通过 Risk Engine 的确定性审核。
- 人工决定是否入场；程序在批准后成为受管账户的唯一订单发送者。
- 人工批准开仓时，同时决定该仓位是否允许自动加仓。
- 自动加仓只在原授权边界内运行，不逐次要求人工确认。
- 止损、减仓、回撤保护和紧急退出不等待人工确认。
- 后期 AI 可以逐步承担开仓审核，但不能绕过确定性风控。
- 长期目标不是“全自动赌方向”，而是“可验证正期望、可审计、可降级的半自动交易指挥系统”。

系统无法保证盈利。这里的“可盈利”只能理解为：

> 在扣除手续费、点差、滑点、资金费率、失败成交和异常损失后，策略在未参与调参的样本、实时影子环境和小额实盘中持续表现出正期望，并且风险处于预定边界内。

---

## 系统假设（待验证、可证伪）

本系统基于以下研究假设：

1. 市场存在趋势惯性。
2. 强势突破后，部分标的会进入持续趋势。
3. 趋势行情具有右偏收益特征：多数小亏，少数大赚。
4. 成交量和持仓量可以辅助判断趋势的参与度和有效性。
5. 单笔交易结果不可控，但组合风险敞口可以被系统化控制。
6. 浮盈加仓不是为了赌更大，而是在已有利润保护下释放更多风险预算。

这六条都是需要通过历史数据、时间外验证、影子盘和小额实盘持续检验的可证伪假设，不是市场定律或盈利保证。若净期望、收益分布、参与度指标的增量价值或风险控制效果未达到预先冻结的验收门，系统必须拒绝晋级或降级相关能力，而不是为假设寻找事后解释。

第 6 条中的“释放更多风险预算”只指保护价上移后，已有浮盈通过降低当前 `Open Heat` 释放原冻结授权内部的风险容量；它不自动提高风险档位、冻结 1R、单笔最大损失、加仓次数或组合上限。正浮盈可以使目标杠杆加仓重新满足实时预算，但不能产生新的未审核授权。

---

## 三、不可被绕过的系统原则

1. **信号和人工交易假设都不是订单。** 检测到突破或用户看图得出方向，只能生成提案；必须经过风险定仓、人工批准和发送前复核才能产生订单。
2. **交易所是订单与仓位的最终事实源。** 交易所派生的本地仓位投影可以通过对账重建；批准、订单意图、幂等身份、风险版本和状态迁移属于不可丢失的内部事实，不能指望从交易所恢复。
3. **风险引擎高于人工、AI、策略和界面。** 风控拒绝后，没有任何角色可以强制开仓。
4. **批准的是有限授权。** 人工或 AI 不能批准模糊的“买 BTC”，只能批准一份冻结、可过期的交易提案。
5. **开仓审核与通知渠道分离。** Web、Telegram 只是交互渠道，真正的授权主体是审批服务中的人工或 AI 身份。
6. **自动加仓只做盈利金字塔。** 默认关闭；启用后盈利里程碑只解锁候选，每个 Add 的数量由当时的目标杠杆差额计算，不使用固定的分级数量或风险百分比；实际加仓还必须同时满足回调过滤、预授权额度和实时风险门。不亏损补仓、不摊低成本。
7. **扩大授权上限必须重新批准。** 在原始批准包内消费已批准的加仓上限不属于扩权，但真正执行时仍要实时占用 Reserved Heat；收紧风险可以立即生效，系统只能自动降级，不能自动升级。
8. **状态不确定等于禁止扩大风险。** 数据、仓位、挂单或成交无法确认时，只允许对账、减仓和退出。
9. **一个交易所故障不能自动转单到另一个交易所。** 换交易所属于新的交易决策。
10. **Vault 可以供给运营资金，但不救仓。** RiskControl / NoTilt Vault 的合格资金可以按开关和折扣计入 Current Portfolio MTM Equity，也可以在独立资金政策下向空仓交易账户补充下一周期运营资金；它永远不能冒充交易所当前可用保证金，不能因浮亏、清算压力或活动仓位自动提款救援，也不能绕过损失停机门。
11. **退出能力独立于审核系统。** 人工、AI、Web 或 Telegram 故障不能阻断已有仓位的保护性退出。
12. **任何参数与模型变化必须有版本。** 不允许因几笔亏损临时调参后继续沿用原验证结论。
13. **资产板块必须先分类再用风险。** 加密货币、美股及股指、贵金属、商品及商品期货参考资产分别设置风险限额；未分类标的默认不得新增风险。
14. **受管账户只有 Trading 一条订单出口。** 无论提案来自系统还是人工，用户、Telegram、FreqUI、策略回调和其他脚本都不能绕过 Trading 直接向交易所发送日常订单。
15. **交易订单与资金划转分权。** Freqtrade 和 OMS 不执行交易所提款、Vault 释放、Bridge 或资金归集；自动与人工资金划转都只能由独立 Capital Transfer Orchestrator 消费专用资金授权，并使用与交易密钥隔离的凭证。
16. **身份标签不等于风控豁免。** 一个用户可以同时拥有多个职责标签，但每个动作仍须同时通过角色权限、属性范围、MFA、拒绝优先策略和 Risk Engine；任何标签都不能授予绕过风控、直接下单或自我扩权的能力。

---

## 四、三项目总体边界

### 4.1 Perptape：市场与机会发现层

Perptape 负责回答“市场发生了什么”：

- 获取 Binance 与 Hyperliquid 公开市场数据。
- 维护 K 线、突破、波动、成交额、持仓量等公开指标和数据健康状态。
- 输出候选机会、信号依据和 readiness。
- 目标上为网页和 Trading 提供受版本约束的快照与实时增量合同。

当前 Perptape 的完整市场流主要是网站内部接口，对外客户接口默认以突破提醒为主，不能在 Phase 0 数据合同冻结前把它当成稳定交易依赖。Trading 需要建立独立 Market Catalog，从 Binance、Hyperliquid Core 和所有可发现的 HIP-3 DEX 动态读取全部 U 本位永续合约，再按标的身份、板块、交易状态、报价资产、流动性、数据质量和执行认证形成可交易子集。Catalog 的发现范围不受实盘白名单限制；`approved` / `certified` 只决定标的能否成为 `eligible_to_trade`。这里的“全部”是发现与分类范围，不代表每个新上线合约或 DEX 自动获得真实交易权限。

Perptape 暂未覆盖的美股、股指、贵金属或商品类标的，仍可通过人工提案进入 Trading；但 Trading 必须从目标交易场所独立取得可成交价格、合约规则、交易时段、预言机和账户事实，不能因为是人工判断就跳过数据门。

Perptape 不负责：

- 保存交易所交易密钥。
- 读取私有账户仓位。
- 计算最终风险预算。
- 审批、下单、止损或资金划转。

Perptape 当前公开指标不能替代可执行盘口深度、完整资金成本和交易规则。信号进入 Trading 后，Trading 仍要基于目标交易所的最新价格、盘口、规则和账户状态重新验证。

### 4.2 Trading：决策、风险与执行核心

Trading 负责：

- 标的标准化与候选接入。
- 信号评分、排序和策略版本管理。
- 单标的、标准化底层资产、相关风险簇、资产板块、单交易所 / DEX 和全组合风险预算。
- 生成系统开仓提案，并接收用户录入的人工交易提案。
- 人工审核和未来 AI 审核。
- 仓位状态机、自动加仓、回撤保护和退出。
- OMS、执行意图、对账和审计。
- 管理 Binance 与 Hyperliquid 各自独立的 Freqtrade 执行实例。
- 管理与订单域隔离的保证金调整意图、资金目标和 Capital Transfer Orchestrator，但不直接持有 Vault owner 或交易所提款主凭证。

Trading 是本次重构的核心项目。

Freqtrade 是当前 Binance 与 Hyperliquid 的 Trading 下属执行引擎，不是新的决策中心。它负责把已经通过 Trading 风控和授权校验的目标仓位落到交易所，并回传订单、成交和运行状态；它不得自行放宽风险、决定开仓价值或绕过人工审核。Trading 的策略、风险、授权和目标仓位合同不依赖 Freqtrade 私有对象，以便未来在不改变核心语义的前提下挂接其他受认证执行适配器。

### 4.3 RiskControl：长期资金闸门

RiskControl 负责：

- 长期资金储备。
- 按权限、额度、等待期和 Panic Lock 控制资金释放。
- 记录链上资金操作。

RiskControl 不负责：

- 交易所仓位、订单和杠杆。
- 分钟级止损或自动加仓。
- 判断交易所是否盈利、空仓或需要补充运营资金。
- 为活动亏损仓位、清算压力或浮亏自动补充保证金。

当前 RiskControl 仍是测试环境与未完成验证的工作区，不是已经可用的生产资金闸门。Vault 释放资产与资产真正到达 Binance 或 Hyperliquid 是两个独立步骤；未来还要单独确定链、资产、收款地址、确认数、桥接或中转钱包和到账对账。Trading、OMS、AI 与交易热密钥均不得拥有 Vault owner 或 whitelist 释放权。当前 NoTilt Vault 的 Panic Lock 会限制资金释放，并阻断部分模式提升和全额退出请求；它仍不等于交易仓位 KILL_SWITCH，二者必须独立建模和告警。

资金预算和风险预算必须分离：

- Vault 决定多少长期资金受合约约束，以及其中多少具备可验证、可折扣的风险预算资格。
- Trading 同时维护交易所可执行权益、Current Portfolio MTM Equity 和冻结授权权益，并决定当前最多允许承担多少损失。
- 只有已经到账并完成交易所对账的资金才能成为可用保证金；把 Vault 纳入风险评估本身不等于授权提款或自动转账。自动归集和自动运营补充必须分别启用独立资金政策与授权。

---

## 五、系统层次与事实源

系统主流程为：

**系统候选信号或人工交易假设 → 标的与板块识别 → 风险定仓 → 冻结交易提案 → 人工批准 → 下单前 Risk Engine 复核 → Trading OMS → Freqtrade 执行实例 → Binance / Hyperliquid → 原生保护与成交对账 → 自动加减仓 → 自动退出与完整记账**

对应事实源：

| 事实 | 最终来源 |
| --- | --- |
| 市场扫描、突破候选、公开指标 | Perptape |
| 人工方向、入场区间、有效期与失效假设 | Web / PWA 中由用户创建的提案草案；冻结后由 Trading 保存 |
| 标的身份、资产板块和可交易资格 | Trading Market Catalog，依据交易所元数据与版本化分类政策 |
| 可成交价格、盘口、资金费率、交易规则 | 目标交易所实时数据 |
| 余额、仓位、挂单、成交、费用 | 目标交易所私有接口 |
| 内部预期状态与生命周期 | Trading 事件账本 |
| 执行请求、执行进度与执行器健康 | 对应 Freqtrade 实例，经 Trading 对账 |
| 策略、风险、审批与 AI 版本 | Trading 配置与审计记录 |
| 长期储备、Vault 权限、余额和资金释放 | RiskControl / NoTilt Vault 链上合约状态；与 Hyperliquid Vault / Subaccount 概念严格区分 |
| 通知送达状态 | Web / Telegram 通知层 |

重要约束：

- Binance BTC 永续与 Hyperliquid BTC 永续是两个不同的可交易标的。
- 两者可以在组合风险层合并计算 BTC 方向暴露，但不能隐式抵消、互相覆盖或跨所平仓。
- Hyperliquid 中间价可以用于观察和估值，但订单风险必须使用真实可成交条件复核。
- Freqtrade 的本地状态和数据库是执行镜像，不覆盖交易所的真实仓位、挂单和成交。
- Web、PWA、Telegram 和 FreqUI 都不能直接成为增险事实源或授权源。
- Redis、缓存和本地数据库不能覆盖交易所或链上事实。
- 审批、订单意图、风险预留、幂等身份和状态迁移必须持久保存；这些内部事实无法从交易所或链上完整重建。
- 同一传统资产在 Core 或不同 HIP-3 DEX 上出现时，也必须保留独立的交易场所、DEX、合约和预言机身份，不能只按显示名称合并。

---

## 六、策略体系

### 6.1 目标主策略

主策略是跨资产 U 本位永续合约的全市场趋势突破，同时允许人工交易假设使用同一仓位管理合同：

- 从全市场候选中选择趋势质量和可执行性最优的机会。
- 不追求高胜率，追求小亏可控、盈利奔跑和右偏收益分布。
- 系统提案使用流动性、成交量、持仓量、波动适配、资金成本和相关性进行排序；人工提案不伪造系统排名，但必须通过同样的绝对数据、成本和风险门。
- 先分配风险，再根据止损距离决定仓位。

### 6.2 策略插件隔离

不同策略必须有独立合同、版本和绩效：

- 人工交易假设：允许没有 Perptape 信号的人工入场，但必须选择已冻结的仓位管理模板；若假设不属于趋势突破，使用独立 `MANUAL_HYPOTHESIS` 策略所有者，不能借用系统趋势策略的正期望证据。
- 低波动突破：保持固定规则和不加仓，不与主策略混算收益证据；只把已完成 K 线趋势通道、退出优先级、存量仓位持续巡检和 `reduce-only` 平仓语义带入新的退出研究。
- 趋势风险预算：允许利润保护后的自动加仓。
- 目标杠杆区间再平衡：吸收 `DynamicPositionSizing` 的 `L_target / L_min / L_max`、目标数量、回调过滤和 `reduce-only` 减仓思想，作为趋势加减仓的数量控制器；它服从 30% / 50% / 100% 里程碑、1 / 2 / 3 次授权和 1R / Heat 上限，不能成为第二套策略所有者。
- Excel 几何模型：只用于压力和复利情景，不证明生产收益。

同一笔仓位只能有一个策略所有者。仓位所有权至少绑定交易所、账户或子账户、标的与策略。若交易所账户采用净仓位模式，不同策略不得在同一账户交易同一标的；应使用独立子账户，或由组合层明确禁止冲突。不同策略不能同时争夺、修改或解释同一仓位。

### 6.3 策略合同

每个可上线策略必须先冻结：

- 依赖上述哪一条系统假设、可观察证据、反证条件和降级门。
- 交易市场和支持标的。
- 信号形成时点和价格口径。
- 开仓、失效、止损、回撤和趋势退出规则。
- `Total Capital Snapshot_0` 口径、Trade Funding Envelope 百分比、1R 定义、仓位口径与风险单位。
- 是否允许加仓、最多次数和加仓条件。
- 加仓模式、目标杠杆区间、回调参考线 / 区间、浮亏减仓门、保证金模式与复合执行路径。
- 单标的、标准化底层资产、相关风险簇、资产板块、交易所 / DEX 执行域、Vault 贡献模式和全组合风险边界。
- 成本假设、异常处理和适用市场状态。
- 哪些参数可以日常调整，哪些变化必须重新验证。

---

## 七、风险语言与风险档位

### 7.1 杠杆不是完整风险

杠杆主要决定保证金使用效率和清算距离；真正的计划损失由仓位数量、止损距离、滑点和退出能力共同决定。

因此系统顺序必须是：

1. 决定这笔交易最多允许损失多少。
2. 根据止损和真实成本反推仓位。
3. 再检查杠杆、保证金、流动性和交易所档位是否允许。

档位杠杆是上限，不要求每笔交易用满。实际杠杆取风险仓位需求、档位上限、交易所限制和流动性容量中的最小约束。

### 7.2 首版 Trade Funding Envelope 与风险档位

每笔交易先冻结一个 **Trade Funding Envelope（交易资金信封）**，再在该资金信封内计算计划最大损失。两者不能混为一谈：资金信封描述这笔完整仓位生命周期最多可以占用多少已到账运营资金，1R 描述从入场到保护退出最坏允许损失多少。

在提案冻结时保存：

> `Total Capital Snapshot_0 = 数据健康时的 Current Portfolio MTM Equity`

> `FundingEnvelope_0 = Total Capital Snapshot_0 × trade_funding_pct`

> `1R_0 = Total Capital Snapshot_0 × 0.5%`

`trade_funding_pct = N%` 是可配置的每笔资金政策；1.5% 只是当前示例，正式默认 `N` 仍需在资金政策中冻结，不得硬编码。该 `N%` 是每笔完整交易从初仓到全部 Add 共用的资金占用上限，不是每次 Add 都重新获得一份资金，也不是最大亏损比例。草稿阶段可以显示估算；待审核提案版本冻结时才正式保存 `Total Capital Snapshot_0`、`FundingEnvelope_0` 和 `1R_0`。任何会改变这三者的重算都必须生成新版本并重新审核，不得在批准或发送前静默刷新。资金逻辑上来自已分配的交易所运营资金或 Vault 储备；若目标交易所尚未实际到账，必须在初仓发送前通过独立资金流程完成划转和对账，不能把在途资金当保证金，也不能持仓中再从 Vault 提款救仓。

首版已固定的风险档位为：

| 档位 | 最大有效杠杆 | 每笔资金信封参数 | 单笔计划最大损失 | 自动加仓默认状态 | 启用后的默认次数上限 |
| --- | ---: | ---: | ---: | ---: | --- |
| 低风险 | 3x | 总资金的 `N%`（1.5% 仅示例） | `1R_0 = 0.5%` | 关闭 | 1 次（Add-1） |
| 中风险 | 5x | 总资金的 `N%`（1.5% 仅示例） | `2R_0 = 1.0%` | 关闭 | 2 次（Add-1 / Add-2） |
| 高风险 | 10x | 总资金的 `N%`（1.5% 仅示例） | `3R_0 = 1.5%` | 关闭 | 3 次（Add-1 / Add-2 / Add-3） |

这里的 R 百分比是整笔交易在初仓、全部已成交 Add、费用、滑点和止损穿透合计后的最大计划损失，不是每一级各有一份 R。每份提案可以选择更低档位、更小资金信封或 0 到该档上限的加仓次数，但不能在持仓中自动提高。提高档位、提高 `trade_funding_pct`、提高冻结 R 或增加剩余次数都属于扩权；三次以上不在 V1 支持范围内。

风险定仓需要区分以下口径，扩展资本回撤另使用一个独立监控口径：

- **交易所保证金权益（Exchange Margin Equity）**：交易所按最新 Mark-to-Market 计算的当前权益，包含已实现和未实现盈亏，用于清算风险、实际保证金、可发送数量和账户回撤监控。
- **交易所风险权益（Exchange Risk Equity）**：用于交易所和账户级新增风险门的保守口径。已对账抵押品和已实现盈亏按可变现价值计入，未实现亏损全部扣减，未实现浮盈不用于扩大该层新增风险，并扣除预计费用、资金费、未决负债和提款 / 转出预留。
- **扩展风险权益（Extended Risk Equity）**：所有 Exchange Risk Equity 加风险政策允许计入的折扣后 Eligible Vault Equity，只作为初始授权、账户和跨场所风险的第二道保守上限；它不替代包含正负未实现盈亏的实时组合分母。
- **当前组合 MTM 权益（Current Portfolio MTM Equity）**：`Σ 已确认 Exchange Margin Equity + Eligible Vault Equity - 尚未在前两项扣除的其他组合负债`。交易所部分完整包含最新正负未实现盈亏；Eligible Vault Equity 是已经扣除 pending、源端转出预留、在途和费用后的净额，这些项目不得在公式尾部再扣一次。它是每次开仓、加仓和组合预算复核的实时分母。
- **冻结授权权益（Frozen Authorization Equity）**：提案冻结时的 `Total Capital Snapshot_0`。它用于固定 `FundingEnvelope_0`、`1R_0` 和本次授权上限，正浮盈或后续入金不能自动把它向上重置。
- **扩展资本 MTM 权益（Extended Capital MTM Equity）**：Exchange Margin Equity 加纳入监控范围的 Vault 保守 Mark-to-Market 价值，用于 C-DD 监控。Vault 纳入 / 移出及外部资金流要对高点做流量调整；数据不可验证时 C-DD 进入 `UNKNOWN` 并触发冻结，而不是把未知余额当成零损失或零资产。

当前组合 MTM 权益遵循：

- 已对账的现金抵押品、已实现盈亏和全部未实现盈亏均按最新可验证价值计入；不能只计浮亏而忽略浮盈，也不能采用交易所界面缓存值。
- 正浮盈可以提高当前 MTM 分母，但最终上限仍取冻结授权与当前预算中更严格者，所以它不能自动扩大冻结 1R、风险档位、资金信封或加仓次数；未实现亏损则会立即收紧当前预算。
- 手续费、预计资金费、未决负债、提现、转出预留和 Unknown 状态的最坏负担需要扣减或单独占用 Heat。
- USDT、USDC 等抵押品按可验证市场价值保守计价，不能永远假设等于 1 美元。
- 每个交易所和全组合分别维护保证金权益高点；外部入金、出金和跨所转账对相应高点做等额调整，不能被误判为盈利或回撤。
- 任一交易所权益无法可靠读取时，不从分母中静默剔除，而是冻结受影响范围的新增风险。

Eligible Vault Equity 只能来自经过认证的 RiskControl / NoTilt Vault，并在当前确认块上满足：余额、资产、链、Vault 身份和控制权可验证；所有 active whitelist 均属于批准控制集合，且不存在未批准的待激活 whitelist、权限扩张或名单变化；扣除 pending withdrawal、源端预留转出、在途、费用和其他负担；再采用保守价格、资产折扣、流动性折扣和时效折扣。它是唯一的 Vault 净贡献账本，Current Portfolio MTM Equity 和 Extended Risk Equity 只引用该净值，不各自重复扣减。出现未知 whitelist、待激活权限扩张、名单变化、Panic Lock、Full Exit pending、owner 变更、RPC 或价格分歧、未知资产或状态陈旧时，对应 Vault 贡献立即归零并要求重新审批。

无论是否计入 Vault，最终允许数量都必须取以下约束中的最小值：

1. 以冻结 `FundingEnvelope_0`、冻结档位 R 和授权次数计算的最高边界。
2. 以最新 Current Portfolio MTM Equity 重新计算的单标的、风险簇、板块、交易场所和组合当前预算。
3. 以 Extended Risk Equity 和目标交易所自身 Exchange Risk Equity 计算的保守账户 / 跨所上限。
4. 以交易所保证金权益、可用保证金和清算缓冲计算的可执行仓位。
5. 盘口深度、滑点和其他流动性上限。

因此链上资金能够影响组合预算，却不能让 Freqtrade 发送交易所没有保证金承载的仓位。

Current Portfolio MTM Equity 和其他风险权益都不包括：

- 未到账或未确认的转账。
- 无法对账的余额。
- 重复计入交易所、Vault、子账户或中转钱包的同一笔资金。

Vault 与交易所之间的资金迁移必须使用互斥状态防止双计：资金从 `VAULT_CONFIRMED` 进入 `TRANSFER_IN_FLIGHT` 后不再计入 Vault，只有交易所完成到账和对账确认后，才可分别进入 Exchange Margin Equity 和保守的 Exchange Risk Equity。转账在途不属于任一方可用保证金。

建议初始默认：

- 自动加仓默认关闭。
- 新策略和新标的先使用低风险。
- 中风险在影子盘和小额实盘验证后开放。
- 高风险永远不是全局默认，并长期保留人工开仓审核。

### 7.3 组合风险建议起点

首版建议使用以下上限作为研究起点：

- 所有持仓、在途增险订单和风险预留的合计开放风险不超过最新 Current Portfolio MTM Equity 的 2.5%；`EXCHANGE_ONLY` 下不计 Vault 贡献。
- 同一标准化底层资产或高度相关风险簇不超过 1.25%。
- 单一交易所计划风险不超过 1.5%。
- 同一底层资产在两所同方向持仓时合并计算。
- 两所反方向持仓不能按完全抵消处理。

这些数字不是永久参数；调整它们属于风险政策版本变更，提高风险上限必须重新验证和批准。若启用 Vault，组合比例使用包含合格 Vault 贡献的最新 Current Portfolio MTM Equity，但交易所级、保证金和清算约束仍只使用交易所事实。

所有标的还必须拥有一个版本化 `sector_id`，并同时受板块 Heat、板块 Stress Heat、单一底层资产和跨板块相关风险簇约束：

| 板块 | 范围 | 风险限额状态 |
| --- | --- | --- |
| 加密货币 | Binance 与 Hyperliquid 的加密资产永续 | 独立配置板块上限 |
| 美股及股指 | 股票、ETF 或股指参考永续 | 独立配置板块上限，并处理交易时段、跳空和公司行动 |
| 贵金属 | 黄金、白银等参考永续 | 独立配置板块上限，并处理预言机与现货/期货基准差异 |
| 商品及商品期货参考资产 | 能源、工业品、农产品等参考永续 | 独立配置板块上限，并处理合约基准、换月和异常结算 |
| 未分类 | 无法可靠识别的新增标的 | 上限为零，不允许增险 |

每个板块的正式数值属于风险与授权附件；未冻结前只能研究或影子展示。板块上限不能替代相关性约束，例如同一宏观风险同时影响股指、贵金属和加密资产时仍要进入共同压力簇。

“单仓”需要按经济意图定义，而不是按交易所界面定义：

- 交易仓位：一份批准计划在一个锁定交易所上的真实执行腿。
- 经济仓位：同一策略、同一标准化底层资产、同一方向的合并风险。
- 资产暴露：跨策略、跨交易所的同一底层资产及相关方向风险。

同一 BTC 交易不能通过拆成 Binance 与 Hyperliquid 两份提案绕过单仓或资产簇上限。

统一风险账本至少区分：

| 风险项目 | 含义 |
| --- | --- |
| Authorized Capacity | 人工批准的最高边界，不占用 Heat，也不保证未来一定能执行 |
| Open Heat | 已有真实仓位按保护价计算的计划风险 |
| Reserved Heat | 即将发送或已在途的增险意图按最坏成交占用的风险 |
| Unknown Heat | 结果未知订单按最坏可成交情况预留的风险 |
| Stress Heat | 计入止损穿透、滑点、流动性下降和未覆盖仓位后的压力风险 |
| Protected Profit | 已被有效保护的同仓浮盈，只作分析；其风险效果只能通过 Open Heat 体现一次 |

每次加仓前必须从最新账户事实重建 Current Portfolio MTM Equity，并在同一决策快照上分层计算最坏损失：

- **Trade Worst-Case Loss_after** 只包含本经济仓位的 Open / Reserved / Unknown Heat、从当前可成交价回吐到有效保护价的损失和本仓费用、资金费、滑点与止损穿透附加项；它只与本交易的冻结档位上限、动态档位上限和 Authorized Capacity 比较。
- **Scope Worst-Case Loss_after(scope)** 对单一底层资产、风险簇、板块、场所和全组合分别汇总该作用域内的全部经济仓位、Reserved / Unknown Heat 和对应成本 / 压力附加项；每个 scope 只与自己的当前预算比较。

同一风险在同一层只能记一次，但不得漏掉任何已有或未知敞口。本次加仓允许数量同时受原始提案冻结授权尚未消费的上限，以及按最新 Current Portfolio MTM Equity 重算后各 scope 剩余容量约束；不得把全组合损失与单笔 R 上限直接比较。正浮盈即使进入最新 MTM，也只可能在冻结授权内部恢复容量，不能自动扩大原批准；未实现亏损则立即缩小可执行量或阻断加仓。

提案获批不会长期占用组合 Heat；初仓和自动加仓在真正进入执行前必须占用 Reserved Heat，失败、撤销或完成对账后再迁移或释放，避免多个并发意图分别通过、合计却超限。

---

## 八、系统风险状态

加仓档位与系统风险状态是两个不同概念：

- 加仓档位：用户批准的低、中、高风险授权包。
- 系统风险状态：系统根据回撤、组合 Heat、数据和执行健康度施加的最高限制。

同一次风险事件未解除前，系统的自动状态迁移只能单向收紧：

| 系统状态 | 允许行为 |
| --- | --- |
| NORMAL | 可生成提案、开仓、自动加仓、减仓和退出 |
| NO_PYRAMID | 禁止加仓，其他动作仍需通过风控 |
| NO_NEW_POSITION | 禁止开新仓和加仓，只管理已有仓位 |
| REDUCE_ONLY | 只允许减仓、取消增险订单和退出 |
| KILL_SWITCH | 取消增险订单，确认保护状态，受控退出或由人工进入只保护 / 减仓 / 退出接管 |

建议的初始回撤触发顺序如下；系统分别计算交易账户 A-DD 和启用 Vault 后的总资本 C-DD，并采用两者要求中更严格的风险状态。A-DD 使用经外部资金流调整后的交易所 Mark-to-Market 权益高点；C-DD 使用经入金、出金、Vault 纳入 / 移出和转账调整后的扩展资本高点。这些同样只是研究默认值，不能在验证和冻结前作为生产阈值：

- 从权益高点回撤约 1.0%：NO_PYRAMID。
- 回撤约 1.5%：NO_NEW_POSITION。
- 回撤约 2.5%：REDUCE_ONLY。
- 回撤约 4.0%：KILL_SWITCH。

连续亏损、异常滑点、数据陈旧、交易所规则变化、账户对账失败和组合 Heat 超限也可以提前触发更严格状态。

KILL_SWITCH 不等于无条件向失效市场发送大额市价单。其首要动作是停止扩大风险、撤销增险订单、确认真实仓位和保护订单，再根据交易所健康度选择受控退出或人工接管。AI 无权选择人工接管；人工接管期间也只能维持保护、减仓或退出，不能借紧急状态增加风险。

人工接管必须在运行手册预设窗口内形成可审计的“继续保护、减仓或退出”决定；未形成决定时保持 KILL_SWITCH 或 REDUCE_ONLY，绝不自动恢复增险。

系统可以自动进入更严格状态。风险事件解除后，恢复必须满足对账、明确恢复阈值、滞回区间和冷却期，并由人工逐级解锁；不得在临界值附近自动来回切换。恢复事件需要单独记录，不能被解释为原风险事件中“自动放宽”。

---

## 九、系统提案、人工提案与有限授权

### 9.1 两种提案入口

系统同时支持两条入口，但从风险定仓开始完全汇合：

1. **系统提案（SYSTEM）**：Perptape 或策略扫描产生候选，Trading 完成排名、风险定仓和提案冻结，等待人工批准。
2. **人工提案（MANUAL）**：用户在 Web / PWA 把看图形成的交易假设录入；系统不评价“图看得对不对”，但必须核验标的、数据、合约、风险和执行能力，并计算允许的最大数量。

人工提案采用严格流程：

**人工草稿 → 自动风险预检与定仓 → 超额拒绝或形成可批准版本 → 用户明确批准 → 发送前 Risk Engine 复核 → Trading OMS / Freqtrade 执行**

用户填写的初仓数量只是“请求数量”。若超过风险系统核准数量，系统必须拒绝并展示安全上限，不能静默缩量后直接下单。“保存草稿”绝不等于“批准执行”；提案者能否审核自己的提案由独立自审政策决定，不能因为同一用户拥有多个标签就默认允许。批准后用户不再直接向交易所下单，程序成为唯一日常订单发送者。

### 9.2 审核对象

人工批准的是一份冻结交易提案，至少包含：

- 提案来源 `SYSTEM / MANUAL`、提案人、交易所、账户、标的、标准化底层资产、板块和方向。
- 策略与参数版本。
- 系统提案的信号与排名，或人工提案的交易假设与决策周期；两者都保存数据时间和市场健康状态。
- 方向、触发价、订单类型、委托价、最大滑点、`reduce_only`、`valid_from`、`valid_until / time-in-force`。
- 初始失效价、交易所原生保护要求和趋势失效条件。
- 用户请求数量、风险核准数量、实际预计杠杆、`L_target / L_min / L_max` 和档位杠杆上限。
- `Total Capital Snapshot_0`、`trade_funding_pct`、`FundingEnvelope_0`、冻结 1R 的货币金额、档位 R 倍数与最大风险；系统还要按最坏允许成交、费用、滑点和止损穿透复算实际风险。
- 开仓后单一底层资产、相关风险簇、板块、交易所或 DEX 和组合风险。
- 是否允许自动加仓。
- 加仓档位、最大次数、30% / 50% / 100% 触发阶梯版本、目标杠杆区间、回调条件和加仓失效条件；不保存分层固定手数或分层风险份额。
- 允许的价格偏离、滑点、初始订单执行有效期和仓位生命周期授权边界。

初仓意图的 `reduce_only` 必须为 `false`；自动减仓、止损和退出意图必须为 `true`。字段组合与交易所语义冲突时直接拒绝，不能靠执行器猜测。

### 9.3 授权语义

开仓批准同时签发两份相关但生命周期不同的权限：

1. 初始订单授权：只在短期执行窗口和允许价格偏离内有效；超时或偏离后必须重新生成提案。
2. 加仓授权包：如果用户已开启自动加仓，则在初仓真实成交后激活，并绑定同一交易所、标的、方向和仓位生命周期，直到仓位退出、用户撤销或触发失效条件。

这不是无限期自动交易许可。

每个策略合同还必须定义加仓授权包的最长持续时间。超过期限但仓位仍在时，已有保护与退出继续，剩余加仓额度失效。

以下情况使初始订单或剩余加仓授权失效：

- 初始订单超过时间或价格偏离边界。
- 交易所、账户、标的或方向改变。
- 策略结构、价格口径或风险放宽版本改变。
- 人工放宽、取消止损，或进行未授权仓位修改。
- 仓位、挂单、余额或成交无法对账。
- 系统进入 NO_PYRAMID：剩余加仓授权失效；未消费初仓授权仍须重新通过实时风控。
- 系统进入 NO_NEW_POSITION、REDUCE_ONLY 或 KILL_SWITCH：未消费的初仓和加仓授权全部失效。
- 仓位完全退出。

以下变化不应误伤合法授权，但必须实时重新复核：

- 按已批准计划收紧或上移止损。
- 正常市场数据刷新。
- 风险政策收紧；新限制立即覆盖原授权。

止损放宽、止损模型改变或风险政策放宽都属于扩大或改变风险，剩余加仓授权失效并重新审核。

### 9.4 升降级

以下行为扩大风险，必须重新人工审核：

- 低风险升为中风险或高风险。
- 中风险升为高风险。
- 持仓期间重新开启自动加仓。
- 增加剩余加仓次数。
- 提高最大损失、仓位或价格容忍范围。
- 更换交易所。

以下行为可以立即生效：

- 关闭自动加仓。
- 高风险降为中风险或低风险。
- 减少加仓次数。
- 收紧止损、仓位或风险上限。

系统永远只能自动收紧，不能自动扩大授权。开仓时已经明确批准的 Add 次数、目标杠杆区间、资金信封和最大损失不属于事后擅自扩权；它们是原始有限授权的一部分，但每次使用前仍要按最新 Current Portfolio MTM Equity 重新占用风险额度并通过全部硬风控。

### 9.5 多标签身份、RBAC 与 ABAC

用户与职责标签是多对多关系：一个用户可以拥有多个标签，同一标签也可以授予多个用户。标签只描述“可能执行哪类动作”，实际授权还要由 ABAC 按账户、交易场所、标的 / 板块、风险档位、动作、金额、时间、渠道、设备可信度和 MFA 状态缩小范围。

V1 基础标签与建议扩展标签如下：

| 标签 | 允许的核心动作 | 明确不允许 |
| --- | --- | --- |
| `OBSERVER`（观察者） | 查看被授权范围内的候选、提案、账户、仓位、告警和审计 | 创建、批准、下单、改风险、划转资金 |
| `PROPOSER`（提案者） | 创建、编辑未冻结草稿并提交系统或人工提案 | 把草稿直接变成订单；仅凭该标签批准提案 |
| `REVIEWER`（审核者） | 在属性范围内批准或拒绝一份已冻结、仍有效的开仓提案 | 修改被审核提案；覆盖 Risk Engine 拒绝 |
| `OPERATOR`（操作员，建议） | 关闭自动加仓、暂停新开仓、请求紧急减仓 / 退出和处理运行异常 | 新增风险、放宽止损、提高档位或划转资金 |
| `RISK_ADMIN`（风险管理员，建议） | 管理版本化风险政策、限额和人工恢复流程 | 直接下单、覆盖硬风控或仅凭该标签批准自己的扩权变更 |
| `TREASURY_ADMIN`（资金管理员，建议） | 在独立资金授权范围内提议、审核或执行 Vault / 交易所划转 | 发送交易订单、把在途资金记为保证金、救援活动仓位 |
| `SYSTEM_ADMIN`（系统管理员，建议） | 管理用户、标签、服务配置和身份基础设施 | 默认不获得提案审核、风险扩权、交易或资金权限 |

权限决策遵循“显式允许、默认拒绝、拒绝优先、最小权限”。只要角色、属性范围、系统风险状态、资金政策或 Risk Engine 中任一层拒绝，动作即被拒绝；多个标签不能通过权限相加绕过某一层的显式拒绝。所有 Web、PWA、Telegram 和未来 AI 身份使用同一授权服务，渠道不能改变权限结果。

自审不是 `PROPOSER + REVIEWER` 的自动推论。默认禁止提案者审核自己的提案；个人单用户部署若确有需要，只能通过版本化 `self_review_policy` 对指定账户、风险档位和动作显式开放，并要求强 MFA、完整风险摘要和不可抵赖审计。高风险扩权、风险政策放宽、身份授权变更和资金划转默认要求职责分离；同一身份不能同时完成其创建与最终批准。

批准 / 拒绝、标签授予 / 撤销、属性范围变化、MFA 结果、自审政策命中、权限拒绝原因和紧急操作都写入不可变审计链。角色变化只影响未来动作，不能追溯性地使历史批准合法，也不能复活已经失效的授权。

---

## 十、仓位生命周期

目标状态流转为：

**空仓 → 系统候选或人工草稿 → 风险预检 → 冻结提案 → 等待人工批准 → 开仓中 → 未保护成交 → 原生保护确认 → 基础仓 → 盈利里程碑 / 回调候选 → Add-1 → Add-2 → Add-3 → 动态去杠杆 / 趋势保护 → 退出 → 空仓**

低风险在 Add-1 后停止，中风险在 Add-2 后停止，高风险才有资格进入 Add-3；具体提案可以选择更少或完全关闭。任何未保护成交、执行结果未知或对账冲突都会进入覆盖性的冻结 / 异常状态，不会继续下一层。

同时存在两个覆盖状态：

- 冻结：禁止扩大风险，只允许对账、减仓和退出。
- 异常：事实不明，需要恢复或人工处理。

需要修正现有文档中的两个旧状态：

- “等待 Telegram 确认”改为“等待开仓审核”，审核渠道和审核主体作为元数据记录。
- 删除“等待加仓人工确认”。自动加仓的合法性来自开仓时的预授权与每次实时风控复核。

任何人工或 AI 审核完成后，下单前仍必须重新检查价格、仓位、组合风险和交易所健康度。旧批准不能在恢复或延迟后盲目执行。

---

## 十一、自动加仓逻辑

本章把 `DynamicPositionSizing` 的目标杠杆区间思想并入趋势仓位合同。目的有两个：在盈利趋势中按授权扩大仓位；在不利波动使有效杠杆过高时自动去杠杆。它不是亏损摊平，也不允许恢复杠杆成为绕过 1R 的理由。

自动加仓全局和单笔默认均为关闭。用户启用后，V1 默认盈利里程碑为：

- Add-1：仓位冻结收益率达到 30%。
- Add-2：仓位冻结收益率达到 50%。
- Add-3：仓位冻结收益率达到 100%。

30% / 50% / 100% 只解锁对应 Add 候选，不代表到达数字后立即追价下单。实际成交还要同时满足当前收益仍不低于该级门、目标杠杆区间产生正调整量、回调过滤、趋势延续、风险容量和执行安全。

为避免 3x / 5x / 10x、追加保证金和不同交易所 UI 口径造成歧义，本文把“仓位盈利百分比”正式定义为：

> `仓位冻结收益率 = 扣除预计平仓成本后的整仓未实现净收益 ÷ 初仓成交后冻结的初始保证金参考额`

分母在整个仓位生命周期不因加仓、转账或交易所 UI 改变。触发使用经执行附件冻结的 Mark / 可成交价格和费用模型，不能直接照搬交易所展示的 ROE。30% / 50% / 100% 是当前业务默认值；调整触发值或价格口径会产生新参数版本。这三个百分比只决定 Add-1 / Add-2 / Add-3 何时获得候选资格，不参与计算加仓数量。

目标杠杆区间使用以下统一语义：

- `E_campaign` 是该经济仓位按真实初仓、全部成交、费用、资金费、已实现和未实现盈亏重建的当前仓位权益；不能使用“最近一笔订单价格 × 当前总数量”重新虚构成本与权益。
- `L_effective = abs(Q_current) × P_mark / E_campaign`。
- 当 `L_effective < L_min` 时，只生成潜在加仓量 `Q_target - Q_current`；仍需通过对应盈利里程碑和回调加仓硬门。
- 当 `L_effective > L_max` 时，生成到 `Q_target` 的 `reduce-only` 去杠杆目标，不需要人工批准，也不消耗 Add 次数。
- 当有效杠杆位于冻结区间内时，不调仓。
- `Q_target = E_campaign × L_target / P_executable` 是每次 Add 的默认目标；`delta_Q_to_target = max(0, abs(Q_target) - abs(Q_current))`，方向沿用原仓。系统不为 Add-1 / Add-2 / Add-3 预设固定手数、初仓百分比或递减系数。
- `delta_Q_final` 取目标杠杆差额与实时风险、资金信封、保证金、交易规则和流动性所允许安全增量中的最小值。约束允许时默认加到当前冻结的 `L_target`；若风险或流动性只允许更小数量，则安全缩量并接受成交后杠杆低于目标，不为追赶目标绕过限制。
- 每份合同必须满足 `L_min < L_target < L_max ≤ 风险档位上限 ≤ 交易所实时上限`。旧配置中超过 3x / 5x / 10x 档位上限的区间不能原样迁移。

每次加仓必须同时满足：

- 仓位生命周期内的加仓授权允许自动加仓，且仍然有效。
- 当前仓位的冻结收益率达到该级触发值，并且扣除成本后仍有真实浮盈。
- `L_effective < L_min`，由真实仓位权益计算出的 `delta_Q` 为正。
- 当前价通过冻结的回调过滤：默认沿用 `DynamicPositionSizing` 的布林带参考线和偏差区间语义，但周期、upper / middle / lower 参考线、上下限和使用已完成 K 线的规则必须版本化。
- 原仓止损已经上移并得到交易所确认。
- 趋势结构仍有效，并出现新的延续确认。
- Risk Engine 已用最新 Current Portfolio MTM Equity 重算预算；加仓后的 Trade Worst-Case Loss 仍在冻结档位 R、动态档位和 Authorized Capacity 内，而底层资产、风险簇、板块、场所与组合的 Scope Worst-Case Loss 分别在各自当前上限内。
- 加仓后实际杠杆、保证金占用、清算缓冲和盘口容量均安全。
- SYSTEM 提案的排名仍合格，或 MANUAL 提案的交易假设与有效期仍成立；两者的流动性、滑点和资金成本都符合要求。
- 上一次订单已经成交并完成对账。
- 不存在未知挂单、数据陈旧、交易所异常或系统风险压制。

加仓数量只由当时的目标杠杆差额发起，并由资金信封、实时风险、保证金、交易规则和流动性安全上限向下裁剪；系统只预先控制 Add 次数，不预先规定每次加多少。

约束固定为：

- 只沿原方向加仓。
- 不允许亏损加仓。
- V1 系统最多三级；启用后低风险默认最多一级、中风险最多两级、高风险最多三级，单笔可以主动选择更少。
- 条件失败时本轮不下单并记录原因；只要该级尚未消费、提案仍有效且以后再次同时满足盈利、回调和风险条件，可以重新形成候选，但不能保留一张脱离实时复核的旧订单排队。
- 有效杠杆下降只是数量条件，不能单独触发加仓。
- Add-1、Add-2、Add-3 各自是独立、只能消费一次的授权单元。订单在确认零成交后撤销或失效，不消费该 Add；一旦出现任何非零真实成交，即使只是部分成交，也完整消费一次 Add，不再以同一级补足到目标杠杆。结果未知时先冻结该 Add 和后续增险，直到对账证明零成交或确认已消费；进程重启不能令同一级重复消费。

生产自动加仓启用前，策略合同还必须明确：

- 每一级精确触发、重新触发、机会失效和成交即消费条件。
- 初始资金信封、冻结档位 R、当前 Open / Reserved / Unknown Heat 和剩余授权次数如何分别记账。
- 手续费、滑点、跳空和资金费后的压力总损失门。
- 受保护浮盈如何通过有效保护价降低 Open Heat，并保证同一份利润不会被二次抵扣。
- 止损与清算价之间的最低压力缓冲。
- 部分成交期间如何保证新增仓位不存在无保护窗口。

这些规则未冻结时，系统只能显示“加仓候选”，不能自动执行。

加仓完成后必须重新确认最终仓位和保护订单覆盖全部真实仓位。任何步骤结果不明，立即停止后续加仓并进入冻结或只减仓状态。

### 11.1 “回调时继续加仓”的正式语义

本版不再把“回调所有盈利加仓”解释为回调后撤掉全部 Add 层。正式语义是：某一级盈利里程碑已经达到后，系统等待价格进入该合同冻结的回调区域；只有回调后的当前收益仍达到该级门、`L_effective < L_min`、趋势与风险仍有效时，才执行该级加仓。

回调默认参考 `DynamicPositionSizing` 的无状态布林带过滤：`deviation = (当前价 - 参考轨) / 参考轨`，当偏差进入冻结区间才合格。它不是“从盈利峰值回撤多少”的同义词。未来若增加 MFE / 高水位回撤条件，必须作为另一个命名清晰的过滤器独立验证，不能与布林带回调混用。

- 本轮回调不合格，不消费 Add 单元；后续行情重新满足全部条件时仍可候选。
- 已成交的 Add 单元不会因为回调自动撤销，也不会重新变成未消费。
- 浮亏或有效杠杆超过 `L_max` 时进入动态减仓；未消费的后续 Add 单元先暂停而非永久撤销。仓位恢复盈利后，只有仍在授权有效期内并重新满足对应里程碑与回调门，才可继续加仓。
- 硬止损、趋势退出、组合减仓或 KILL_SWITCH 永远高于回调加仓；同一轮同时出现增仓与减仓结论时，只执行更小的目标仓位。

### 11.2 `DynamicPositionSizing` 的迁移边界

核心只迁移两类策略思想：`L_target / L_min / L_max` 与基于真实仓位权益的目标数量数学，以及布林带参考线偏差作为继续加仓过滤器。多空参数、价格限制、最小数量、拆单、滑点和 `reduce-only` 仍是新系统需要的约束，但必须在 Trading / OMS 中重新定义、验证和实现，不视为搬运旧执行代码。

必须重构：最近订单价格成本锚点、轮询脚本直接下单、可用保证金乘 95% 决定加仓、任意挂单阻塞止损、无持久化状态、无次数控制、无幂等和错误后仍标记“已执行”。旧 Binance 文件实际使用币本位 / 全仓配置，旧 Hyperliquid 文件也没有真正的逐仓保证金调整；两者都不能作为当前 U 本位生产实现原样复制。

---

## 十二、止损、回撤与退出

人工决定是否建立风险，但风险退出不能等待人工。

系统至少需要三层退出：

1. **硬保护止损**：防止单笔损失突破授权预算，优先使用交易所原生保护能力。
2. **趋势退出**：趋势结构失效、反向突破或排名持续恶化时退出。
3. **动态去杠杆与回撤保护**：不利波动使 `L_effective > L_max` 时先以 `reduce-only` 减到目标仓位；独立冻结的硬回撤或组合风险条件可以要求更小仓位或退出。普通回调本身不是减仓指令。

原则：

- 加仓后不能为了容纳更大仓位而放宽止损。
- 保护订单缺失或数量不足时，停止一切新增风险并优先恢复保护；无法恢复则受控退出。
- 紧急退出路径不依赖 Perptape、Telegram、AI 或审批网页。
- 平仓后必须核对残余仓位、挂单、费用、资金费和实际 R 结果。

### 12.1 `low_vol_breakout_bn` 的参考边界

新系统参考该目录中已经存在的四项行为语义：

- 每轮先同步真实持仓和未完成订单；事实不明时禁止新增风险。
- 趋势退出只使用已完成 K 线形成通道：多头跌破最近 N 根已完成 K 线低点、空头突破最近 N 根已完成 K 线高点时退出。
- 固定损失、可选固定止盈和趋势退出存在确定性优先级，不允许多个模块竞争发送平仓单。
- 旧实现的全仓退出使用 `reduce-only`，且已有仓位不会因开仓白名单变化而失去管理；新系统把这一减险原则扩展到自动减仓与退出，但部分减仓是待新建并独立验收的能力。

但当前 `low_vol_breakout_bn` 没有浮盈加仓、高水位回撤止损、交易所原生保护、幂等订单或完整部分成交状态机；它还使用固定美元盈亏和“保证金 × 杠杆”定仓。因此新系统只重构上述退出理念到 Trading 的唯一退出仲裁器，不直接复用其风险定仓，也不让 Freqtrade ROI、跟踪止损或策略回调形成第二套退出逻辑。

生产退出优先采用冻结 1R 和初始失效价建立交易所原生保护。`low_vol_breakout_bn` 的固定美元止损可以作为兼容研究参数，但不能覆盖更严格的原生保护；可选固定止盈和趋势通道退出由 Trading 合并成唯一目标仓位。多种退出同时触发时，以更小的目标剩余仓位和更高的执行紧迫度为准。

---

## 十三、Binance 与 Hyperliquid

### 13.1 统一策略，独立故障域

两个交易所共享：

- 策略、排名和组合风险语言。
- 同一底层资产、板块与相关风险簇暴露计算。
- 审批、审计和绩效归因标准。

两个交易所必须独立：

- 账户、密钥、资金上限和权限。
- OMS 发送链路、订单日志和幂等空间。
- 健康状态、限速、熔断和恢复。
- 仓位、挂单、成交与对账。

Hyperliquid 还要把 validator-operated Core 和每个启用的 HIP-3 DEX 当成不同执行认证域。它们可以共享 Hyperliquid 基础设施或账户抵押品，却不能共享预言机、市场运营、流动性、费用、限速和故障结论。

故障需要区分：

- 仅某所公开行情故障：冻结该所候选；另一所可在组合事实完整时继续。
- 某所私有仓位、挂单、成交或余额未知：全局新增风险默认关闭，因为同一底层资产、板块和组合暴露也变成未知。
- 只有两个交易所预先硬隔离风险额度，并按故障所最坏可能暴露计入 Unknown Heat 后，健康交易所才可使用其独立剩余额度。

任何情况下，健康交易所都不能接管、补做或对冲故障交易所的未知订单。

### 13.2 保证金与杠杆

当前目标运行方式是 Binance U 本位优先使用全仓；若启用 Binance 逐仓，其业务语义与可移除保证金的逐仓模式一致，但每一步按 Binance 原生规则独立求值，不能套用 Hyperliquid 公式。Hyperliquid 必须逐标的读取真实模式，不能把所有 `onlyIsolated` 标的视为同一种能力：`noCross` 表示只能逐仓但允许移除保证金，`strictIsolated` 表示逐仓且活动仓位不能移除保证金，只会随减仓按比例释放。

需要动态检查：

- Binance 的交易对杠杆、保证金模式和名义价值档位。
- Hyperliquid 的资产最大杠杆、保证金模式和名义仓位档位。
- Hyperliquid Core 与每个 HIP-3 DEX 的抵押资产、预言机、市场状态、持仓上限和 DEX 级保证金事实。
- 实际清算距离与止损距离。

保证金能力归一化为三类：

| 能力类 | 适用示例 | 浮盈加仓 | 浮亏减仓 |
| --- | --- | --- | --- |
| `CROSS_SHARED` | Binance 全仓；Hyperliquid 允许且认证的全仓 | 未实现浮盈是否进入可用抵押品由真实账户抽象决定；在风险授权内直接执行单腿净增仓，不做单仓保证金转移 | `reduce-only` 到目标仓位；释放的抵押品回到对应共享池，不假定一定成为空闲余额 |
| `ISOLATED_REMOVABLE` | Binance 逐仓；Hyperliquid 普通逐仓或 `noCross` | 先按安全目标执行单腿净增仓；成交对账后，只有适配器依据场所原生事实计算、试算或请求确认的 `max_removable_margin` 仍满足冻结清算缓冲时，才可执行 excess-margin normalization | 先 `reduce-only` 到目标数量；成交对账后，将本次释放保证金中合同要求保留的部分回加到剩余仓，恢复冻结的目标保证金缓冲；不能增仓、放宽止损或调用 Vault |
| `STRICT_ISOLATED` | Hyperliquid `strictIsolated` | 活动仓位不能移出保证金，也不以“先减仓再重开”绕过限制；只有可用抵押品和清算缓冲足以支持经认证的单腿净增仓时才加，否则跳过 | 减仓按场所规则释放保证金；若该标的已认证 top-up 能力，则按同一合同把本次释放额中的冻结部分回加到剩余仓，否则继续保持更小仓位或退出 |

`unrealized_pnl`、适配器求得的 `max_removable_margin`、`released_margin_after_reduction` 与 `free_collateral_after_reconcile` 是四个不同事实，不能统称为“浮盈可提取”。`max_removable_margin` 可能来自原生字段、场所公式、只读试算或实际请求后的确认，不假定两所都直接返回同名字段。Hyperliquid 与 Binance 分别按各自原生规则求值并标记 `withdrawal_rule = VENUE_NATIVE`，不能把一个场所的公式套到另一个场所。

用户要求的“浮盈加仓”在系统中按经济结果实现：把经风险授权、场所确认为可用的盈利能力转化为净新增仓位；不会为了复刻界面动作而默认先减仓、实现盈亏再重开。用户要求的“浮亏减仓后把释放保证金重新添加到剩余仓位”登记为 `POST_REDUCTION_MARGIN_NORMALIZATION`：它在启用逐仓 DynamicPositionSizing 时随开仓合同一并预授权，只能使用本次减仓释放且已对账的场内保证金，净仓位不得增加，止损不得放宽，也不得触发 Vault 或外部入金。

订单与保证金 normalization 如果同时存在，必须属于一个可恢复的复合意图：冻结原仓、目标仓位、目标保证金缓冲和最坏滑点；对同一账户的订单、保证金动作和释放额预留使用 fencing 串行化；先执行并对账净仓位变化，把实际释放额从共享可用池预留给该意图，再在保证金腿发送前复核费用、其他仓位、挂单和可用抵押品。保证金腿失败时保持较小风险终态或继续减仓，不能反向开仓追赶目标。交易订单仍只由 Freqtrade 执行；保证金增加 / 移除由 Trading 下属、逐场所认证的 Margin Controller 调用官方能力。任何一方状态未知都冻结增险。

每份执行证书还必须冻结 `account_abstraction`、`collateral_scope` 和 `collateral_pool_id`。Hyperliquid 全仓抵押品的共享范围随 Standard、Unified 或 Portfolio Margin 账户抽象变化；账户抽象、抵押池、保证金模式或能力快照变化时，证书立即失效并进入 `NO_NEW_RISK`。

选择了 10x 档位并不意味着交易所一定允许或系统一定使用 10x。若计划不满足交易所或风险边界，系统必须阻止提案并说明原因，不能静默替换规则。

### 13.3 交易所选择

提案生成时可根据流动性、点差、资金费率、可用容量和健康状态推荐交易所。

一旦人工批准，执行交易所即被锁定。批准后自动换所、拆到另一所或用另一所补救都属于新决策，必须重新生成提案。

### 13.4 跨资产合约的额外硬门

U 本位支持宇宙包含非加密底层资产，但这些仍是交易所提供的永续衍生品，不等同于直接持有股票、指数、贵金属现货或传统商品期货。每个标的进入真实交易前还必须冻结：

- 底层资产、报价与抵押资产、合约乘数、交易状态和价格精度。
- 目标账户、账户模式与所在司法辖区是否实际允许访问该产品；不可用时只展示，不生成可执行提案。
- 主要交易时段、休市与节假日、盘前盘后、周末、跳空和流动性真空规则。
- 预言机来源、更新频率、陈旧门、异常价与交易所暂停机制。
- 股票拆并股、分红、指数成分或计算方法变化。
- 商品参考基准、传统期货换月、负价格或异常结算如何映射到永续合约。
- 单标的和所属 DEX 的持仓上限、杠杆档位、费用、资金费与容量。

任一字段未知时，该标的可以被发现和展示，但 `eligible_to_trade = false`。

### 13.5 为未来交易场所保留的适配边界

首期只实现 Binance 与 Hyperliquid，不为尚未决定的 CFD、股票或其他衍生品平台提前编写业务分支。但 Trading 核心只依赖版本化 `Venue Capability Contract`，至少描述：

- 标的身份、交易时段、报价 / 抵押 / 结算资产、价格源和公司行动。
- 合约乘数、tick / lot、最小数量 / 名义价值、到期、换月与现金 / 实物结算语义。
- 账户、子账户、净仓 / 双向仓、全仓 / 逐仓、`collateral_scope / collateral_pool_id` 及保证金调整能力。
- 市价 / 限价 / 条件单、`reduce-only` 等价语义、原生保护、TIF、幂等身份和结果查询。
- 余额、仓位、挂单、成交、费用、融资 / 隔夜费和强平事实源。
- 限速、停牌、休市、拒单、部分成交、断线和人工接管行为。

当前分别使用 `BinanceVenueAdapter` 与 `HyperliquidVenueAdapter`；两者的 Order Execution Port 以 Freqtrade 为后端，Margin / Collateral Port 由场所原生 Margin Controller 实现。未来 `CFDBrokerVenueAdapter`、`EquityBrokerVenueAdapter` 可以使用 SDK、FIX 或其他受认证驱动，但不能复制出另一套策略、Risk Engine 或授权中心。不能满足唯一发送者、幂等、保护或对账硬门的平台只能做只读数据源或保持不接入。

入金、提款、Vault 释放、Bridge 和内部资金划转不属于可写 VenueAdapter；它们由独立 `CapitalTransferAdapter` 和 Capital Transfer Orchestrator 使用隔离凭证处理，避免扩大订单执行密钥的权限。

---

## 十四、Freqtrade 执行、OMS 与对账原则

当前采用 Freqtrade 作为 Binance 与 Hyperliquid 的底层订单执行引擎。Trading 仍然拥有策略、风险、授权、目标仓位、复合意图和组合账本；Freqtrade 只负责在对应交易所执行已经通过校验的交易订单并回传状态，Margin Controller 只执行已批准的场内保证金调整。提案来自系统还是人工、未来是否增加其他 VenueAdapter，都不会改变这条边界。

执行分层为：

1. Trading 生成系统提案，或把人工草稿经过风险定仓后冻结为提案和目标仓位。
2. 人工或未来 AI 只在允许范围内签发授权。
3. Risk Engine 在发送前重新校验并占用风险额度。
4. Trading OMS 产生唯一、不可复用的执行意图。
5. 对应交易所的 Freqtrade 实例执行订单；复合逐仓流程由同一 Trading 意图协调 Margin Controller。
6. Trading 使用交易所私有事实完成对账和风险迁移。

Freqtrade 不得：

- 根据自身界面、内置 Telegram 或外部直接请求绕过 Trading 开仓。
- 自行扩大仓位、风险档位、加仓次数、价格容忍或杠杆。
- 把本地“已发送”或“已创建交易”当成交易所已经成交。
- 在结果未知时自行重复提交同一增险意图。
- 自动切换到另一交易所执行。

Trading OMS 与 Freqtrade 的共同原则：

- 以目标仓位与交易所真实仓位之差作为执行对象。
- 每个交易意图只有一个不可复用的身份，避免重复下单。
- 发送前再次检查精度、最小名义价值、风险和提案有效性。
- 处理部分成交、撤单失败、订单超时和结果未知。
- 结果未知时主动查询，不盲目重试。
- 真实成交后更新状态，不以“请求已发送”假定成功。
- 加仓前后都确认保护订单能覆盖真实仓位。
- 用户请求数量超过风险核准数量时由 Trading 拒绝；Freqtrade 不替 Trading 静默缩量、放宽价格或重新解释最大风险。

部署与账户边界：

- Freqtrade 的一个 bot / config 只冻结一种 `margin_mode`，并假设自己是该杠杆账户的唯一交易者；不能依靠同一实例按标的在全仓与逐仓之间动态切换。
- 首期 Binance 全仓使用独立账户或子账户与独立 Freqtrade worker；Hyperliquid 逐仓使用另一独立 subaccount 与 worker，并逐标的区分 normal、`noCross`、`strictIsolated` 的保证金能力。
- 若以后启用 Binance 逐仓或 Hyperliquid 全仓，分别新增专用账户 / 子账户、margin-mode worker 与执行证书，不和现有 worker 共用受管账户。
- Hyperliquid Core 与每个批准的 HIP-3 DEX 按独立执行域配置、限速、审计和认证；不能共享幂等空间或混淆订单归属。
- 每个账户只允许一个活动订单发送者，Freqtrade 实例必须假设自己独占其受管交易账户。
- 不在同一受管账户中进行绕过系统的人工交易；若人工直接处理紧急仓位，系统立即冻结该账户并重新对账。
- Freqtrade 版本、CCXT 版本和交易所执行附件共同冻结；升级后先重放、影子和小额验证。
- Freqtrade REST、WebSocket、FreqUI 和内置 Telegram 不直接暴露为用户交易入口，只允许 Trading 后端受控访问。
- 如果 Freqtrade 的默认订单语义超过提案允许的滑点、价格或保护边界，则该动作被阻断，而不是接受执行器默认值。
- 系统风险状态持久保存在 Trading；Freqtrade 重启后默认只对账，在 Trading 重新下发当前状态前不得恢复增险。
- Freqtrade 自主开仓、仓位加码和与 Trading 竞争的退出逻辑保持关闭；它只消费 Trading 签发的一次性意图。
- 受管账户中的日常订单只允许来自这一执行链。用户所谓“人工寻找点位、人工决定入场”发生在提案层，不是直接去交易所手工下单。

任何进程重启都先进入对账模式：

1. 读取交易所真实仓位、挂单和最近成交。
2. 与事件账本和预期状态比较。
3. 解决未知订单和残余仓位。
4. 恢复保护订单。
5. 完成后才允许新增风险。

灾难恢复不得简单重放历史订单请求。

采用 Freqtrade 不改变事实源：余额、真实仓位、真实挂单和真实成交仍以交易所私有接口为最终来源；Trading 事件账本保存授权和意图，Freqtrade 状态用于执行协调和差异发现。

---

## 十五、正期望证据与策略晋级

### 15.1 盈利证据链

系统只能通过以下链路建立正期望证据：

**冻结策略合同 → 验收数据 → 含成本回测 → 时间外验证 → 历史事件重放 → 实时影子盘 → 模拟盘 → 小额实盘 → 分级扩容 → 持续监测**

任何阶段失败都应退回上一阶段，而不是现场修改参数掩盖问题。

### 15.2 数据要求

研究数据至少覆盖：

- 历史时点可交易标的，包括下架、改名和流动性衰退资产。
- OHLCV、Mark / Last、盘口、持仓量、资金费率和交易规则。
- K 线完成状态、缺口、重复、乱序和修复记录。
- Binance 与 Hyperliquid 各自真实合约规格。
- 每个标的的板块、标准化底层资产、Core / HIP-3 DEX、报价与抵押资产映射。
- 非加密底层资产的交易时段、节假日、预言机、公司行动、指数方法、商品基准与换月事件。

不能接受：

- 使用当前标的清单回测过去。
- 把未完成 K 线当成已知数据。
- 只保留最优参数试验。
- 用屏幕价格假设大额订单全部成交。
- 忽略手续费、点差、滑点和资金费率。
- 把休市或预言机停更误判成低波动，把公司行动或传统期货换月误判成趋势信号。

### 15.3 防过拟合与独立验证

每个策略必须登记全部研究试验，不能只保存最终最优结果。验证至少遵循：

- 按时间顺序拆分研究集、多个 walk-forward 验证窗口和最终冻结测试集。
- 最终测试集在策略与参数冻结前不得查看；一旦用于调参，就不再是最终测试集。
- 参数选择采用稳定区域，而不是单点最优；关键参数在合理邻域扰动后不能立即从有效变成严重失效。
- 复杂度增加必须在多个未参与调参的窗口产生稳定改进。
- 市场状态最初用于归因；只有经过独立验证后才能升级为过滤条件。

每个策略合同还要在测试开始前写入一份可机械判定的验收附录，明确：

- 时间外窗口数量和最终测试集边界。
- 最低有效交易样本或最短观察周期，取更严格者。
- 净 R 期望及其统计区间要求。
- 基准、1.5 倍和 2 倍成本各自的通过条件。
- 策略证据回撤 S-DD、尾部损失和收益集中度上限。
- 组合政策回放 P-DD 的状态触发正确性与执行穿透。
- 实盘账户回撤 A-DD 与可选总资本回撤 C-DD 的全局风险状态门。
- 实盘滑点、费用、资金费与研究模型的允许偏差。
- 每一级扩容所需样本、运行时间和市场状态覆盖。

总纲不为所有策略使用同一个数字，但没有这份数值化附录的策略不能晋级。

四类回撤不能混用：

- S-DD 使用策略完整 Mark-to-Market 影子净值，证明策略本身的回撤和恢复特征。
- P-DD 回放 Heat 与系统状态，只证明风控动作正确，不证明策略具有优势。
- A-DD 使用外部资金流调整后的交易所账户保证金权益高点，是交易账户生存与清算风险门，无论 Vault 多少都不能放宽。
- C-DD 只在 `EXCHANGE_PLUS_VAULT` 启用时计算，使用经外部资金流和范围变化调整后的 Extended Capital MTM Equity 高点，反映交易所与纳入范围 Vault 资金的总资本 Mark-to-Market 回撤；它是回撤时间序列，不替代每次决策使用的 Current Portfolio MTM Equity，也不与保守 Extended Risk Equity 混用。

A-DD 和 C-DD 都可以要求全局进入 NO_PYRAMID、NO_NEW_POSITION、REDUCE_ONLY 或 KILL_SWITCH，系统采用更严格者；C-DD 永远不能抵消或放宽 A-DD。

### 15.4 成本与压力

回测和模拟至少包含：

- Maker / Taker 手续费。
- 点差和基于订单规模的滑点。
- 部分成交、撤单和重报成本。
- 资金费率与持仓跨越的结算时点。
- 极端行情中止损穿透和流动性下降。

结果至少比较基准成本、1.5 倍成本和 2 倍成本。何谓通过或结构性失效必须在策略验收附录中预先量化，不能在看到结果后解释。

### 15.5 晋级门

| 阶段 | 核心验收 |
| --- | --- |
| 研究合同 | 规则无歧义，策略与参数版本冻结 |
| 数据验收 | 关键数据完整、时间一致、缺口可追踪、相同输入可重复 |
| 时间外验证 | 扣除成本后达到预先登记的净期望、统计区间和稳定性门 |
| 历史重放 | 部分成交、断线、未知订单、止损和恢复可重现 |
| 影子 / 模拟盘 | 信号、风险、审核、OMS 和对账长期没有状态冲突 |
| 小额实盘 | 约目标风险的 10%，真实成本与模型处于容差内 |
| 分级扩容 | 约 25% → 50% → 100%，每一级重新评估 |

正式扩容前至少满足：

- 时间外每 R 净期望为正。
- 2 倍成本压力下达到预先登记的压力门。
- S-DD 在策略合同的预设预算内，P-DD 风控动作正确，A-DD 未越过交易账户政策；启用 Vault 时 C-DD 也未越过扩展资本政策。
- 收益集中度、最大单笔贡献、单标的和板块集中度没有超过预设上限。
- 结果不依赖单一趋势阶段。
- 实盘滑点、费用和资金费率处于预先登记的容差内。
- 无重复订单、无保护仓位和长期对账分歧。

趋势策略天然可能由少数大趋势贡献主要收益，因此不强制“删除最大盈利后仍然盈利”。系统应报告收益集中度，并验证在主要趋势缺失、延迟入场和成本上升情景下，风险与回撤仍可接受。

### 15.6 上线后盈利归因

每笔交易的结果拆分为：

- 信号选择贡献。
- 排名与组合分配贡献。
- 初始仓贡献。
- 第一次、第二次、第三次加仓贡献。
- 止损、趋势退出和回撤保护贡献。
- 手续费、滑点和资金费率。
- 人工或 AI 审核贡献。
- Binance 与 Hyperliquid 执行差异。
- 系统提案与人工提案贡献，以及四个资产板块的独立表现。

只看总 PnL 无法说明系统为什么盈利或亏损，也无法判断加仓和 AI 是否真正增加价值。

策略的晋级与扩容依据是证据持续成立，不是累计赚了多少钱。短期盈利可能来自运气。

---

## 十六、人工审核向 AI 审核演进

### 16.1 AI 的正确角色

AI 不应成为可以自由改仓位的“第二个交易员”。它是一个受政策、权限、适用范围和审计约束的审核主体。

确定性风控始终位于 AI 之前和之后：

**安全与数据门 → Risk Engine → 策略资格 → 人工或 AI 审核 → 下单前实时复核**

AI 只能批准 Risk Engine 已允许的提案，不能：

- 修改交易所、方向、止损、仓位、杠杆或加仓授权。
- 改写风险政策或策略参数。
- 绕过数据陈旧、组合超限或账户异常。
- 持有交易所密钥、钱包私钥或资金划转权限。
- 自动提高风险档位。
- 从 Vault 释放资金。

如果 AI 认为提案需要修改，只能拒绝或退回重新生成。

AI 只能接收一份标准化、最小化并冻结的输入合同：

- 唯一交易提案及数据来源、时间与完整性状态。
- Risk Engine 的确定性检查结果和当前组合风险。
- 策略、参数、风险政策、模型和审核提示版本。
- 允许披露的市场、流动性、成本与账户健康摘要。

API Key、钱包私钥、提款能力和任意账户权限不得进入模型。新闻与社交内容如未来接入，只能作为隔离的不可信证据，并保留来源与提示注入防护，不能改变系统政策。

### 16.2 审核成熟度

| 阶段 | AI 权限 | 目标 |
| --- | --- | --- |
| 阶段 0：纯人工 | 无 AI 决策 | 建立唯一提案、人工理由与结果数据 |
| 阶段 1：AI 影子 | 不展示、不影响交易 | 独立判断批准、拒绝或放弃，验证能力 |
| 阶段 2：AI 辅助 | 给人工建议，人工仍批准 | 补充反证、风险和不确定性，缩短审核时间 |
| 阶段 3：有限代理 | 首轮只批准已认证、自动加仓关闭的低风险标准提案 | 由审批服务在明确白名单内签发短期单次授权 |
| 阶段 4：条件扩展 | 低风险稳定后，单独认证部分中风险 | 人工负责例外和高风险 |

高风险 10x 建议长期保持人工审核，不能因为低风险 AI 表现良好而自动继承权限。

### 16.3 AI 审核输出

影子和辅助阶段，AI 对冻结快照只能输出建议，没有授权效力：

- 建议批准。
- 建议拒绝。
- 主动放弃并转人工。
- 支持交易的主要证据。
- 反对交易的主要证据。
- 数据缺口、不确定性与失效条件。
- 经过校准的审核置信度。

必须分开显示：

- 数据可信度。
- 策略适用程度。
- AI 审核置信度。

AI 置信度不是盈利概率，也不能用一个高综合分掩盖数据缺失。

有限代理阶段，AI 仍然不签名、不下单。确定性审批服务在再次验证白名单、模型版本和硬风控后，才把“建议批准”转换成短期、单次授权。AI 拒绝或放弃时，若所有硬条件仍合格，提案进入人工队列；人工可以拒绝或撤销，但不能反向绕过硬风控。

### 16.4 硬阻断与转人工

以下情况属于硬阻断或提案失效，人工同样无权批准：

- 数据缺失、陈旧、互相矛盾或价格越过提案边界。
- Binance / Hyperliquid 仓位、订单或余额无法对账。
- 未知挂单、保护订单异常或 Risk Engine 拒绝。
- 系统处于 NO_NEW_POSITION、REDUCE_ONLY 或 KILL_SWITCH。NO_PYRAMID 只禁止加仓；它仍可允许 Risk Engine 单独批准“自动加仓关闭”的新仓。
- 策略、风险政策或关键价格口径不再匹配原提案。

只有在数据和所有硬风控合格时，以下情况才转人工：

- 高风险 10x、风险升级或超出 AI 认证范围。
- 新标的、新策略、新市场状态或异常行情。
- AI 模型超时、版本不明、输出不规范或低置信。
- 外部新闻、社交文本不可验证或疑似提示注入。

正确放弃是 AI 的必要能力，不是失败。

### 16.5 AI 晋级证据

AI 不能只根据“和人工意见一致”或“最近赚了钱”晋级。

必须评估：

- 硬风控遵守率和政策逃逸次数。
- 高置信错误、拒答质量和置信度校准。
- 人工与 AI 分歧的原因。
- AI 批准组相对匹配人工组的净 R 期望；匹配必须控制提案来源、策略版本、风险档位、交易所 / DEX、方向、底层资产、板块和市场状态，避免选择偏差。
- 最大回撤、尾部损失和错误批准。
- 错误拒绝造成的机会成本。
- 不同交易所、方向和市场状态下的稳定性。

一笔合规好交易可以亏损，一笔违规坏交易也可能侥幸盈利。因此不能把单笔 PnL 直接当成 AI 标签。

模型、供应商、审核提示、风险政策或关键数据源发生变化，都视为新版本，重新经历历史回放、影子和小额验证。禁止根据近期盈亏在线自我调参。

### 16.6 回滚

- AI 纯可用性中断：新提案自动回到人工审核，不默认批准；已签发的短期授权只有在审批服务确认代理版本仍有效、没有完整性风险且尚在原有效期内时才可继续。
- 模型漂移、高置信错误上升、版本完整性异常或其他可信度降级：立即退回影子模式，停止签发新 AI 授权，撤销未消费授权，撤销或对账在途增险单，并冻结 AI 批准仓位的剩余加仓额度转人工复核。
- OMS 最终复核不仅检查授权内容和有效期，还要确认签发该授权的 AI 代理版本仍处于有效授权状态。
- AI 回滚不能影响已有止损、减仓和退出。
- 从 AI 有限代理恢复必须重新通过认证，而不是手动打开开关。

---

## 十七、产品形态

### 17.1 Web / PWA 是主控制台

构建一套自有 Web 主控制台并支持 PWA，使电脑和手机共享同一账户、风险状态和操作语义。它是唯一完整汇总视图和主操作入口，但本身不是事实源；事实仍分别来自交易所、Trading 内部账本、Freqtrade 执行实例和链上合约。

网页负责：

- Perptape 候选、排序、组合风险、Freqtrade 实例和交易所健康状态。
- 系统提案、人工提案创建、人工审核和未来 AI 审核。
- 当前仓位、加仓授权、保护状态和退出状态。
- 订单、成交、异常、审计与绩效归因。
- 自动加仓开关与低 / 中 / 高风险档位设置。
- 四个资产板块的已用 / Reserved / Unknown / Stress Heat、板块上限和相关风险簇占用。
- 最新 Current Portfolio MTM Equity、未实现盈亏、冻结授权基准、各级当前预算，以及 `EXCHANGE_ONLY / EXCHANGE_PLUS_VAULT` 风险资本模式、Vault 折扣后贡献、数据新鲜度和状态；启用或提高 Vault 贡献必须走独立风险政策审批，不是单笔提案里的便捷开关。
- 用户、Observer / Proposer / Reviewer 等多标签、属性范围、自审政策、MFA 状态和权限审计；标签管理与业务批准使用不同权限。
- 暂停新开仓、关闭自动加仓、只减仓、紧急减仓和异常处置。

人工提案页应支持把图表判断结构化为：

- 交易场所 / 账户 / 标的 / 板块、方向和决策周期。
- 触发价、订单类型、委托价、最大滑点、数量、`reduce-only` 和有效期。
- 初始失效价、`Total Capital Snapshot_0`、资金信封 `N%` / 金额、冻结 1R / 最大风险和风险档位。
- 自动加仓开关、低 1 / 中 2 / 高 3 的次数预览、30% / 50% / 100% 候选触发、`L_target / L_min / L_max` 与回调条件；明确显示加仓数量将在每次执行时按目标杠杆差额计算。
- 用户请求数量、风险系统核准上限、拒绝原因和交易后的板块 / 组合风险预览。

开仓审核页应优先展示：

- 为什么值得交易，以及最主要反证。
- 数据是否新鲜、完整和可成交。
- 入场、止损、资金占用、计划最大损失、冻结 R 和实际预计杠杆，并把“资金占用不等于最大亏损”直接展示在摘要中。
- 自动加仓开关、风险档位、目标杠杆区间和最大次数。
- 交易前后的最新 Current Portfolio MTM Equity、未实现盈亏，以及单一底层资产、风险簇、板块、交易所 / DEX 和组合最坏损失。
- 授权有效期和失效条件。
- 当前用户标签、审核作用域、是否命中自审政策和 MFA 要求。
- AI 建议、置信度和不确定性；AI 启用后展示。

任何“调整”都会生成新提案，不能在已批准提案上静默改值。

Web / PWA 只能向 Trading 提交意图。它不能直连 Freqtrade 执行开仓，也不能把 FreqUI 当成生产主控制台。FreqUI 可以保留为受限的内部诊断工具，但不能拥有独立于 Trading 的操作权限。

PWA 离线或连接不稳定时只能展示带明确时间戳的最后快照，不能离线批准、恢复交易或假定紧急操作已经成功。电脑、手机和 Telegram 上的同一动作必须汇合到 Trading 的同一状态与操作回执，避免多端重复执行。

### 17.2 Telegram 是辅助入口

Telegram 负责：

- 接收开仓提案、成交、止损、加仓、风险预警和系统异常。
- 批准或拒绝一份仍然有效的冻结开仓提案。
- 关闭某仓位或全局的自动加仓。
- 暂停新开仓。
- 提交紧急减仓请求。
- 跳转至完整 Web 审核页。

Telegram 不允许：

- 任意输入标的、方向、仓位或杠杆直接开仓；完整人工提案只能在 Web / PWA 创建并经过风险定仓。
- 放宽止损、提高风险档位、增加加仓次数或恢复新开仓。
- 直接调用 Freqtrade 的开仓、强制入场或配置重载能力。
- 绕过 Trading 的身份、授权、风险、幂等和实时账户校验。

用户面对的是 Trading Telegram Bot，而不是 Freqtrade 内置 Telegram 控制面。每个按钮或命令都先变成 Trading 中的一次受限操作意图；通过风控后，才由对应 Freqtrade 实例执行。拒绝开仓、关闭加仓和暂停新开仓可以立即收紧；恢复交易或扩大风险必须回到 Web / PWA 完成更强审核。

Telegram 批准或拒绝开仓属于首期能力，但批准者必须具有作用域匹配的 `REVIEWER` 标签；关闭加仓、暂停和紧急减仓也分别校验 `OPERATOR` 或更窄的显式权限。Telegram 只能作用于一份已经冻结、信息完整且仍有效的提案；需要修改风险档位、加仓包或其他参数时必须回到 Web / PWA 生成新提案。按钮必须绑定唯一提案、身份、标签与 ABAC 决策、自审政策结果、有效期和必要的强 MFA，重复或延迟点击无效。

---

## 十八、故障降级与灾难恢复

| 故障 | 系统行为 |
| --- | --- |
| Perptape 数据陈旧或不可用 | 禁止新仓和加仓；已有仓位依赖交易所数据与原生保护管理 |
| 单所公开行情异常 | 冻结该所候选；组合事实完整时不必自动冻结健康交易所 |
| 单所私有仓位、订单或余额未知 | 全局新增风险默认关闭；只有预先隔离额度并计入最坏 Unknown Heat 时才允许健康交易所继续 |
| 仓位、挂单或成交状态未知 | 冻结相关标的，主动对账，不盲目重试 |
| Risk Engine 不可用 | 关闭所有新增风险，减仓和退出继续 |
| 仅 AI 模型不可用 | 新开仓回到人工审核；经授权服务验证的既有人工加仓包可按硬规则继续 |
| 审批服务或授权记录不可验证 | 禁止开仓和加仓，只允许保护、减仓和退出 |
| Web 或 Telegram 不可用 | 通知和人工开仓受限；止损、减仓和退出继续 |
| 单个 Freqtrade 实例不可用 | 冻结该实例的新仓与加仓；确认交易所原生保护并对账，健康实例不得接管未知订单 |
| HIP-3 DEX、预言机或市场运营异常 | 冻结该 DEX 或标的的新仓和加仓；其他域只有在抵押品、Unknown Heat 与风险限额可独立确认时才继续 |
| Freqtrade 与交易所状态不一致 | 以交易所私有事实为准，进入冻结或只减仓并修复账本差异 |
| Freqtrade / CCXT 升级或交易所语义变化 | 受影响执行认证失效，重放与影子验证后再恢复 |
| 数据库或审计不可用 | 禁止新增风险，只保留受控减仓与恢复路径 |
| Redis 丢失 | 重建市场缓存，完成预热前不产生新交易 |
| Vault、链上 RPC、价格、owner 或 whitelist 控制权异常 | Eligible Vault Equity 立即按政策降级或归零并重算风险容量；Extended Capital MTM Equity / C-DD 无法可靠计算时进入 `UNKNOWN`；先关闭新增风险和自动加仓，若已知风险门要求则受控减仓；已有仓位仍按交易所保证金和退出合同管理，不能自动提款救仓 |
| 保护订单缺失 | 立即禁止加仓，优先修复；无法修复则受控退出 |
| 系统时钟、鉴权或交易规则异常 | 停止对应适配器，重新验证后恢复 |

恢复原则：

- 所有重启先进入只对账模式。
- 可从交易所重建当前仓位、当前挂单和 API 可查询窗口内的成交；完整成交历史、批准、订单意图、风险预留和状态迁移必须从 Trading 持久账本恢复。
- 可从链上重建 Vault 余额、权限和请求。
- 策略版本、风险版本、授权、订单意图和状态迁移必须有持久备份。
- 恢复后所有旧提案重新验证，不能直接执行。
- REDUCE_ONLY 和 KILL_SWITCH 恢复需要人工解锁。
- 每个交易所同时只能有一个活动订单发送者，并具备防止双主的隔离机制。
- 审批、订单意图、风险账本和审计记录不能只保存在 Redis，必须保存到与主库、主机不同故障域的持久备份；上线运行手册必须定义可接受数据损失、恢复时间和恢复后重新开放增险的条件。
- 系统整体不可用时，人工只能在交易所直接维持保护、减仓或退出；系统恢复后强制冻结并对账，不能继续沿用原预期状态。

必须演练的场景包括：

- 订单发送后未收到响应。
- 部分成交后进程退出。
- 单所和双所中断。
- 数据库与 Redis 丢失。
- 审批渠道和 AI 中断。
- 密钥撤销、RPC 中断和系统时钟偏移。
- 两个执行实例同时尝试成为活动发送者。
- Web、Telegram 或 FreqUI 绕过 Trading 直接触发执行。
- Freqtrade 重启后恢复出旧开仓或旧加仓机会。

---

## 十九、资金与安全治理

### 19.1 资金分层

- RiskControl / NoTilt Vault：长期储备；默认不参与风险分母，显式启用后只以折扣后 Eligible Vault Equity 同时参与 Current Portfolio MTM Equity 和保守 Extended Risk Equity。
- 交易所运营资金：每个场所只保留资金政策分配的 `n%` 目标，用于正常保证金、费用和退出。
- 每笔 Trade Funding Envelope：按提案冻结时总资金的可配置 `N%` 形成，1.5% 仅为示例，正式默认 `N` 属于待冻结资金政策；它是完整交易生命周期共享的资金占用上限，不是最大损失，也不是新的 Vault 余额池。
- 已实现利润：完成成交和对账后，才进入利润回收决策。
- 浮盈：不是可从 Vault 再融资的理由。

资金配置使用独立资本口径，不复用风险权益：

> `Managed Settled Capital = 已确认交易所资本 + 已确认且纳入资金政策的 Vault 资本`

在途、未知、待确认、已预留转出和无法可靠估值的金额不进入该口径。每个交易场所按一个冻结的资金配置周期保存 `venue_operating_target_pct = n%`、目标金额 `T_v`、下沿 `T_low`、上沿 `T_high` 和最低退出 / 费用储备；所有场所目标之和不能超过资金政策允许分配的资本。目标在一个配置周期内固定，不能因为刚产生利润或亏损就循环改变分母。

场所运营资金目标 `n%` 与单笔 `trade_funding_pct = N%` 是两个不同参数：前者决定场所平时保留多少结算资金，后者决定一笔交易最多占用其中多少。Risk Engine 必须证明所有活动与 Reserved 资金信封合计不超过该场所已确认运营资本。已有运营资本足够时只做逻辑预留；不足时，Vault → 交易所划转必须在账户空仓、初仓发送之前完成并对账，随后重新核验价格、有效期、Current Portfolio MTM Equity 和风险预算。在途或预期到账永远不能满足初仓保证金门。

### 19.2 自动利润归集与运营资金补充

系统需要支持两条彼此独立、均可暂停的自动政策：

1. **`AUTO_PROFIT_SWEEP`**：交易所真实空仓时，把已实现且完成对账、超过 `T_high` 的可提款利润归集到指定 RiskControl / NoTilt Vault。
2. **`AUTO_OPERATING_REFILL`**：可选且默认关闭；交易所结算资本低于 `T_low` 时，从 Vault 补充到冻结目标，供下一轮交易或尚未发送初仓的已批准资金信封使用。

自动利润归集必须同时满足：

- 该交易所及其受管子账户真实仓位为零。
- 无未完成、未知、条件、保护或可能恢复成交的订单。
- 所有费用、资金费、已实现盈亏、内部划转和前序提款已经对账。
- 提款资产、网络、Token、目标 Vault、费用和最低到账额处于认证白名单。
- Vault 当前无阻止正常资金治理的未知 owner / whitelist 变化；Panic Lock、Full Exit pending 或目标资产异常时不自动归集。
- 归集后仍保留 `T_v`、提款费、最低余额和异常缓冲。

建议归集额取以下约束的最小值：已对账可用利润、交易所结算资本超过 `T_high` 的部分、交易所单次 / 日限额、资金政策单次 / 日限额和目标 Vault 可接受额度。浮盈永远不触发交易所提款。

自动运营补充只用于下一轮运营，不用于救援当前仓位。它必须同时满足：账户空仓、无挂单和 Unknown 状态；A-DD、日周损失门和 KILL_SWITCH 允许重新开始；已过冷却期；未超过单次、日、周、月、连续补充次数和 Vault 最低保留额；Vault 处于允许正常释放的状态，Panic Lock、Full Exit、owner / whitelist 变更、请求等待期、执行窗口、权限或控制权均无异常。补充金额取 `T_v - 当前已确认交易所资本`、待执行资金信封缺口、合约实时可释放净额、各周期上限和目标交易所可接收额度中的安全最小值；`maxReleaseNet` 一类金额查询不能替代完整状态与权限校验。即使划转由某笔提案触发，资金授权与交易授权仍是两个身份、两个状态机。

若账户仍有仓位或接近清算，自动和人工 Vault → 交易所划转都被拒绝；本系统不提供以 Vault 救援活动仓位的 `MANUAL_CAPITAL_INJECTION` 例外。用户若在系统外注入外部资金，Trading 立即冻结新增风险、标记外部资本流并强制重新对账，不能据此重置亏损、回撤或停机计数。否则会形成“亏完—补充—继续亏损”的无限资金泄漏循环。

自动归集和自动补充分别启用、分别限额、分别冷却和分别一键关闭。功能在上线认证前均保持关闭；人工双向划转始终通过同一资金提案、授权、执行和对账流程，不允许人工模式绕过地址、空仓、额度或状态硬门。

### 19.3 Capital Transfer Orchestrator 与完整状态机

Freqtrade、交易 OMS 和策略不处理提款。独立 Capital Transfer Orchestrator 只消费 `Capital Transfer Authorization`，并把自动与人工划转统一为：

**`SOURCE_CONFIRMED → SOURCE_RESERVED_OUT → WITHDRAW_SUBMITTED → TRANSFER_IN_FLIGHT → DESTINATION_CONFIRMING → DESTINATION_CONFIRMED → SETTLED`**

交易所到 Vault：

1. 资金政策生成自动或人工提案，Risk Engine / Treasury Policy 校验空仓、利润、目标余额、地址、资产、网络、费用和限额。
2. 授权后立即从交易所风险权益扣除转出预留；CTO 策略把提款限制到冻结 Vault 地址、资产、网络和额度，交易所支持地址白名单或子权限时同时启用硬限制，不能把软件策略误称为交易所一定提供的密钥权限。
3. 保存交易所 withdrawal ID、链、Token 地址、金额、费用和 tx hash；广播后金额进入在途。
4. 达到冻结确认深度后，以 Vault 实际余额增量和链上交易确认到账；交易所直接 ERC20 Transfer 可能没有 Vault 自身 Deposit 事件，不能只依赖应用事件。
5. 只有资产、价格、Vault、控制权和时效资格均合格后才进入 Eligible Vault Equity；费用与到账差额单独记账。

Vault 到交易所：

1. 授权时先从 Vault 源端资金账本和 Managed Settled Capital 扣除 gross 预留；若该 Vault 已计入风险资本，则在唯一 Eligible Vault Equity 账本中只扣减一次，Current Portfolio MTM Equity 和 Extended Risk Equity 随后同源重算，不得各扣一次。`EXCHANGE_ONLY` 下 Eligible Vault Equity 为零也不能阻断合法资金划转或导致在途双计。
2. 人工路径可以使用 owner direct release；自动路径原则上使用预先批准的专用 whitelist funding wallet，不能把 Vault owner 私钥交给自动服务。当前合约对 requester、等待期和全局释放额度的限制，不等于逐 funding wallet 的资产、网络、地址与单授权额度硬限制；这些先由 CTO / 授权策略控制，若要求不可绕过则需受限签名器、独立 Vault / 钱包或额外合约约束。
3. whitelist 路径必须真实经历请求、等待、执行、资金到 funding wallet，再按认证网络充值交易所；当前合约不能自行判断空仓、跨链、Bridge、Swap、memo 或交易所到账。
4. 释放后资金进入中转 / 链上 / 交易所充值在途，不再计入 Vault；只有交易所私有接口确认可用余额后才进入 Exchange Risk Equity。
5. `netAmount`、协议费、Gas、Bridge / 中转费和实际到账差额分别记账。

任何金额同一时刻只能属于源端、在途或目的端之一。所有中转继续沿用同一个 `capital_transfer_id`，关联资金授权、Vault request、交易所 withdrawal / deposit ID、tx hash、源端扣减、目的端到账、费用、确认高度和时间。失败或未知不能直接回到可用状态；必须先证明源端未扣、已退回或目的端已到账。

资金划转属于外部资本流，要等额调整 Exchange Margin Equity、A-DD 和 Extended Capital MTM Equity 的高点；实际费用仍属于真实损失。Capital Transfer Orchestrator 故障只能冻结新划转，不能阻断已有仓位止损、减仓和退出。

Vault 纳入风险评估采用两种明确模式：

- `EXCHANGE_ONLY`：默认模式，只展示链上储备，风险贡献为零。
- `EXCHANGE_PLUS_VAULT`：经过风险政策审批后，按 Vault 身份、确认余额、pending 锁定、资产价格、流动性、控制权和时效折扣计入，并设置独立贡献上限。

启用、提高计入比例、提高资产折扣或扩大合格链 / 资产都属于风险扩权，只能在 Web / PWA 的风险政策流程中完成；Telegram、单笔提案和 AI 无权修改。Vault 贡献下降会自动收紧风险，贡献恢复不会自动恢复开仓或加仓。

RiskControl / NoTilt Vault 与 Hyperliquid 自身的 Vault / Subaccount 是两个不同概念，界面、账本和告警必须使用完整名称。资金接入先从只读评估与人工小额划转认证开始，再分别晋级自动归集和自动运营补充；无论自动还是人工，每次划转都需要独立 `Capital Transfer Authorization`，不得复用交易提案或自动加仓授权。

当前 RiskControl 实现是每位用户独立 Vault、按地址 / 链 / 资产保存真实余额，不是 FundShare 或“份额 × NAV”模型。Trading 必须逐 Vault 读取链上确认余额并扣除 pending locked deduction、预留转出和费用；前端展示的静态 USD 估值不能作为风险价格源。合约允许某资产存入也不等于该资产自动具有可靠价格、流动性或风险贡献资格。

### 19.4 权限最小化

- Perptape 没有交易凭证。
- 策略、排名和 AI 没有下单凭证。
- Risk Engine 没有交易和提款凭证。
- OMS 每个交易所使用独立、仅交易、禁止提款的凭证。
- Capital Transfer Orchestrator 使用与交易密钥分离的提款 / funding wallet 凭证，按冻结地址、资产、网络和额度执行，不能下交易订单；软件限制与场所 / 合约硬限制分别标记，未具备硬限制时以独立钱包、受限签名器、低额度和链上监控补强，不能虚构密钥本身已不可绕过。
- 紧急执行服务只允许发送 reduce-only 或平仓意图，不允许开仓、加仓或提款。若交易所无法在密钥层提供对应硬限制，必须通过独立账户、独立服务边界和验收证明其不能经其他入口扩大仓位，不能把应用规则误称为交易所权限。
- Vault 资金释放与交易操作角色分离；`TREASURY_ADMIN` 的提议、批准和执行权限按金额与账户继续细分，创建者与最终批准者默认不得为同一身份。

### 19.5 当前实施前安全门

当前 trading 目录即使建立本文档提交，也仍未形成可开发、可发布的代码与秘密治理基线；工作区还存在未跟踪的敏感凭据、日志和钱包材料。任何开发、测试或部署开始前必须：

- 轮换所有可能暴露的 API Key、私钥、助记词和 Bot 凭据。
- 把秘密移出仓库、源码和日志。
- 建立明确的版本基线和忽略规则。
- 禁止提现权限进入自动交易服务。
- 将生产、测试和研究账户完全分离。

RiskControl 当前工作区存在未提交合约修改，不能在未完成 ABI、测试、文档和安全审查前作为稳定集成基线。

Perptape 当前本地 main 位于 8319612，并比 origin/main 落后 9 个提交；在冻结数据合同前必须选择、更新并验证一个明确提交，不能直接把当前目录称为集成基线。

RiskControl 当前为 detached HEAD 7214fa5，存在 4 个未提交合约文件；现有缓存中记录了 5 项历史失败，本次未重跑。本方案只复用其资金闸门理念，不把当前工作区视为生产能力。

---

## 二十、审计与运营指标

每个交易提案应永久记录：

- 原始数据时间、来源和健康状态。
- 策略、参数、风险政策和审核版本。
- 信号、排名、反证和市场状态。
- 交易所、方向、入场、止损、`Total Capital Snapshot_0`、资金信封、档位 R、风险和授权边界。
- 每次风险复核使用的 Current Portfolio MTM Equity、未实现盈亏、Open / Reserved / Unknown Heat，以及加仓后 Trade / 各层 Scope Worst-Case Loss。
- 人工或 AI 的批准、拒绝、放弃和原因，以及用户标签、ABAC 作用域、MFA 与自审政策结果。
- 下单前复核结果。
- 订单、部分成交、重试、费用和资金费。
- 每次自动加仓与原始授权的关联、目标杠杆差额、安全缩量原因、实际成交和 Add 消费状态。
- 退出结果、最终 R 收益与模型偏差。

运营指标分为四组：

| 维度 | 重点 |
| --- | --- |
| 策略 | 净 R 期望、收益分布、Profit Factor、回撤、恢复时间、市场状态分解 |
| 执行 | 滑点、延迟、成交率、部分成交、重复订单、对账时间、保护覆盖 |
| 风险 | Current Portfolio MTM Equity、未实现盈亏、Portfolio Heat、相关暴露、单所风险、风险状态、越界和紧急事件 |
| 权限 | 标签与属性变更、允许 / 拒绝、自审、MFA、职责分离和高权限操作 |
| 资金 | Trade Funding Envelope、场所运营目标、划转在途、费用、归集和补充 |
| AI | 拒答率、置信度校准、高置信错误、人工分歧、相对人工增益和漂移 |

日常复盘关注异常、保护和执行；周期复盘关注策略证据、参数稳定性和是否需要降级。不能因为短期亏损在生产环境随意调参。

---

## 二十一、开发与上线顺序

| 阶段 | 范围 | 晋级条件 |
| --- | --- | --- |
| 0. 基线与安全 | 固定三项目边界、轮换凭据、建立版本基线、冻结策略合同 | 无秘密进入代码或日志，职责和事实源无冲突 |
| 1. 只读提案 | Perptape 系统候选与 Web 人工草稿进入 Trading，生成风险提案但不下单 | 两类来源可区分，数据质量、字段、去重、版本、板块和风险解释可审计 |
| 2. Freqtrade 影子执行 | 两所分别建立执行实例，读取真实账户、模拟订单生命周期和重启对账 | 无重复意图、未知状态可恢复、保护逻辑可重放 |
| 3. Web / PWA 人工审核 | 完整开仓审核、多标签 RBAC / ABAC、Trading Telegram 通知与受限操作、模拟盘自动退出 | 授权不可复用、会过期；拒绝优先、MFA、自审和职责分离可验证，任何界面都无法绕过风控直达 Freqtrade |
| 4. Binance 基础小额实盘 | 独立 Freqtrade 实例、低风险、少标的、人工开仓，自动加仓关闭 | 真实成本、原生止损、退出、部分成交、对账和故障演练通过 |
| 5. 自动加减仓独立晋级 | 对每个 `venue × account × execution domain × margin topology` 重复：加仓关闭基线 → 动态去杠杆 → 低风险 Add-1 → 中风险 Add-2 → 高风险 Add-3 → 保证金 normalization | 30/50/100 盈利里程碑、加到 `L_target` 的差额、安全缩量、成交即消费、最新 MTM 组合预算、场内保证金预留、原生保护和压力损失门分别通过 |
| 6. Hyperliquid 小额实盘 | 独立 Freqtrade worker、专用 subaccount；Core 与每个 HIP-3 DEX 单独认证，首轮加仓与 normalization 均关闭 | 不依赖跨所接管，订单、预言机、滑点、保护、清算、抵押品和限速边界验证；之后按阶段 5 独立晋级 |
| 7. 统一板块与组合风险 | 四板块、两所和 HIP-3 执行域按包含未实现盈亏的最新 Current Portfolio MTM Equity 集中计算风险，执行仍隔离 | 冻结授权与当前预算取严格者；单标的、风险簇、板块、单所和组合降级长期稳定 |
| 8. AI 影子与辅助 | 收集人工审核数据，AI 影子后进入辅助 | 置信度、拒答、分歧和风控遵守率达标 |
| 9. AI 有限代理 | 首轮只开放自动加仓关闭的低风险标准提案 | 无政策逃逸，可立即回滚，结果不劣于匹配人工基线 |
| 10. RiskControl 资金接入 | 只读展示 → 人工小额双向划转 → 初仓前资金信封到账 → 自动利润归集 → 可选自动运营补充；风险计入与划转开关分开晋级 | 资金占用与最大损失分离，无双计、无假价格、空仓 / 订单硬门和完整在途状态通过；Vault 不救活动仓，链上故障不影响交易所退出 |
| 11. 可选新场所认证 | 仅在有明确业务需求时新增 CFD、股票或其他衍生品 VenueAdapter；先以加仓和 normalization 关闭的小额基线开始 | 核心提案与风险合同不变；新平台的订单、保护、保证金、结算和资金适配逐项通过，再完整重走阶段 5，不满足则保持只读或不接入 |

AI 批准开仓并同时预授权一次自动加仓，必须在低风险纯开仓代理稳定后作为独立晋级门。浮盈自动加仓只在基础开仓、止损、退出、OMS 对账和重启恢复稳定之后开放。

FundPool、FundShare、管理费和公开募资不属于当前交易模块首期范围，应在个人交易系统长期稳定后独立立项。

---

## 二十二、关键验收标准

### 数据与信号

- 真实交易链路不允许 mock 或 mixed 数据进入。
- 数据来源、价格口径、完成状态、时间和缺口可追踪。
- 重复、迟到和修复事件不会生成重复交易意图。
- Perptape 异常被识别为未知或不可用，不能被误解为无信号。

### 风险与审批

- 人工或 AI 不能绕过 Risk Engine。
- 每次开仓对应唯一、冻结、会过期的授权。
- SYSTEM 与 MANUAL 提案均先经过风险定仓；人工请求数量超限时拒绝，不能静默缩量后执行。
- 每笔提案冻结 `FundingEnvelope_0 = Total Capital Snapshot_0 × N%` 和 `1R_0 = Total Capital Snapshot_0 × 0.5%`；低 / 中 / 高最大损失分别严格为 1R / 2R / 3R，资金占用不会被误当成允许亏损。
- 每次自动加仓都能追溯到原始开仓授权；30% / 50% / 100% 只解锁候选，数量默认按当时 `E_campaign` 与 `P_executable` 加到 `L_target`，不存在分层固定手数或风险份额。
- 风险或流动性限制会把 Add 安全缩小；确认零成交不消费次数，任何非零真实成交完整消费一次，部分成交、结果未知和重启不会导致同一级补单或重复消费。
- 每次加仓前使用包含最新未实现盈亏的 Current Portfolio MTM Equity 重算预算；本仓 Trade Worst-Case Loss 取冻结授权和动态档位中更严格者，底层资产、风险簇、板块、场所与组合的 Scope Worst-Case Loss 分别不得突破各自预算。
- 扩大授权上限必须重新人工批准；在原授权包内消费已批准加仓上限不属于扩权，但每次执行仍需实时占用 Reserved Heat。
- 同一次风险事件内自动状态只能收紧；风险解除后只能经过对账、冷却和人工逐级解锁。

### 身份与权限

- 一个用户可以同时拥有 Observer、Proposer、Reviewer 等多个标签，但多标签不产生 Risk Engine 豁免或隐式自审权。
- RBAC 与 ABAC 同时生效，默认拒绝且拒绝优先；账户、场所、板块、档位、动作、金额、渠道和 MFA 范围均可限制权限。
- 提案、审核、操作、风险、资金和系统管理权限可独立授予；高风险扩权、资金划转和权限变更满足冻结的职责分离政策。
- Web、PWA 与 Telegram 对同一身份得到相同权限结果；标签变更、MFA、自审与全部允许 / 拒绝决定可审计。

### 执行与恢复

- 交易所私有数据是余额、仓位和订单最终事实。
- 处理部分成交、结果未知、重复请求和交易所规则变化。
- 重启先对账，不重复下单。
- 任何真实仓位都具有已确认的保护或明确的受控退出状态。
- 单所故障不污染另一所，也不自动跨所补单。
- Web、PWA、Telegram、FreqUI 和 Freqtrade API 都不能绕过 Trading Risk Engine 直接增险。
- Binance 与 Hyperliquid 的 Freqtrade 实例、账户状态和执行认证互相独立。
- Hyperliquid Core 与每个启用的 HIP-3 DEX 分别认证，非加密标的必须通过交易时段、预言机和板块风险门。

### 资金划转

- 交易资金信封只能由已确认的场所运营资金承载；若需从 Vault 提取，资金必须在初仓前完成到账与对账，持仓中不得提款补足信封或救援仓位。
- 交易所利润归集只在真实空仓、无挂单 / Unknown、费用与盈亏完成对账且余额超过冻结上沿时执行。
- 自动运营补充默认关闭，只为空仓账户的下一周期补到冻结目标；活动仓、清算压力、损失门或 KILL_SWITCH 不能触发自动提款。
- 人工与自动双向划转共用唯一 `capital_transfer_id` 和完整源端、在途、目的端状态；同一金额不能在两端重复计入。
- 风险权益纳入 Vault、自动利润归集和自动运营补充是三个独立开关，任一启用都不能推导另外两个已获授权。
- Capital Transfer Orchestrator 故障只冻结新划转，不能阻断止损、减仓和退出。

### 盈利证据

- 时间外净期望在完整成本后为正。
- 压力成本下达到策略合同预先登记的数值门。
- 收益集中度、单笔贡献、单标的集中度、板块集中度和市场状态依赖均处于预设边界。
- 小额实盘成本、滑点和回撤处于研究合同预先登记的容差。
- 扩容具有明确晋级、降级和停机门。

### AI 审核

- AI 没有密钥、下单权、参数修改权和资金权。
- 数据不可信、无法对账或硬风控拒绝时直接阻断；仅在硬条件合格但超出认证范围或低置信时转人工。
- 模型版本、输入、输出、置信度和回滚可审计。
- 低风险纯开仓代理、AI 预授权自动加仓和中风险分别独立认证；高风险保持人工。
- AI 中断或回滚不影响已有仓位退出。

---

## 二十三、已经固定与仍待研究的决策

### 已经固定

- 支持范围为 Binance 与 Hyperliquid 可发现的 U 本位永续合约，覆盖加密货币、美股及股指、贵金属、商品及商品期货参考资产；真实交易仍逐标的、逐执行域认证。
- 首批交易所为 Binance 与 Hyperliquid。
- Freqtrade 作为两个交易所的交易订单执行后端，Trading 保留风险、授权、OMS、控制动作与对账主权；Margin Controller 执行场内保证金动作，Capital Transfer Orchestrator 执行资金划转。
- 同时支持系统提案与 Web / PWA 人工提案，初仓均由人工明确批准，Risk Engine 可以拒绝超额风险。
- 人工决定入场，程序是受管账户唯一日常订单发送者。
- Observer、Proposer、Reviewer 是可叠加的基础用户标签，并建议分设 Operator、RiskAdmin、TreasuryAdmin、SystemAdmin；实际动作同时受 RBAC、ABAC、MFA、拒绝优先、自审和职责分离政策约束。
- 加仓通过开仓时预授权自动执行，不逐次审核。
- 低、中、高风险档位对应 3x、5x、10x 最大有效杠杆。
- 每笔交易冻结可配置总资金 `N%` 的 Trade Funding Envelope；1.5% 仅为示例，正式默认 `N` 待资金政策冻结。它是资金占用而非允许亏损，若需从 Vault 划转必须在初仓前到账。
- 默认 `1R_0 = Total Capital Snapshot_0 × 0.5%`；低 / 中 / 高的单笔最大计划损失分别为 1R / 2R / 3R，即 0.5% / 1.0% / 1.5%。
- 自动加仓默认关闭；启用后低 / 中 / 高默认最多 1 / 2 / 3 次，系统 V1 硬上限为三级。
- 默认盈利触发阶梯为冻结收益率 30% / 50% / 100%，只解锁对应候选；每次 Add 默认按当时 `E_campaign` 和 `P_executable` 加到冻结 `L_target`，只预先控制次数，不预设分层手数。
- 风险或流动性约束可以把 Add 安全缩量；零成交不消费次数，任何非零真实成交完整消费一次 Add。
- 回调是加仓过滤而不是撤销已成交 Add：里程碑到达后，只有回调时仍满足盈利、目标杠杆、趋势、授权和实时风险门才继续加仓；普通回调不自动移除任何已成交加仓层。
- `L_effective > L_max` 时由系统 `reduce-only` 动态去杠杆，不需要再次人工确认，也不消耗 Add 次数。
- 每次加仓前按包含最新未实现盈亏的 Current Portfolio MTM Equity 重算组合预算；本仓 Trade Worst-Case Loss 与冻结 / 动态单笔上限比较，各 Scope Worst-Case Loss 与对应风险簇、板块、场所或组合上限比较；正浮盈不自动扩权。
- 扩大授权上限重新人工批准；原授权包内的自动加仓消费已批准上限无需逐次审核，但每次执行仍需实时占用 Reserved Heat；风险收紧立即生效。
- 止损、回撤保护和紧急退出自动执行。
- Web / PWA 是主控制台，Telegram 是辅助入口。
- Telegram 只提供通知与受限操作；所有动作先经过 Trading，只有交易订单交给 Freqtrade，暂停 / 关闭加仓留在 Trading，保证金动作交给 Margin Controller，资金划转交给 Capital Transfer Orchestrator。
- Perptape 只做市场与信号，Trading 做风险与执行，RiskControl 只做资金闸门。
- Vault 是否同时计入 Current Portfolio MTM Equity 和 Extended Risk Equity 可以选择，默认不计入；即使计入也不能成为交易所保证金、提款授权或单仓救援资金。
- 每个交易场所保存可配置 `n%` 运营资金目标；真实空仓时可按独立开关自动归集已实现利润，自动运营补充默认关闭且只能用于空仓后的下一周期，人工双向划转共用同一资金授权与状态机。
- 当前订单执行由 Binance / Hyperliquid 各自的 VenueAdapter 组合 Freqtrade 后端完成；未来可选 CFD、股票或其他衍生品适配器，不承诺接入。
- 首期保证金拓扑固定为 Binance 全仓独立账户 / worker 与 Hyperliquid 逐仓独立 subaccount / worker；以后启用 Binance 逐仓或 Hyperliquid 全仓必须新增隔离账户、worker 和执行证书。
- 后期 AI 从影子、辅助逐步进入低风险有限代理。

### 仍需通过研究确定

- 不同周期突破、确认和趋势退出的最终参数。
- 正式 `trade_funding_pct = N%`、各场景资金信封占用规则、三档 `L_target / L_min / L_max`，以及组合 Heat 和回撤阈值；1R = 0.5% 与三档 1R / 2R / 3R 不属于待研究值。
- 布林带回调的周期、参考轨、偏差区间、确认规则、每级机会失效和保护距离；未来若研究 MFE / 高水位回撤，必须作为独立命名过滤器验证。
- 三种保证金能力的逐场所执行证书、可移除保证金公式、可选 normalization 缓冲与失败降级规则。
- 各交易场所运营资金 `n%`、上下沿、归集 / 补充限额、冷却期、链路和 Vault 最低保留额。
- 四个板块的正式风险限额、跨板块压力簇和 Vault 最大贡献比例。
- 不同标的与执行域的流动性、滑点、资金费率、交易时段和预言机门槛。
- 同一底层资产在 Binance、Hyperliquid Core 与 HIP-3 DEX 的优先执行规则。
- 授权的时间与价格偏离失效阈值。
- AI 置信度门槛、适用白名单和中风险认证条件。

这些未决参数不能靠主观一次确定，应通过策略合同、时间外验证、影子盘和小额实盘逐步冻结。

---

## 二十四、最终形态

最终系统不是一个看到突破就自动满仓的机器人，而是一套分权、可回滚的交易操作系统：

- Perptape 发现机会。
- 用户也可以把图表判断转换成人工提案；Trading 对系统和人工提案使用同一风险定仓。
- 用户可以同时拥有 Observer、Proposer、Reviewer 等多个标签，但权限、MFA、自审和职责分离仍逐动作判定。
- 人工在首期批准开仓、资金信封、档位最大损失和有限 Add 次数；每次 Add 由系统按当时目标杠杆差额计算数量。
- AI 后期先提供独立审核，再在低风险白名单内有限代理。
- Risk Engine 始终拥有最高风险否决权。
- Trading OMS 只生成已批准且实时仍安全的执行意图，Freqtrade 只执行这些意图。
- Binance、Hyperliquid Core 与各 HIP-3 DEX 使用包含最新未实现盈亏的 Current Portfolio MTM Equity 统一计算底层资产、板块和组合风险，但分别执行和故障隔离。
- RiskControl 守住长期资金；其合格 Vault 资金可选择性进入扩展风险预算，并可在独立资金授权下与空仓交易账户双向调度，但不参与逐笔仓位救援。
- 系统通过完整成本后的正期望、稳定执行和分级扩容持续积累证据，但不能证明或保证未来盈利。

这套架构的核心不是预测每一笔交易，而是让好机会获得受控风险，让错误快速结束，让盈利趋势可以扩展，同时确保任何人、任何 AI 和任何单个模块都无法绕过系统边界。

---

## 二十五、当前官方规则参考

- Binance USDⓈ-M Futures：[初始杠杆](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#change-initial-leverage)、[保证金模式](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#change-margin-type)、[逐仓仓位保证金增减](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade#modify-isolated-position-margin)、[名义价值与杠杆档位](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/account#notional-and-leverage-brackets)。

- Hyperliquid：[保证金与逐仓 / 全仓](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/margining)、[账户抽象模式](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/account-abstraction-modes)、[保证金调整接口](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/exchange-endpoint)、[资产与名义仓位杠杆档位](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/margin-tiers)、[清算机制](https://hyperliquid.gitbook.io/hyperliquid-docs/trading/liquidations)、[HIP-3 Builder-deployed Perpetuals](https://hyperliquid.gitbook.io/hyperliquid-docs/hyperliquid-improvement-proposals-hips/hip-3-builder-deployed-perpetuals)、[Perpetuals 元数据接口](https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api/info-endpoint/perpetuals)。

- Freqtrade：[交易所支持、Hyperliquid 与 HIP-3 配置差异](https://docs.freqtrade.io/en/2026.3/exchanges/)、[杠杆与单一账户边界](https://www.freqtrade.io/en/stable/leverage/)、[REST API](https://www.freqtrade.io/en/stable/rest-api/)、[Telegram 控制](https://www.freqtrade.io/en/stable/telegram-usage/)、[FreqUI](https://docs.freqtrade.io/en/latest/freq-ui/)。

交易所规则会变化。正式实现和每次部署前，必须重新核对官方文档与目标账户的实际返回值。
