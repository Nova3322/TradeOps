# 交易账户历史快照语义验收（2026-08-05）

## 复现与修复

- Hyperliquid 当前只读探针被上游限流，API 明确返回 `UPSTREAM_RATE_LIMITED` / `HYPERLIQUID_RATE_LIMITED`，因此页面只能展示 8 月 5 日 12:39 的最后快照。
- 修复前，最后快照中的“无持仓、无委托、无资金费”仍使用绿色成功空态，容易被误解为当前实时账户为空。
- 修复后，所有不能证明当前状态的历史空态使用黄色警示语义；连接摘要明确为“实时账户事实不可用；仅展示最后快照”。
- 可用余额补齐与权益一致的 USDC/USDT 币种，避免脱离单位解读金额。

## 当前事实边界

- Freqtrade 是执行底座，Hyperliquid worker 已配置，HIP-3 `xyz` 目录可用。
- 这些执行底座事实不等于实时账户连接健康，也不等于允许真实下单。
- 当前 Hyperliquid 账户只读连接仍受上游限流；页面保留最后快照但不把它标记为实时。
- 未执行订单、资金操作、签名、广播或 Gate 变更。

## 验收证据

- `01-hyperliquid-desktop.png`：桌面端连接分类、最后快照警示、币种与历史空态。
- `02-hyperliquid-390.png`：390 px，无横向溢出，警示语义和表格卡片完整。
- `03-hyperliquid-430.png`：430 px，无横向溢出，状态卡和历史数据完整。

## 测试

- API/Web/连接与运行时单元测试：76 passed。
- 隔离 PostgreSQL 多角色、Binance/Hyperliquid 只读和运行时同步集成测试：69 passed。
- 覆盖系统管理员、普通创建者、独立审核人、资金成员、无管理权限用户和禁用成员。

