# 今日、审核、Telegram 与权限验收（2026-08-05）

## 范围与目标

- 流程：今日 → 审核队列 → 系统状态 / Telegram 审核通知。
- 权限：系统管理员、普通创建者、独立审核人、资金权限成员、无管理权限用户、禁用成员。
- 安全：仅做读取、登录和页面导航；未批准或拒绝生产提案，未创建授权、订单或资金动作。

## 当前步骤

1. **今日：健康。** 管理员看到 33 笔非本人待审核，明确拆分为 32 笔系统机会和 1 笔人工判断；运行告警为零时没有重复待办。
2. **审核队列：健康。** 只列未投票且未过期提案，按到期时间排序；来源可筛选，并显示 `已审核人数 / 所需人数`。
3. **Telegram 状态：健康。** `/api/runtime/status` 显示长轮询 `HEALTHY`、线程运行、最近成功时间及连续失败为零；Web 审核仍是权威入口。
4. **成员权限：健康。** 仅系统管理员看见入口和成员数据；页面用岗位、状态和范围摘要呈现，不展开冗长权限说明。
5. **六身份：健康。** 创建者仅见今日/机会；审核人仅见今日/审核队列；资金成员仅见今日/资金；观察者仅见只读模块；禁用成员登录被拒绝；管理员可见全部管理入口。

## P0 / P1 / P2

- P0：本批未发现新的权限绕过、自动审核、订单或资金动作。
- P1（已修复）：Bot API HTTP 错误过去会被误报为网络不可达；现按认证、冲突、限流、网络和无效响应分类，并投影到系统状态。
- P1（已修复）：审核列表只有状态，没有 `0/2` 审核进度；现由后端统一统计并展示。
- P1（已修复）：Telegram `/todo` 在队列较大时一次输出过多；现只列最早到期 10 项，说明剩余数量并提供唯一 Web 审核入口。
- P2（已修复）：系统状态卡重复显示“消息通知 审核通知可用”；现简化为“审核通知 / 通知可用”。
- P2（待后续实机验收）：内置浏览器当前固定为 1422×800，无法切换移动视口；本批补了响应式静态检查，但不把它当作手机实机通过。

## 截图证据

- `01-review-queue-desktop.png`：审核队列首屏。
- `02-members-admin-desktop.png`：管理员成员权限页。
- `03-system-status-desktop.png`：系统状态首屏。
- `04-telegram-health-desktop.png`：Telegram 轮询健康卡及外部连接事实。

## 外部限制

- Freqtrade worker 未启动；只读控制台不能被表述为可执行交易。
- NoTilt 生产范围仍缺少受信任 Vault / 网络 / 白名单等非敏感配置，保持不可执行。
- Binance / Hyperliquid 探针可能受短时网络或限流影响；页面按每轮事实显示具体失败类别，后续轮询恢复后才重新标记只读已连接。
